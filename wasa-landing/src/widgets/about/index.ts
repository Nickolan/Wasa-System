/**
 * API pública de la slice `widgets/about` (FSD). `ABOUT_SECTIONS` es detalle
 * interno del widget y no se exporta acá (mismo patrón que `STEPS`/`TOOLS`,
 * D-9 de design.md).
 */
export { AboutWidget } from './ui/AboutWidget'
