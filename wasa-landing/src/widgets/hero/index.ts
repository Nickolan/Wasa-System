/**
 * API pública de la slice `widgets/hero` (FSD). Los consumidores importan
 * de acá, nunca de una ruta interna de `model/` o `ui/`.
 */
export { HeroWidget } from './ui/HeroWidget'
export type { HeroWidgetProps } from './ui/HeroWidget'
