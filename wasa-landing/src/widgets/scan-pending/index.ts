/**
 * API pública de la slice `widgets/scan-pending` (FSD). `SCAN_PENDING_COPY`
 * es detalle interno del widget y no se exporta acá (mismo patrón que
 * `STEPS` en `widgets/how-it-works`, D-9 de design.md).
 */
export { ScanPendingWidget } from './ui/ScanPendingWidget'
export type { ScanPendingWidgetProps } from './ui/ScanPendingWidget'
