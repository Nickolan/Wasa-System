/**
 * API pública de la slice `widgets/scan-form` (FSD). Los consumidores
 * importan de acá, nunca de una ruta interna de `model/` o `ui/`.
 */
export { SCAN_FORM_ANCHOR_ID } from './model/anchor'
export { ScanFormWidget } from './ui/ScanFormWidget'
export type { ScanFormWidgetProps } from './ui/ScanFormWidget'
