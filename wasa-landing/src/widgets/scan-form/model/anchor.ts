/**
 * Identificador estable del ancla de desplazamiento de la sección del
 * formulario de escaneo (D-2, D-5). Única fuente de verdad: `HeroWidget`
 * recibe este valor por prop desde `pages/LandingPage`, nunca lo importa
 * directamente de esta slice — ver D-2 de `design.md`.
 */
export const SCAN_FORM_ANCHOR_ID = 'scan-form'
