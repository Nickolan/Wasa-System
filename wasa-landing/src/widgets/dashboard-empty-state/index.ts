/**
 * API pública de la slice `widgets/dashboard-empty-state` (D-8, FSD). Los
 * consumidores importan de acá, nunca de una ruta interna.
 */
export { DashboardEmptyState } from './ui/DashboardEmptyState'
export { DASHBOARD_EMPTY_STATE_MESSAGE } from './model/content'
