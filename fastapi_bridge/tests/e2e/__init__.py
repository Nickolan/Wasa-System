"""Suite de humo end-to-end (CHANGE-22, `e2e-smoke-test`).

Opt-in por `WASA_E2E=1` (ver `conftest.py` de este paquete). NUNCA se colecta
ni se ejecuta como parte de la suite unitaria habitual de `fastapi_bridge/tests`.
"""
