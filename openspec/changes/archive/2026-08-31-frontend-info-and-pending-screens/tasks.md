# Tasks — `frontend-info-and-pending-screens`

> Todos los comandos se corren desde `wasa-landing/`.
> **Modo TDD estricto activo**: cada grupo de implementación arranca por el test (RED), sigue por el mínimo código que lo pone en verde (GREEN), triangula con un segundo caso y recién ahí refactoriza.
> Los grupos 2–4 tocan el camino crítico del escaneo: el grupo 1 (safety net) es obligatorio antes de modificar nada.

## 1. Safety net y checkpoint de decisiones

- [x] 1.1 Correr `npm run test:run` completo y registrar la línea de baseline (`N passed`) en las notas del change; si alguna suite ya falla antes de tocar nada, detenerse y reportarlo como fallo preexistente sin arreglarlo. **Baseline (2026-08-31): 51 test files, 560 tests passed, 0 failed.**
- [x] 1.2 Registrar el baseline por archivo de los tres tests del camino crítico —`tests/use-scan-form.test.tsx`, `tests/scan-form.test.tsx`, `tests/scan-form-widget.test.tsx`— con `npx vitest run tests/use-scan-form.test.tsx tests/scan-form.test.tsx tests/scan-form-widget.test.tsx`; verificar que los tres pasan. (Nota 2026-08-31: `tests/landing-page.test.tsx` y `pages/LandingPage` ya no existen — fueron retirados por un fix de deuda de tests aparte a este change, ver D-2 de `design.md`. Su equivalente vivo es `tests/app-hydration.test.tsx`, que no forma parte del camino crítico del escaneo y no requiere baseline especial acá.) **Baseline: 3 test files, 48 tests passed.**
- [x] 1.3 Confirmar con el usuario la redacción de la sección de privacidad de `/about` (design.md D-7, Open Question 1) y el rótulo/posición del link del `Navbar` (D-8, Open Question 2); dejar el texto acordado escrito en las notas del change antes de escribir copy. **Ya aprobado por el usuario en el checkpoint de governance (ver instrucciones del apply): D-7 texto exacto de privacidad, D-8 `NAV_LINKS` = [Inicio, Acerca de, Escanear].**

## 2. Quitar la redirección al Dashboard (`scan-submission` REMOVED, `scan-pending-screen` §persistencia)

- [x] 2.1 **RED** — En `tests/use-scan-form.test.tsx`, eliminar el bloque `describe('useScanForm — aceptación y redirección al Dashboard (8.1–8.6, D-11)')` completo, con sus `Object.defineProperty(window, 'location', …)` de `beforeEach`/`afterEach` y el import de `dashboardUrl`; en su lugar escribir un test que afirme que tras un `202` el hook expone `scanResponse` con la confirmación del Bridge y que **no** se asigna ninguna dirección a `window.location`. Verificar que el archivo compila y que el test nuevo describe el comportamiento aún no implementado (D-9).
- [x] 2.2 **GREEN** — Borrar de `features/scan-form/model/useScanForm.ts` el `useEffect` de redirección (líneas ~143-157), la constante `SUCCESS_REDIRECT_DELAY_MS` y el import de `dashboardUrl`; verificar con `npx vitest run tests/use-scan-form.test.tsx` que pasa.
- [x] 2.3 **TRIANGULATE** — Agregar el caso de no-navegación para la matriz completa de rechazos (`401`, `400`, `422`, `429`, `502` y fallo de red) sobre el mismo hook, verificando que ninguno navega ni setea `scanResponse`; verificar que los seis casos pasan.
- [x] 2.4 **RED→GREEN** — Escribir el test de no-regresión estructural (nuevo archivo `tests/scan-form-no-redirect.test.ts`, patrón de `tests/fsd-boundaries.test.ts`): ningún archivo bajo `src/features/scan-form/` importa `dashboardUrl` desde `@shared/config/env` ni contiene una asignación a `window.location`. Verificar que falla contra el código pre-2.2 (o contra un fixture que lo simule) y pasa contra el actual (D-9).
- [x] 2.5 Reformular `SCAN_SUCCESS_MESSAGE` en `useScanForm.ts` para que no prometa navegación al Dashboard y mencione la entrega por email (D-2), y actualizar en `tests/scan-form.test.tsx` la sustitución de `window.location` (se elimina) y las aserciones que dependían del texto viejo; verificar con `npx vitest run tests/scan-form.test.tsx`.

## 3. Elevar el estado de aceptación hasta la página (design.md D-1)

- [x] 3.1 **RED** — Test sobre `ScanForm`: recibiendo una prop opcional `onAccepted`, la invoca **exactamente una vez** con la respuesta del Bridge cuando el escaneo es aceptado, y no la invoca ante ningún rechazo. Verificar que falla (la prop todavía no existe).
- [x] 3.2 **GREEN** — Agregar la prop opcional `onAccepted?: (response: ScanResponse) => void` a `features/scan-form/ui/ScanForm.tsx`, disparada desde un `useEffect` sobre `scanResponse` (no desde el `try` del submit, D-1); verificar que 3.1 pasa.
- [x] 3.3 **TRIANGULATE** — Test de desmontaje: desmontar el componente antes de que corra el efecto no invoca `onAccepted` ni propaga error; y test de ausencia: sin la prop, el flujo de aceptación sigue funcionando y muestra la confirmación inline `role="status"` (camino por defecto — hoy sólo se ejercita en tests unitarios de `ScanForm`/`ScanFormWidget` renderizados sueltos, ya que `LandingPage` no existe más, D-2 actualizado). Verificar que ambos pasan.
- [x] 3.4 **RED→GREEN** — Reenviar la prop desde `widgets/scan-form/ui/ScanFormWidget.tsx` como `onScanAccepted?`, opcional, sin lógica propia; test en `tests/scan-form-widget.test.tsx` que verifica el reenvío y que el widget sin la prop se comporta igual que antes.
- [x] 3.5 Verificar que `tests/app-hydration.test.tsx` sigue en verde (heredó el único test con valor propio de `landing-page.test.tsx`, la aserción de que `<App/>` renderiza el heading de WASA); no depende de este grupo pero conviene confirmarlo no regresionó.

## 4. Widget de pantalla de espera (`scan-pending-screen`)

- [x] 4.1 **RED** — Test del copy como datos: `widgets/scan-pending/model/copy.ts` exporta las cadenas del encabezado, del estado, de la duración estimada y del aviso de email; el test afirma que ninguna está vacía, que todas están en español, que el texto de duración menciona los diez minutos como estimación y que el de email menciona la casilla de la cuenta del usuario. Verificar que falla (el módulo no existe).
- [x] 4.2 **GREEN** — Crear `widgets/scan-pending/model/copy.ts` con el texto de D-3; verificar que 4.1 pasa.
- [x] 4.3 **RED** — Test de `ScanPendingWidget` (nuevo `tests/scan-pending-widget.test.tsx`): renderizado con una respuesta de escaneo, expone una región de estado (`role="status"`) con nombre accesible y encabezado propio, y muestra los tres hechos obligatorios de la spec. Verificar que falla.
- [x] 4.4 **GREEN** — Crear `widgets/scan-pending/ui/ScanPendingWidget.tsx` + `widgets/scan-pending/index.ts`, consumiendo `copy.ts` y sin literales de texto en el JSX; verificar que 4.3 pasa.
- [x] 4.5 **TRIANGULATE** — Tests adicionales sobre el widget: (a) muestra el `scan_id` como referencia y **no** muestra el `message` crudo de la respuesta del Bridge; (b) el texto no le pide al usuario mantener la página abierta; (c) cualquier indicador visual de progreso es decorativo (`aria-hidden`) y el estado se entiende con el texto solo. Verificar que los tres pasan.
- [x] 4.6 **RED→GREEN** — Test y luego implementación de las salidas (D-4): un `<Link to="/">` "Volver al inicio" (navegación interna, requiere envolver en Router en el test) y un enlace secundario al Dashboard leído de una única constante del widget; el test verifica que hay al menos una salida activable y que activarla no emite ninguna solicitud al Bridge.
- [x] 4.7 **REFACTOR** — Concentrar las clases Tailwind en constantes al tope del archivo, siguiendo el patrón de `ScanFormWidget.tsx` (D-13 de CHANGE-19); verificar que los tests del grupo 4 siguen verdes.

## 5. Cablear la pantalla de espera en `ScanPage`

- [x] 5.1 **RED** — Test de `ScanPage` (nuevo `tests/scan-page.test.tsx`): con sesión activa, tras una aceptación `202` la pantalla de espera está visible **y** los campos del formulario y su control de envío ya no están presentes en el documento; ante un rechazo, el formulario sigue presente con sus valores. Verificar que falla.
- [x] 5.2 **GREEN** — En `pages/ScanPage/index.tsx`, sostener `acceptedScan` con `useState`, pasar un `onScanAccepted` estable (`useCallback`) al `ScanFormWidget`, y renderizar `<ScanPendingWidget>` en lugar del widget de formulario cuando hay aceptación; verificar que 5.1 pasa.
- [x] 5.3 **TRIANGULATE** — Test de que el encabezado de la página también cambia ("Iniciar Escaneo" deja de anunciarse sobre una pantalla de espera, D-1) y de que la transición no navega el navegador a ninguna dirección ni recarga; verificar que pasan.
- [x] 5.4 Test de que un remontaje de la página vuelve al formulario (estado en memoria, D-5), documentando el trade-off en el test mismo; verificar que pasa.

## 6. Página informativa `/about` (`about-page`)

- [x] 6.1 **RED** — Test del contenido como datos (`widgets/about/model/sections.ts`): exporta exactamente las cuatro secciones obligatorias (qué es WASA, herramientas, flujo end-to-end, datos y privacidad), cada una con `title` no vacío y `body` con al menos un párrafo no vacío; la sección de herramientas nombra OWASP ZAP, Nuclei, ffuf y SQLMap; la del flujo menciona la duración estimada y la entrega por email; el conjunto declara el uso sobre objetivos autorizados. Verificar que falla.
- [x] 6.2 **GREEN** — Crear `widgets/about/model/sections.ts` con el contenido acordado en 1.3, redactado contra la tabla de comportamientos verificados de D-7 (sin prometer cifrado en reposo, retención, borrado a pedido ni certificaciones); verificar que 6.1 pasa.
- [x] 6.3 **RED→GREEN** — Test y luego implementación de `widgets/about/ui/AboutWidget.tsx` + `index.ts`: cada sección se renderiza como región identificable con su propio encabezado, iterando sobre los datos y sin literales de texto en el JSX.
- [x] 6.4 **RED→GREEN** — Test y luego implementación de `pages/AboutPage/index.tsx` (nuevo `tests/about-page.test.tsx`): compone `AboutWidget` + `FooterWidget`, se renderiza completa sin sesión activa, no monta ningún muro de autenticación y no emite ninguna solicitud HTTP.
- [x] 6.5 **TRIANGULATE** — Test de que el contenido es idéntico con sesión activa y sin ella (misma spec, escenario "La sesión no cambia el contenido"); verificar que pasa.
- [x] 6.6 **REFACTOR** — Verificar la adaptabilidad: ninguna clase de la página fija ancho ni ancho mínimo en píxeles y las disposiciones multi-columna parten de una columna, siguiendo el criterio ya verificado en `tests/landing-responsive.test.ts`; extender ese test o agregar el equivalente para `/about`.

## 7. Ruta y navegación

- [x] 7.1 **RED** — Test de routing (extender `tests/app-*.test.tsx` o archivo nuevo): navegar a `/about` renderiza la página informativa y no `HomePage` ni `ScanPage`. Verificar que falla.
- [x] 7.2 **GREEN** — Agregar `<Route path="/about" element={<AboutPage />} />` en `app/App.tsx`; verificar que 7.1 pasa.
- [x] 7.3 **RED→GREEN** — Test y luego implementación de la entrada nueva en `NAV_LINKS` de `widgets/navbar/ui/Navbar.tsx` (D-8): la entrada existe en la presentación de escritorio y en la de móvil, navega internamente (sin recarga, sin ventana nueva) y se marca como activa cuando `location.pathname` es `/about`.
- [x] 7.4 **TRIANGULATE** — Verificar que las entradas existentes ("Inicio", "Escanear") y el botón externo al Dashboard conservan su comportamiento; correr los tests de navbar/landing y comparar contra el baseline de 1.1.

## 8. Cierre: fronteras, sincronización documental y verificación global

- [x] 8.1 Correr `npx vitest run tests/fsd-boundaries.test.ts tests/structure.test.ts tests/shared-domain-agnostic.test.ts tests/manifest.test.ts` y verificar que las slices nuevas (`widgets/scan-pending`, `widgets/about`, `pages/AboutPage`) no violan la dirección de capas ni la regla de que `shared/` no conoce el dominio. (Nota: `tests/structure.test.ts` tenía un manifiesto hardcodeado de las slices de `widgets/` de CHANGE-19 — se actualizó para incluir `about` y `scan-pending`, las dos slices nuevas de este change.)
- [x] 8.2 Verificar que `shared/config/env.ts` **no** cambió y que `VITE_DASHBOARD_URL` sigue siendo obligatoria (`npx vitest run tests/env.test.ts` en verde) — su baja es CHANGE-26, no este change.
- [x] 8.3 Editar a mano el `## Purpose` de `openspec/specs/scan-submission/spec.md` para quitar la mención a "la redirección al Dashboard de RN-WS-08" y describir la entrega de control a la pantalla de espera (D-10); verificar que el texto resultante no menciona ninguna navegación automática.
- [x] 8.4 Anotar la enmienda de **RN-WS-08** en `knowledge-base/05_reglas_de_negocio.md` (la aceptación ya no redirige al Dashboard; el usuario queda en una pantalla de espera y el reporte llega por email), con referencia a este change; verificar que la KB no sigue describiendo el comportamiento retirado.
- [x] 8.5 Marcar `[CHANGE-24]` como completado en `CHANGES.md` y tildar sus cinco Criterios de Aceptación; verificar que cada tilde tiene su evidencia (test o comando) identificada.
- [x] 8.6 Verificación global: `npm run build` (incluye `tsc -b`) sin errores, `npm run lint` sin hallazgos nuevos y `npm run test:run` con un total ≥ al baseline de 1.1 y cero fallos. **Resultado (2026-08-31): `npm run build` limpio (tsc -b + vite build); `npm run lint` con un único warning `react(set-state-in-effect)` en `Navbar.tsx` — confirmado preexistente (mismo warning contra el `git stash` del baseline, sólo se corrió de línea 29 a 35 por un comentario nuevo); `npm run test:run`: 60 archivos, 611 tests, 0 fallos (baseline: 51 archivos, 560 tests) — 51 tests nuevos, 9 archivos de test nuevos, ningún archivo perdido salvo el retiro ya autorizado de `landing-page.test.tsx` (fuera del scope de este change).**
- [ ] 8.7 Verificación manual en `npm run dev`: enviar un escaneo con sesión activa y confirmar que el navegador **no** sale de la aplicación, que la pantalla de espera queda a la vista indefinidamente y que sus salidas funcionan; abrir `/about` desde el `Navbar` en escritorio y en móvil y leer las cuatro secciones. **PARCIAL (2026-08-31, verificado con Playwright headless contra `npm run dev`):** confirmado en escritorio — el `Navbar` muestra `Inicio · Acerca de · Escanear` en ese orden (D-8), `/about` renderiza las cuatro secciones con el copy de privacidad exacto aprobado (D-7, `"proyecto académico"` presente), `/scan` sigue funcionando (muro de sesión visible, sin romperse) y no hay errores de consola en ninguna de las tres rutas. **Sigue pendiente**: el flujo real de envío de un escaneo con sesión activa hasta ver la pantalla de espera (requiere login real contra el FastAPI Bridge + Postgres corriendo, no disponible en este entorno) y la vista móvil específica de `/about` (el comportamiento responsive general ya está cubierto por `tests/landing-responsive.test.ts`, grupo 6, pero no se miró visualmente en viewport móvil). Screenshots en el scratchpad de la sesión.
