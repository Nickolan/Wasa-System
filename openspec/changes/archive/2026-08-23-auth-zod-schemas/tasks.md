> **Modo TDD estricto activo** (Vitest). Cada grupo sigue el ciclo RED → GREEN → TRIANGULATE → REFACTOR: el test se escribe **antes** que el módulo de producción y debe fallar por ausencia del símbolo, no por un error de sintaxis. Todos los comandos se ejecutan desde `wasa-landing/`.
>
> Tests nuevos: `tests/auth-schemas.test.ts` (schemas y política) y `tests/auth-schemas-parity.test.ts` (paridad con el Bridge). Módulos nuevos: `src/entities/user/`.

## 1. Red de seguridad

- [x] 1.1 Correr `npm run test:run` y anotar el baseline de la suite completa ("N tests passing"). Si algo ya falla, **detenerse** y reportarlo como fallo preexistente sin arreglarlo. → Baseline: **137 tests passing** (13 test files).
- [x] 1.2 Correr `npx tsc -b` y confirmar que compila limpio antes de tocar nada (baseline de tipos). → Limpio, sin errores.
- [x] 1.3 Releer el contrato del Bridge en `fastapi_bridge/schemas/auth_schemas.py` y confirmar los dos valores a replicar: `REGISTER_PASSWORD_MIN_LENGTH = 8` y `_BCRYPT_MAX_PASSWORD_BYTES = 72` (D-1, D-7). Si alguno cambió respecto de lo anotado en el design, actualizar el design antes de seguir. → Confirmado: ambos valores coinciden exactamente con el design, sin cambios necesarios.

## 2. Política de contraseña compartida (`model/passwordRules.ts`)

- [x] 2.1 **RED**: en `tests/auth-schemas.test.ts`, escribir los tests de `utf8ByteLength` importándola de `@entities/user`: `"abc"` → 3, `"é"` → 2, `"🔒"` → 4, `""` → 0. Ejecutar y confirmar que fallan por módulo inexistente. → Confirmado: fallo por `Failed to resolve import "@entities/user"`.
- [x] 2.2 **GREEN**: crear `src/entities/user/model/passwordRules.ts` con `PASSWORD_MIN_LENGTH = 8`, `PASSWORD_MAX_BYTES = 72` y `utf8ByteLength(value: string): number` implementada con `new TextEncoder().encode(value).length` (D-1). Ejecutar hasta verde. → 4/4 tests verdes.
- [x] 2.3 **TRIANGULATE**: agregar el test que separa bytes de caracteres — `"🔒".repeat(19)` tiene `.length === 38` pero `utf8ByteLength` devuelve `76` — y el caso de frontera `utf8ByteLength` de una cadena de exactamente 72 bytes ASCII → 72. Verificar que una implementación con `.length` haría fallar estos tests. → 6/6 tests verdes.
- [x] 2.4 **GREEN**: exportar desde el mismo módulo el schema base de contraseña con el techo aplicado (`.refine(v => utf8ByteLength(v) <= PASSWORD_MAX_BYTES, { message: 'La contraseña no puede superar los 72 bytes.' })`), listo para que ambos schemas lo compongan (D-2, D-12). → `passwordWithByteCeiling` exportado.
- [x] 2.5 **REFACTOR**: dejar en el módulo un comentario que nombre el origen del techo (bcrypt, D-2 de CHANGE-02) y su contraparte en el backend, sin repetir el número en ningún otro archivo del frontend. → Comentario agregado en `passwordRules.ts`.

## 3. `loginSchema`

- [x] 3.1 **RED**: tests de `loginSchema` — `{ email: "not-email", password: "x" }` lanza `ZodError` y el issue apunta a `email`; `{ email: "user@test.com", password: "" }` falla sobre `password`. Confirmar que fallan por módulo inexistente. → Confirmado (falla porque `loginSchema` importado es `undefined`, módulo aún no existe).
- [x] 3.2 **GREEN**: crear `src/entities/user/model/loginSchema.ts` con `email: z.string().trim().email(...)` y `password` = schema base de 2.4 con `.min(1, ...)` (D-3, D-11, D-12). Ejecutar hasta verde. → 2/2 verdes. Nota de implementación: `passwordWithByteCeiling` se redefinió como función `(base: z.ZodString) => ZodEffects` en vez de un valor preconstruido, porque un `.refine()` ya aplicado no admite encadenar `.min()` después (ZodEffects no tiene ese método) — necesario para que el chequeo de longitud builtin corra antes que el refine de bytes (ver design.md, comportamiento de Zod v3).
- [x] 3.3 **TRIANGULATE**: agregar (a) `{ email: "user@test.com", password: "x" }` **pasa** — el login NO exige 8 caracteres, con el porqué de D-3 escrito en el nombre del test o en un comentario al lado; (b) una contraseña de más de 72 bytes en el login **falla** (el techo también rige acá); (c) un email con espacios alrededor pasa y se devuelve recortado. → 5/5 verdes.
- [x] 3.4 **REFACTOR**: verificar que el módulo no repite el literal 72 ni el 8, y que toma todo de `passwordRules.ts`. → Verificado: `loginSchema.ts` no contiene los literales `72` ni `8`.

## 4. `registerSchema`

- [x] 4.1 **RED**: tests de los tres criterios de aceptación del roadmap — `password/confirmPassword = "1234567"` lanza `ZodError` sobre `password` (< 8); `password: "pass1234"` con `confirmPassword: "diferente"` lanza `ZodError` **sobre `confirmPassword`**; email inválido lanza `ZodError` sobre `email`. Confirmar RED. → Confirmado: los 3 tests fallan porque `registerSchema` importado es `undefined`.
- [x] 4.2 **GREEN**: crear `src/entities/user/model/registerSchema.ts`: objeto con `email` (trim + email), `password` (schema base + `.min(PASSWORD_MIN_LENGTH, ...)`), `confirmPassword` (`.min(1, ...)`), más `.superRefine` que compara y emite el issue con `path: ['confirmPassword']` (D-4). Sin `.strict()` (D-5). Ejecutar hasta verde. → 3/3 verdes.
- [x] 4.3 **TRIANGULATE — fronteras de longitud**: 8 caracteres exactos **pasa**; 7 falla; una contraseña ASCII de 73 caracteres falla; una de exactamente 72 bytes pasa. → 4/4 verdes.
- [x] 4.4 **TRIANGULATE — bytes vs caracteres**: `"🔒".repeat(19)` (38 caracteres, 76 bytes) como contraseña confirmada **falla** sobre `password`. Este es el test que un `.max(72)` dejaría pasar: sin él, la paridad con el Bridge no está cubierta. → 1/1 verde.
- [x] 4.5 **TRIANGULATE — confirmación**: confirmación vacía falla sobre `confirmPassword`; confirmación idéntica a una contraseña válida pasa; y el caso combinado (contraseña de 7 caracteres **y** confirmación distinta) reporta **los dos** issues en la misma pasada (comportamiento verificado de Zod v3, D-4). → 3/3 verdes, incluido el caso combinado.
- [x] 4.6 **TRIANGULATE — claves desconocidas**: parsear un objeto válido con una clave extra **pasa** y la salida de `parse` **no** contiene esa clave (D-5: el descarte por defecto es lo que mantiene limpio el cuerpo que viaja al Bridge con `extra="forbid"`). → 1/1 verde.
- [x] 4.7 **REFACTOR**: unificar los mensajes en español (D-12) y revisar que no queden literales duplicados entre `loginSchema` y `registerSchema`. → Verificado: ningún literal `72`/`8` fuera de `passwordRules.ts`; mensajes ya en español y consistentes entre ambos schemas.

## 5. Tipos y API pública de la slice

- [x] 5.1 Crear `src/entities/user/model/types.ts` con `UserRegister` (`email`, `password`, `confirmPassword`), `UserLogin` (`email`, `password`), `UserRegisterRequest = Omit<UserRegister, 'confirmPassword'>`, `TokenResponse` (`access_token: string`, `token_type: 'bearer'`, `expires_in: number`) y `AuthApiError` (`type: string`, `title: string`, `status: number`, `detail: string | null`, `instance: string`) — nombres del cable en `snake_case`, sin renombrar (D-10). → Creado.
- [x] 5.2 Agregar en `loginSchema.ts` y `registerSchema.ts` los guards de tipo `Assert<Equals<z.infer<typeof schema>, Tipo>>` de D-6, exportados como **tipos** (no como constantes: `noUnusedLocals` está activo). Verificar que funcionan: introducir temporalmente un campo de más en el schema, comprobar que `npx tsc -b` falla, y revertir. → **Desviación del design.md, corrección necesaria**: el `Equals<A,B>` tal como estaba escrito en el design (rama negativa `never`) NO detecta ninguna divergencia — verificado empíricamente con 3 casos aislados (campo de más, campo de menos, tipo distinto en el mismo campo): los tres compilan limpio con `never`, porque `never` satisface trivialmente cualquier restricción genérica (`T extends true`). Se corrigió a `false` en la rama negativa (en `loginSchema.ts`, `registerSchema.ts` y en `design.md`); con `false`, los 3 casos fallan `tsc` como se espera. Verificado en el propio `registerSchema.ts`: se introdujo un campo `temporaryExtraFieldForGuardTest`, `npx tsc -b --force` falló con `Type 'false' does not satisfy the constraint 'true'` en la línea del guard, y se revirtió; `tsc -b --force` vuelve a compilar limpio.
- [x] 5.3 Crear `src/entities/user/index.ts` como API pública de la slice: re-exporta los dos schemas, los tipos y las constantes/utilidades de política (D-8). Confirmar que los tests importan de `@entities/user` y no de rutas internas. → Creado; todos los tests nuevos importan de `@entities/user`.
- [x] 5.4 **RED → GREEN**: test de que el cuerpo de la petición de registro no lleva la confirmación — construir un `UserRegisterRequest` a partir de un `UserRegister` válido descartando `confirmPassword`, y verificar que sus claves son exactamente `['email', 'password']`. → 1/1 verde. Nota: `types.ts`/`index.ts` (5.1/5.3) ya existían al escribir este test porque son declaraciones de tipo sin lógica de rama — no hay "RED por módulo inexistente" que preceda a este caso; el test sí ejerce el comportamiento real (destructuring + tipado) y habría fallado si `UserRegisterRequest` no excluyera `confirmPassword`.

## 6. Paridad verificada con el Bridge

- [x] 6.1 **RED**: crear `tests/auth-schemas-parity.test.ts` que lea `../fastapi_bridge/schemas/auth_schemas.py` (ruta relativa a `wasa-landing/`), extraiga por regex `REGISTER_PASSWORD_MIN_LENGTH` y `_BCRYPT_MAX_PASSWORD_BYTES`, y los compare con `PASSWORD_MIN_LENGTH` y `PASSWORD_MAX_BYTES` (D-7). → Confirmado: falla por `Failed to resolve import "./support/pythonConstants"` (helper aún no existía).
- [x] 6.2 **GREEN**: hacer pasar el test. Si hay divergencia real, corregir el **frontend** para alinearlo con el Bridge; el backend es la garantía, el cliente la refleja. → 2/2 verdes, sin divergencia real (8 y 72 coinciden).
- [x] 6.3 **TRIANGULATE — modo de fallo**: verificar que si el archivo del backend no existe o la regex no encuentra una constante, el test falla con un mensaje que **nombra** la constante que no pudo leer, en vez de saltearse silenciosamente (D-7). Ejercitar el helper de extracción con un texto de prueba sin la constante. → 2/2 verdes.
- [x] 6.4 **REFACTOR**: extraer la lectura/extracción a un helper de `tests/support/` si el test queda con lógica de parseo mezclada con las aserciones, siguiendo el patrón ya usado en `tests/support/`. → Ya extraído desde el GREEN de 6.2 (`tests/support/pythonConstants.ts`).

## 7. Estructura, fronteras y limpieza

- [x] 7.1 Eliminar `src/entities/.gitkeep` (la capa dejó de estar vacía) y actualizar en `tests/structure.test.ts` la aserción "`src/entities` contiene solo `.gitkeep`" para que describa los módulos de la slice `user` (D-9). Dejar intactas las aserciones de `src/features`, `src/shared/ui` y `src/shared/api`. → `.gitkeep` eliminado; `structure.test.ts` actualizado con 3 aserciones nuevas sobre `src/entities/user/`. 29/29 verdes.
- [x] 7.2 Correr `npm run test:run` y confirmar que `tests/fsd-boundaries.test.ts` sigue verde: ningún módulo de `entities/` importa de `app`, `pages`, `widgets` o `features`. → 6/6 verdes, sin cambios necesarios.
- [x] 7.3 Agregar en `tests/auth-schemas.test.ts` la verificación de pureza de la slice: ningún archivo de `src/entities/user/` importa React, ni un cliente HTTP, ni el store de sesión, ni menciona `localStorage`. → Implementado con el parser de TS (`getImportedModules` de `tests/support/fsd.ts`) para distinguir un import real de "react" de una mención en comentario a "react-hook-form" (falso positivo detectado y corregido durante el propio ciclo RED/GREEN de este test). 18/18 verdes.

## 8. Verificación final y sincronización del roadmap

- [x] 8.1 `npx tsc -b` sin errores (criterio de aceptación del roadmap: `tsc --noEmit` limpio en `entities/user/`). → Limpio.
- [x] 8.2 `npm run test:run` en verde, con el conteo de tests mayor al baseline de 1.1 y **cero** tests preexistentes rotos. → **178 tests passing** (17 test files), frente al baseline de 137 (+41 tests nuevos, 0 rotos).
- [x] 8.3 `npm run lint` (oxlint) sin hallazgos nuevos y `npm run build` exitoso. → oxlint sin hallazgos; `npm run build` exitoso (`tsc -b && vite build`).
- [x] 8.4 Marcar en `tasks.md` cada tarea completada y anotar en `CHANGES.md`, bajo CHANGE-14, la nota de implementación: la slice quedó con un cuarto módulo (`model/passwordRules.ts`) y un `index.ts` de API pública respecto de los tres archivos listados en el scope original (D-2, D-8), y se agregó `UserRegisterRequest` para que `confirmPassword` no viaje al Bridge. → Hecho; ver también la corrección aplicada al guard de tipo de D-6 (nota en 5.2) y la corrección espejo en `design.md`.
