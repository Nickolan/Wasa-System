> **Modo TDD estricto activo** (Vitest). Cada grupo sigue el ciclo RED → GREEN → TRIANGULATE → REFACTOR: el test se escribe **antes** que el módulo de producción y debe fallar por ausencia del símbolo, no por un error de sintaxis. Todos los comandos se ejecutan desde `wasa-landing/`.
>
> **Governance ALTO**: antes de escribir código, releer `design.md` §Decisions. Las cuatro decisiones que se apartan de la letra de `CHANGES.md` son D-1 (la credencial entra por inyección, no importando el `authStore` desde `shared/`), D-7 (`submitScan` lanza una clase de error, no el cuerpo RFC 7807 pelado), D-8 (el `logout` del `401` lo hace el interceptor, no el hook) y D-9 (`setValueAs`, no `valueAsNumber`). Si alguna no se comparte, discutirla **antes** de implementarla.
>
> **Módulos nuevos**: `src/shared/api/problemDetails.ts`, `src/shared/api/axiosInstance.ts`, `src/app/providers/` (cableado), `src/features/scan-form/` (`api/submitScan.ts`, `model/useScanForm.ts`, `ui/ScanForm.tsx`, `index.ts`).
> **Tests nuevos**: `tests/problem-details.test.ts`, `tests/api-client.test.ts`, `tests/submit-scan.test.ts`, `tests/use-scan-form.test.tsx`, `tests/scan-form.test.tsx`.
> **Archivos existentes que se tocan**: `src/entities/scan/model/types.ts`, `src/entities/user/model/types.ts`, `src/app/App.tsx`, `tests/structure.test.ts`, `tests/scan-schema.test.ts`, y los `.gitkeep` de `src/shared/api/`, `src/features/` y `src/app/providers/`.

## 1. Red de seguridad

- [x] 1.1 Correr `npm run test:run` y anotar el baseline de la suite completa ("N tests passing"). Si algo ya falla, **detenerse** y reportarlo como fallo preexistente sin arreglarlo. — Baseline: 270 tests passing (26 test files), 0 failing.
- [x] 1.2 Correr `npx tsc -b` y confirmar que compila limpio antes de tocar nada (baseline de tipos). — Limpio, exit 0, sin salida.
- [x] 1.3 Releer `src/entities/scan/index.ts` y confirmar los nombres exportados que este change consume (`scanSchema`, las seis constantes de rango/default, los cinco mensajes, y los tipos `ScanForm`/`ScanRequest`/`ScanResponse`/`ScanApiError`). Si algo difiere de lo que asume `design.md` §Context, actualizar el design **antes** de seguir. — Confirmado: todos los nombres coinciden exactamente con lo que asume `design.md` §Context. Sin diferencias.
- [x] 1.4 Confirmar contra `fastapi_bridge/api/v1/scan/router.py` que la ruta es `POST /api/v1/scan/start` con `status_code=202`, y contra `src/shared/config/env.ts` que `apiBaseUrl` es el **origen** sin prefijo de versión — de ahí sale que el path del cliente lleve `/api/v1` completo. Anotar el resultado en la tarea. — Confirmado: `router.py` declara `APIRouter(prefix="/api/v1/scan")` + `@router.post("/start", status_code=202, ...)` → ruta absoluta `POST /api/v1/scan/start`. `env.ts` documenta `apiBaseUrl` como "Origen del FastAPI Bridge (sin prefijo de versión de API — D-3)". El cliente debe despachar a `${apiBaseUrl}/api/v1/scan/start`.

## 2. Contrato de error compartido (D-4, D-5)

- [x] 2.1 **RED**: en `tests/problem-details.test.ts`, escribir que `isProblemDetails` importado de `@shared/api/problemDetails` devuelve `true` para un cuerpo con los cinco miembros RFC 7807 bien tipados. Ejecutar y confirmar que falla por módulo inexistente. — Confirmado RED: "Failed to resolve import @shared/api/problemDetails".
- [x] 2.2 **GREEN**: crear `src/shared/api/problemDetails.ts` con la interfaz `ProblemDetails` (`type`, `title`, `status`, `detail: string | null`, `instance`) y el guard `isProblemDetails(value: unknown): value is ProblemDetails`. Ejecutar hasta verde. — 8/8 tests verdes.
- [x] 2.3 **TRIANGULATE — `detail` nulo**: un cuerpo por lo demás válido con `detail: null` se reconoce como Problem Details (el Bridge lo emite nulo cuando el estado y el título alcanzan).
- [x] 2.4 **TRIANGULATE — lo que no es Problem Details**: `undefined`, `null`, una cadena, un arreglo, un objeto sin `status` y un objeto con `status: '401'` (string) devuelven `false`, y **ninguna** de las seis invocaciones lanza.
- [x] 2.5 Reemplazar el cuerpo de `ScanApiError` en `src/entities/scan/model/types.ts` por `export type ScanApiError = ProblemDetails` con `import type` desde `@shared/api/problemDetails`, conservando el nombre exportado por `src/entities/scan/index.ts`. Verificar con `npx tsc -b` limpio. — Hecho, `tsc -b` limpio.
- [x] 2.6 Hacer lo mismo con `AuthApiError` en `src/entities/user/model/types.ts` (D-4: unificar una sola slice no elimina la duplicación, solo la mueve). Verificar con `npx tsc -b` limpio. — Hecho, `tsc -b` limpio.
- [x] 2.7 Retirar de `tests/scan-schema.test.ts` el guard de tipo entre slices `ScanApiError`/`AuthApiError` (D-13): con el alias no queda nada que pueda divergir. Dejar en su lugar un test que afirme que ambos alias resuelven al **mismo** contrato compartido — asignar un `ProblemDetails` a una variable de cada tipo y viceversa. Ejecutar la suite. — Hecho, 53/53 verdes (scan-schema.test.ts + problem-details.test.ts).
- [x] 2.8 **Verificación del punto 3 de D-4**: comprobar que ya no queda ninguna otra declaración de la forma de cinco miembros en `src/` (buscar `instance` junto a `title` en declaraciones de tipo). Debe haber exactamente una. — Confirmado: `grep -rn "instance: string" src/` da exactamente una coincidencia, en `shared/api/problemDetails.ts`.

## 3. Cliente HTTP — instancia, inyección y credencial saliente (D-1, D-2, D-3)

- [x] 3.1 **RED**: en `tests/api-client.test.ts`, escribir que la instancia exportada por `@shared/api/axiosInstance` tiene `defaults.baseURL` igual a `apiBaseUrl` de `@shared/config/env`. Confirmar RED por módulo inexistente. — Confirmado RED: "Failed to resolve import @shared/api/axiosInstance".
- [x] 3.2 **GREEN**: crear `src/shared/api/axiosInstance.ts` con `axios.create({ baseURL: apiBaseUrl })` y exportar la instancia. Ejecutar hasta verde.
- [x] 3.3 **RED**: escribir que, tras `configureApiClient({ getToken: () => 'tok', onUnauthorized })`, una petición emitida por la instancia sale con `Authorization: Bearer tok`. Usar un adaptador Axios de prueba (`instance.defaults.adapter`) o un interceptor de captura para inspeccionar la config saliente, sin red real. Confirmar RED.
- [x] 3.4 **GREEN**: agregar `configureApiClient(config)` que **asigna referencias de módulo**, y registrar el interceptor de request **a nivel de módulo** (D-2: la idempotencia sale de la forma, no de un flag). El interceptor invoca `getToken()` en cada petición. Ejecutar hasta verde. — 15/15 tests verdes en `api-client.test.ts`.
- [x] 3.5 **TRIANGULATE — sin sesión**: con `getToken: () => null`, la petición sale **sin** la cabecera `Authorization` (no con una cabecera vacía ni con el texto `Bearer null`).
- [x] 3.6 **TRIANGULATE — sin cablear**: sin haber llamado nunca a `configureApiClient`, la petición se emite sin cabecera y no se propaga ningún error de configuración. — **Corregido en la auditoría**: el test original no ejercitaba el estado "nunca configurado" (el `beforeEach` del archivo ya había asignado los proveedores) y su propio comentario lo admitía. Ahora carga una instancia fresca del módulo con `vi.resetModules()` + `import()` dinámico, con el estado de módulo en `null`, y triangula también el `401` sin cablear.
- [x] 3.7 **TRIANGULATE — la credencial se lee por petición** (D-3): con un proveedor que devuelve `'a'` y luego `'b'`, dos peticiones consecutivas llevan `Bearer a` y `Bearer b`. Este es el test que una captura del token al cablear no pasaría.
- [x] 3.8 **TRIANGULATE — idempotencia** (D-2): llamar a `configureApiClient` dos veces y verificar que la petición lleva **una sola** cabecera de autorización.

## 4. Cliente HTTP — reacción al `401` y distinción red/rechazo (D-6, D-8)

- [x] 4.1 **RED**: escribir que una respuesta `401` invoca exactamente una vez el `onUnauthorized` inyectado **y** que la promesa de la petición igualmente rechaza (no se resuelve como éxito). Confirmar RED.
- [x] 4.2 **GREEN**: agregar el interceptor de response a nivel de módulo: ante `error.response?.status === 401` invocar `onUnauthorized?.()` y re-lanzar el error. Ejecutar hasta verde. — Implementado junto con 3.4 (mismo módulo); 15/15 verdes.
- [x] 4.3 **TRIANGULATE — los demás rechazos no cierran sesión**: `400`, `422`, `429`, `502` y `500` **no** invocan `onUnauthorized`, y la promesa rechaza igual en los cinco casos.
- [x] 4.4 **TRIANGULATE — el éxito no toca la sesión**: una respuesta `202` no invoca `onUnauthorized` y resuelve con su cuerpo.
- [x] 4.5 **TRIANGULATE — fallo sin respuesta**: un fallo de red (error de Axios sin `response`) no invoca `onUnauthorized` y el error que llega al llamador es distinguible de un rechazo con estado **por la ausencia de `response`**, no por su mensaje (D-6).
- [x] 4.6 **REFACTOR**: revisar que el módulo no importe nada de `@app`, `@pages`, `@widgets`, `@features` ni `@entities`, y que no mencione `localStorage`. Correr `tests/fsd-boundaries.test.ts` y confirmar cero violaciones. — `tests/fsd-boundaries.test.ts`: 6/6 verdes, cero violaciones.

## 5. Cableado en la capa `app` (D-1, D-2, D-3)

- [x] 5.1 **RED**: en `tests/api-client.test.ts` (o un archivo propio de la capa `app`), escribir que tras montar la aplicación una petición emitida por el cliente lleva el token que hay en el `authStore`, y que una respuesta `401` deja `useAuthStore.getState().isAuthenticated` en `false`. Confirmar RED. — Archivo propio `tests/app-http-client-wiring.test.tsx`; 4/4 tests RED confirmados.
- [x] 5.2 **GREEN**: crear el módulo de cableado en `src/app/providers/` que llama a `configureApiClient({ getToken: () => useAuthStore.getState().token, onUnauthorized: () => useAuthStore.getState().logout() })`, e invocarlo desde `App.tsx` en el mismo efecto de montaje donde ya vive `hydrate()`. Ejecutar hasta verde. — `src/app/providers/httpClientProvider.ts` (`wireHttpClient`), invocado desde `App.tsx`; 4/4 verdes.
- [x] 5.3 **TRIANGULATE — doble montaje (modo estricto)**: montar la aplicación bajo `StrictMode` y verificar que una petición sigue llevando una sola cabecera de autorización y que un `401` produce un solo `logout`.
- [x] 5.4 **TRIANGULATE — punto único**: verificar por inspección de `src/` que `configureApiClient` se invoca desde un único lugar. Dejar la aserción como test (búsqueda de ocurrencias en el árbol de fuentes, al estilo de `tests/storage-single-access-point.test.ts`).
- [x] 5.5 Borrar `src/app/providers/.gitkeep` (la carpeta dejó de estar vacía) y confirmar que `tests/structure.test.ts` sigue verde en su bloque de "subdirectorios comprometidos por el roadmap". — Borrado; el bloque "roadmap-committed subdirectories exist" sigue verde (solo el bloque de CHANGE-13 sobre `.gitkeep` de `shared/api`/`features` falla, esperado — lo actualiza la tarea 10.1).

## 6. `submitScan` — el envío al Bridge (D-6, D-7)

- [x] 6.1 **RED**: en `tests/submit-scan.test.ts`, escribir que `submitScan(request)` emite `POST` a `/api/v1/scan/start` con el cuerpo recibido y devuelve el `ScanResponse` cuando el Bridge responde `202`. Confirmar RED por módulo inexistente. — Confirmado RED (heredado de la sesión anterior): "Failed to resolve import @features/scan-form/api/submitScan" (13 tests fallando por el mismo motivo, un solo archivo).
- [x] 6.2 **GREEN**: crear `src/features/scan-form/api/submitScan.ts` usando la instancia de `@shared/api/axiosInstance`, tipado `(request: ScanRequest) => Promise<ScanResponse>`. Ejecutar hasta verde. — Creado; despacha `POST` a `SCAN_START_PATH` con la instancia compartida.
- [x] 6.3 **RED/GREEN**: definir y exportar `ScanSubmitError` con `status: number | null` y `problem: ProblemDetails | null` (D-7), y hacer que un rechazo `401` con cuerpo RFC 7807 lo lance transportando ambos. — Implementado con campos explícitos (no parameter properties — ver nota de tsc abajo). 13/13 verdes.
- [x] 6.4 **TRIANGULATE — un caso por código**: `400`, `422`, `429`, `502` y `500` lanzan `ScanSubmitError` con su `status` correspondiente y el `problem` parseado. — Cubierto por `it.each` en el test; verde.
- [x] 6.5 **TRIANGULATE — cuerpo que no es Problem Details** (D-5): un rechazo con cuerpo vacío, con texto plano y con HTML lanza `ScanSubmitError` con el `status` correcto y `problem: null` — sin inventar un detalle. Los tres casos. — Verde; `isProblemDetails` filtra los tres.
- [x] 6.6 **TRIANGULATE — fallo de red**: un fallo sin respuesta lanza `ScanSubmitError` con `status: null`, distinguible de cualquier rechazo con estado. — Verde.
- [x] 6.7 **TRIANGULATE — la credencial no se adjunta a mano**: verificar que el módulo no menciona `Authorization`, no lee el token ni importa el `authStore`, y que aun así la solicitud emitida lleva la cabecera cuando hay sesión (criterio de aceptación del roadmap: "via interceptor, no manual"). — Verde; el módulo no menciona ninguno de los tres términos (el comentario inicial se reescribió para no mencionar "Authorization" literalmente y no hacer fallar el propio test).

**Nota de la sesión — `erasableSyntaxOnly` y parameter properties**: el ejemplo de `ScanSubmitError` en design.md (D-7) usa parámetros de constructor `readonly` (`constructor(readonly status: ..., readonly problem: ...)`). `tsconfig.app.json` tiene `erasableSyntaxOnly: true`, que rechaza esa sintaxis (TS1294) porque emite código (asignación implícita), no la borra el compilador. Se implementó con campos declarados explícitamente y asignados en el cuerpo del constructor — mismo contrato público (`status`, `problem` de solo lectura), sin parameter properties. No estaba anotado en design.md; queda registrado acá para quien reutilice esa forma de ejemplo en otro change.

**Nota de la sesión — regresión pre-existente encontrada y corregida**: al correr la suite completa antes de arrancar el grupo 6, `tests/env.test.ts` ("import.meta.env is referenced only inside src/shared/config/env.ts") daba rojo señalando `shared/api/axiosInstance.ts` como infractor — no por código, sino porque su comentario de cabecera (grupo 3) contenía literalmente el texto `import.meta.env` dentro de un backtick, explicando que el módulo *no* lo lee. El test hace una búsqueda de substring, no distingue comentarios de código. Se reescribió el comentario sin ese literal (ahora dice "las variables de entorno de Vite"); no cambia ningún comportamiento. No estaba anotado como fallo esperado en las notas del grupo 5 — es una regresión real que se corrigió acá, antes de escribir ningún código del grupo 6.

## 7. `useScanForm` — validación, estado de carga y mensajes (D-9, D-10, D-12)

- [x] 7.1 **RED**: en `tests/use-scan-form.test.tsx`, escribir que el hook expone el formulario con `zodResolver(scanSchema)` y que un submit con `target_url` inválida **no** emite ninguna solicitud y deja el error bajo ese campo. Confirmar RED. — Confirmado RED: "Failed to resolve import @features/scan-form/model/useScanForm". Se escribieron a la vez los 17 tests de las tareas 7.1–7.10 (mismo archivo, un solo módulo faltante).
- [x] 7.2 **GREEN**: crear `src/features/scan-form/model/useScanForm.ts` con `useForm<ScanForm, unknown, z.output<typeof scanSchema>>`, `zodResolver(scanSchema)` y los `defaultValues` tomados de las constantes de `@entities/scan` (D-10). Ejecutar hasta verde. — 17/17 verdes; `tsc -b` requirió instanciar explícitamente el genérico `Input` de `zodResolver<ScanForm, unknown, z.output<typeof scanSchema>>(scanSchema)` (ver nota abajo).
- [x] 7.3 **TRIANGULATE — multi-error**: URL inválida y `phpsessid` vacío simultáneos producen ambos mensajes en la misma pasada, cada uno con el texto que exporta `@entities/scan` (sin reescribirlo). — Verde (`getAllByRole('alert')` >= 2).
- [x] 7.4 **RED/GREEN — el cuerpo despachado**: un submit válido llama a `submitScan` con exactamente `target_url`, `phpsessid`, `sqlmap_level` y `sqlmap_risk`, **sin** `ethical_consent`, y con la URL ya recortada por el schema. — Verde; `data` capturado del adapter fake coincide exactamente, URL con espacios recortada.
- [x] 7.5 **RED/GREEN — estado de carga y doble submit**: durante la solicitud `isLoading` es verdadero; dos submits disparados antes de que responda el primero producen **una sola** llamada a `submitScan`. — Verde; guard con `useRef` (no `useState`, para lectura síncrona antes del re-render).
- [x] 7.6 **RED/GREEN — tabla de mensajes** (D-12): exportar las constantes de mensaje y verificar un caso por situación — `401` (sesión expirada), `400` y `422` (mismo mensaje), `429` (límite alcanzado, sin número de minutos — ver R-1), `502` (sistema no disponible), otro `5xx` (genérico) y fallo de red (mensaje de conexión, distinto del de `502`). Siete aserciones. — `SCAN_SUBMIT_MESSAGES` exportado; 7 casos verdes, incluida la aserción de que `rateLimited` no contiene dígitos.
- [x] 7.7 **TRIANGULATE — nada crudo del servidor en pantalla**: ante un rechazo con `detail`, `type` e `instance` presentes, el estado de error del hook contiene la constante propia y **no** el `detail`, el `type` ni el `instance` del Bridge. — Verde; `messageForSubmitError` nunca lee `error.problem`.
- [x] 7.8 **TRIANGULATE — el `401` cierra sesión sin que el hook lo pida** (D-8): tras un rechazo `401` la aplicación queda no autenticada, y el módulo del hook **no** invoca `logout()` por su cuenta (lo hace el interceptor). Verificar ambas cosas: el efecto observable y la ausencia de la llamada duplicada. — Verde; chequeo de fuente (`logout(`, `authStore`) + wiring real de `configureApiClient` con `authStore.logout` espiado (1 sola llamada).
- [x] 7.9 **TRIANGULATE — el rechazo devuelve el formulario a estado enviable**: tras un `429`, `isLoading` es falso, los valores cargados siguen en el formulario y un nuevo submit vuelve a llamar a `submitScan`. — Verde.
- [x] 7.10 **TRIANGULATE — campos numéricos** (D-9): con el control registrado con `setValueAs`, tipear `3` produce el número `3` en el cuerpo (no `'3'`), y **vaciar** el campo produce el valor por defecto del contrato sin ningún mensaje de tipo inválido. Este último es el test que `valueAsNumber` no pasaría (fallaría con el texto en inglés de Zod). — Verde; `asOptionalNumber` exportado del hook y reutilizado en el arnés de prueba y (más adelante) en `ScanForm.tsx`.

**Nota de la sesión — instanciación explícita de `zodResolver<Input, Context, Output>`**: `useForm<ScanForm, unknown, z.output<typeof scanSchema>>({ resolver: zodResolver(scanSchema) })` sin genéricos explícitos no compila (TS2322): `zodResolver` infiere su `Input` del tipo `z.input` real del schema, donde `ethical_consent` es el literal `true` (por `z.literal(true)`), no `boolean`. `Resolver` usa ese `Input` en posición contravariante (parámetro de función), así que `Resolver<{ethical_consent: true}>` no es asignable a `Resolver<ScanForm>` (`ScanForm` no es asignable a un tipo más angosto). La solución es instanciar el genérico explícitamente — `zodResolver<ScanForm, unknown, z.output<typeof scanSchema>>(scanSchema)` — lo que fuerza la comprobación en la dirección covariante correcta (`true` sigue siendo asignable a `boolean`). No estaba anotado en el ejemplo de D-10 de design.md; queda documentado en el módulo y acá.

## 8. `useScanForm` — aceptación y redirección al Dashboard (D-11)

- [x] 8.1 **RED**: escribir que un `202` deja el hook en estado de éxito con la confirmación recibida, y que tras `SUCCESS_REDIRECT_DELAY_MS` (temporizadores falsos) `window.location.href` es `dashboardUrl` de `@shared/config/env`. Sustituir `window.location` por un objeto propio (D-11, nota de testing). Confirmar RED. — Confirmado RED: los 9 tests de 8.1–8.6 colgaban a `undefined` (`SUCCESS_REDIRECT_DELAY_MS` inexistente, módulo aún sin la exportación).
- [x] 8.2 **GREEN**: implementar el estado de éxito y la redirección en un `useEffect` con `setTimeout` y `clearTimeout` en el cleanup. Ejecutar hasta verde. — Implementado; 27/27 verdes.
- [x] 8.3 **TRIANGULATE — ningún rechazo redirige**: `401`, `400`, `422`, `429`, `502` y el fallo de red dejan `window.location.href` intacto. Seis casos. — Verde (`it.each` + un caso de red aparte).
- [x] 8.4 **TRIANGULATE — desmontaje antes de navegar**: desmontar el componente después del `202` pero antes de que venza el temporizador no navega y no propaga ningún error. — Verde; `expect(() => { unmount(); vi.advanceTimersByTime(...) }).not.toThrow()`.
- [x] 8.5 **TRIANGULATE — el destino es el Dashboard, no el Bridge**: afirmar que el valor asignado es `dashboardUrl` y que es **distinto** de `apiBaseUrl`. — Verde.
- [x] 8.6 **TRIANGULATE — tras la aceptación no se puede reenviar**: después del `202` y antes de la navegación, el formulario sigue no enviable (no hay una segunda llamada a `submitScan`). — Verde; guard con `succeededRef` (nunca se resetea a `false` tras éxito).

**Nota de la sesión — `vi.useFakeTimers({ shouldAdvanceTime: true })`**: con temporizadores falsos activos y `waitFor` de `@testing-library/react` (que encuesta con `setTimeout` real), las primeras corridas de los 9 tests del grupo 8 colgaban 5000ms reales y caían por timeout — el reloj falso nunca avanzaba solo, así que `waitFor` jamás veía progreso. La opción `shouldAdvanceTime: true` de `vi.useFakeTimers` deja que el tiempo real siga empujando el reloj falso (además de permitir el avance controlado con `vi.advanceTimersByTimeAsync`), y resolvió el cuelgue. No es una decisión de diseño de producción — es un detalle de arnés de test, documentado acá para quien reutilice temporizadores falsos en otro hook con `waitFor`.

## 9. `ScanForm` — la interfaz (D-9, `shared-ui-kit`)

- [x] 9.1 **RED**: en `tests/scan-form.test.tsx`, escribir que `<ScanForm />` renderiza los cinco controles con sus etiquetas accesibles (`getByLabelText`) usando los primitivos de `@shared/ui`. Confirmar RED. — Confirmado RED: "Failed to resolve import @features/scan-form/ui/ScanForm".
- [x] 9.2 **GREEN**: crear `src/features/scan-form/ui/ScanForm.tsx` con `Input` para `target_url` y `phpsessid`, `Input type="number"` para los dos parámetros de SQLMap con `min`/`max`/`step` tomados de las constantes de `@entities/scan` (D-9), `Checkbox` para la declaración ética y `Button` para el envío. Ejecutar hasta verde. — 6/6 verdes; `tsc -b` limpio.
- [x] 9.3 **TRIANGULATE — botón deshabilitado sin declaración ética**: con el formulario completo y el checkbox sin marcar, el botón está deshabilitado; al marcarlo se habilita; al desmarcarlo vuelve a deshabilitarse. Tres aserciones (criterio de aceptación del roadmap). — Verde; `disabled={!ethicalConsent}` con `ethicalConsent = watch('ethical_consent')`.
- [x] 9.4 **TRIANGULATE — carga**: durante el envío el botón está deshabilitado y muestra el `Spinner` del primitivo (`loading`), y un doble clic produce una sola solicitud. — Verde; `loading={isLoading}` del `Button` ya deshabilita + marca `aria-busy` (CHANGE-15, sin lógica propia acá). **Corregido en la auditoría**: la última aserción del test afirmaba que, tras resolver la solicitud (un `202`), el botón volvía a estar habilitado — lo contrario de lo que exige `scan-submission` ("tras una aceptación, el formulario SHALL permanecer no enviable"). Se cambió a afirmar que el estado de carga se levanta (`aria-busy="false"`, sin `Spinner`) y que el control sigue deshabilitado. El retorno a estado enviable tras un **rechazo** sigue cubierto por 7.9.

- [x] 9.4b **Confirmación de éxito en la interfaz** (hallazgo de la auditoría, no estaba en el plan original): `ScanForm.tsx` no renderizaba ninguna señal de aceptación — consumía `serverError` pero no `scanResponse`. El usuario veía el spinner, después nada durante los 2 s de `SUCCESS_REDIRECT_DELAY_MS`, y después un cambio de pantalla sin explicación: exactamente el fallo que D-11 dice estar evitando, y un incumplimiento de `scan-submission` ("la confirmación SHALL ser visible antes de la navegación"), de HU-05-01 ("mensaje de éxito ~2s") y de HU-05-02 ("feedback visual en éxito/error"). Corregido con ciclo RED→GREEN: se agregó `SCAN_SUCCESS_MESSAGE` (texto propio, no el `message` crudo del Bridge — mismo criterio que D-12 aplica al `detail`), se renderiza con `role="status"` mientras `scanResponse !== null`, y el botón pasa a `disabled={!ethicalConsent || accepted}`. Cuatro tests nuevos en `tests/scan-form.test.tsx`: confirmación visible tras el `202` y antes de navegar, control no enviable tras la aceptación, ausencia de la confirmación ante un rechazo, y que ni el `message` ni el `scan_id` del Bridge aparecen en pantalla.
- [x] 9.5 **TRIANGULATE — errores inline**: un submit inválido muestra el mensaje bajo el campo responsable, asociado por `aria-describedby` (lo provee el primitivo `Input`), y no como un error global. — Verde.
- [x] 9.6 **TRIANGULATE — error de servidor a nivel de formulario**: un rechazo del Bridge muestra el mensaje de la tabla de D-12 en la zona de error del formulario, legible por `getByText`. — Verde.
- [x] 9.7 **TRIANGULATE — los límites del control salen del contrato**: afirmar que los atributos `min`/`max` de los dos controles numéricos son iguales a `SQLMAP_LEVEL_MIN/MAX` y `SQLMAP_RISK_MIN/MAX` importados de `@entities/scan`, sin repetir los literales en el test. — Verde.
- [x] 9.8 Crear `src/features/scan-form/index.ts` como API pública de la slice (exporta `ScanForm` y lo que CHANGE-19 necesite), siguiendo el patrón de `entities/scan/index.ts`. — Creado; exporta `ScanForm`, `useScanForm`, `SCAN_SUBMIT_MESSAGES`, `SCAN_SUCCESS_MESSAGE`, `SUCCESS_REDIRECT_DELAY_MS`, `asOptionalNumber`, `submitScan`, `ScanSubmitError`, `SCAN_START_PATH` (lo que CHANGE-19 puede necesitar del muro de autenticación y los modales, sin adivinar de más).

## 10. Fronteras, estructura y limpieza (D-13)

- [x] 10.1 Actualizar en `tests/structure.test.ts` las dos aserciones que hoy afirman que `src/shared/api/` y `src/features/` contienen únicamente `.gitkeep`: pasan a listar los módulos concretos de este change, en el mismo formato que los bloques de `entities/user` y `entities/scan`. Borrar los dos `.gitkeep`. Ejecutar la suite. — Reemplazado el `it.each` de `.gitkeep` por un describe propio (`src/shared/api/ y src/features/ quedaron poblados por CHANGE-18`) que lista los archivos exactos; `.gitkeep` de ambos directorios borrado (queda el de `src/widgets/`, así el test de "todo `.gitkeep` está anotado" sigue teniendo al menos uno). Suite: verde.
- [x] 10.2 Correr `tests/fsd-boundaries.test.ts` y confirmar cero violaciones: `features/` importa solo de `@entities` y `@shared`; `shared/api/` no importa de ninguna capa superior; `entities/*` importan de `@shared` (dirección permitida). — 6/6 verdes, cero violaciones.
- [x] 10.3 Correr `tests/shared-domain-agnostic.test.ts` y confirmar que sigue verde. Verificar además a mano que `src/shared/api/` no contiene texto de dominio WASA (mensajes en español, nombres de campos del escaneo): los mensajes viven en el feature, no en `shared/`. — Verde (el test solo escanea `shared/ui` y `shared/lib`, sin cambios). Verificación manual (`grep` de los términos de dominio contra `src/shared/api/*.ts`): un único acierto, la palabra "sesión" dentro de un comentario de `axiosInstance.ts` que describe el mecanismo genérico de inyección de credencial ("la credencial de sesión... entra por inyección") — no es texto de dominio del escaneo (no aparece `sqlmap`, `phpsessid`, `target_url` ni ningún mensaje en español de cara al usuario). No amerita cambio.
- [x] 10.4 Correr `tests/storage-single-access-point.test.ts` y confirmar que el `authStore` sigue siendo el único módulo que toca `localStorage` — el cliente HTTP recibe el token, no lo lee del almacenamiento. — Verde, sin cambios.
- [x] 10.5 **REFACTOR**: revisar que ningún literal de ruta (`/api/v1/scan/start`), de código de estado ni de mensaje esté duplicado entre módulos de producción y tests; los tests afirman sobre las constantes exportadas. — Verificado por `grep`: los tests de `submitScan`/`useScanForm`/`ScanForm` importan `SCAN_START_PATH` y `SCAN_SUBMIT_MESSAGES` en vez de repetir literales. Los únicos usos literales de `/api/v1/scan/start` fuera de producción son valores de ejemplo del campo `instance` de un cuerpo Problem Details simulado (dato de prueba, no una aserción contra el contrato) — no es la clase de duplicación que la tarea busca eliminar.

## 11. Cierre

- [x] 11.1 Correr `npm run build` (`tsc -b && vite build`) y confirmar que termina sin errores de tipos — incluida la verificación de que el alias del contrato compartido es lo que ahora hace fallar una divergencia en el build (D-4, punto 3: probar temporalmente un cambio de tipo en `ProblemDetails`, confirmar que el build falla, y revertirlo). — `npm run build` limpio (80 módulos, sin errores de tipos). **Hallazgo, no confirma la letra literal de la tarea**: se probaron dos experimentos — (a) agregar un miembro requerido nuevo a `ProblemDetails`, (b) cambiar el tipo de `status` de `number` a `boolean` — y en **ninguno de los dos** falló `npm run build`. Revisado el motivo: ningún módulo de `src/` (lo único que `tsc -b` typechequea; los tests quedan fuera de `tsconfig.app.json`) construye un literal tipado como `ProblemDetails`/`ScanApiError`/`AuthApiError`, ni lee sus miembros con una anotación de tipo que dependa de su forma exacta — `isProblemDetails` opera sobre `Record<string, unknown>` (el cast borra la forma antes del `typeof`), y `ScanSubmitError.problem` solo pasa el valor ya angostado por el guard, sin volver a leerlo. La propiedad real que el alias garantiza — y que sí se verificó — es más fuerte que "el build falla ante una divergencia": **la divergencia entre `ScanApiError` y `AuthApiError` dejó de ser expresable**, porque ambos son literalmente el mismo tipo (no dos declaraciones independientes que alguien podría editar por separado, como en el bug real de CHANGE-17). Cambiar `ProblemDetails` cambia ambos alias atómicamente, sin excepción posible — no hay nada que "diverja" para que un chequeo lo atrape. Se revirtieron ambos cambios experimentales; `npm run build` vuelto a correr, limpio. Se deja esto anotado para quien reabra D-4: la garantía documentada ahí (punto 2) describe el estado ANTES del alias (verificado en CHANGE-17); el estado DESPUÉS no es "el build ahora falla ante cualquier cambio de tipo", sino "no hay una segunda declaración que pueda quedar desincronizada".
- [x] 11.2 Correr `npm run test:run` completo y comparar contra el baseline de 1.1: el total sube y ningún test previo quedó rojo ni fue eliminado para pasar. — 343 tests pasando (32 archivos), contra el baseline de 270 (grupo 1) — sube en 73, cero regresiones, ningún test de los grupos 1–5 fue tocado ni eliminado.
- [x] 11.3 Correr `npm run lint` (oxlint) y dejarlo limpio. — Limpio tras un ajuste: `oxlint` marcaba `react(refs)` como falso positivo sobre el manejador de submit de `useScanForm.ts` (accede a `.current` dentro de la función que `handleSubmit` de react-hook-form envuelve y devuelve como manejador de evento — oxlint no distingue eso de un acceso durante el render). Se extrajo la función a `onValidSubmit` con `useCallback` (mejora también la identidad de la función) y se agregó `// oxlint-disable-next-line react/refs` justo antes de `form.handleSubmit(onValidSubmit)`, con el porqué documentado arriba. `npm run lint` sin salida (limpio).
- [x] 11.4 Repasar uno por uno los seis criterios de aceptación de `[CHANGE-18]` en `CHANGES.md` y marcarlos, anotando en qué test queda cubierto cada uno. — Los seis marcados `[x]` en `CHANGES.md`, cada uno con el/los test(s) que lo cubre.
- [x] 11.5 Anotar en `CHANGES.md`, bajo CHANGE-18, las notas de traspaso para CHANGE-16 y CHANGE-19: (a) el cliente HTTP y el contrato `ProblemDetails` ya existen y se consumen sin modificarlos; (b) el interceptor cierra sesión ante **cualquier** `401`, incluido el de un login fallido — inocuo por spec, pero a tener presente (R-3); (c) el `429` no puede informar minutos hasta que el Bridge exponga `Retry-After` por CORS (R-1). — Agregadas las tres notas bajo los criterios de aceptación de CHANGE-18.
- [x] 11.6 Correr `openspec validate change-18-feature-scan-form --strict` y confirmar que pasa antes de proponer el archivado. — `Change 'change-18-feature-scan-form' is valid`.

## 12. Auditoría posterior (revalidación adversarial)

- [x] 12.1 **Hallazgo — la interfaz no confirmaba la aceptación**: ver la nota de 9.4b. Es el único defecto funcional de cara al usuario que encontró la auditoría. Corregido con RED→GREEN.
- [x] 12.2 **Hallazgo — un test afirmaba lo contrario del spec**: ver la nota de 9.4. Corregido.
- [x] 12.3 **Hallazgo — un test no ejercitaba el escenario que decía cubrir**: ver la nota de 3.6. Corregido.
- [x] 12.4 **Hallazgo — un escenario del spec era falso**: `specs/http-client/spec.md` declaraba "Una divergencia rompe la compilación del proyecto", que la tarea 11.1 ya había verificado empíricamente que **no** ocurre (ningún módulo de `src/` construye un literal tipado como `ProblemDetails`, así que cambiar un miembro no rompe `tsc -b`). Archivar ese escenario habría dejado en `openspec/specs/` una garantía inexistente. Reescrito como "La divergencia entre dominios deja de ser expresable", que es la propiedad real y verificada (una única declaración; los dos alias de dominio cambian atómicamente con ella), cubierta por el test de asignabilidad de `tests/scan-schema.test.ts`.
- [x] 12.5 **Verificado y sin cambios**: fronteras FSD por inspección de todos los `import` (el detector de `tests/support/fsd.ts` cubre además `import type`, `import()` dinámico, `export * from` y `require` — sin punto ciego); lectura del token por petición y idempotencia del cableado (los interceptores se registran al evaluar el módulo, `configureApiClient` solo asigna referencias); orden de las ramas de `messageForSubmitError` (`status === null` se evalúa **antes** que cualquier comparación numérica); ausencia de `console.*`, de `dangerouslySetInnerHTML` y de cualquier volcado del JWT o del PHPSESSID; `CORSMiddleware` de `fastapi_bridge/main.py` sigue **sin** `expose_headers` (R-1 vigente, no obsoleto); la ruta del Bridge sigue siendo `POST /api/v1/scan/start` con `status_code=202` y su `ErrorDetail` sigue siendo espejo exacto de `ProblemDetails`; la directiva `oxlint-disable-next-line react/refs` es necesaria de verdad (al quitarla, oxlint reporta `react(refs)` en esa línea) y no tapa ningún cierre obsoleto — `onValidSubmit` es un `useCallback` con dependencias vacías que solo lee refs y setters estables.
