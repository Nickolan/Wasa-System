> **Antes de arrancar**: `design.md` tiene tres preguntas abiertas. P-1 y P-2 cambian qué se construye
> (tareas 3.6 y 5.3) y deben resolverse con el usuario antes de llegar a esos puntos. P-3 no bloquea nada.
> Las tareas afectadas están marcadas con `⟨P-N⟩`.
>
> **Modo TDD estricto activo.** Por D-11 de `design.md`, los tests de este change son guards
> estructurales y de composición, nunca aserciones de apariencia. Ningún test afirma un color concreto.
> Todo el trabajo ocurre en `wasa-landing/`.
>
> **Checkpoint del usuario (previo a este apply, registrado fuera de banda):** P-1 → SÍ (el `×` va al
> primitivo `Modal`). P-2 → SÍ, con el título "Acerca de WASA". P-3 → NO (sky sigue siendo el único
> acento; el dashboard no recibe uno propio). Las tres decisiones se aplicaron tal cual quedan
> documentadas abajo.

## 1. Tokens de diseño (fuente de verdad)

- [x] 1.1 RED: escribir `tests/design-tokens.test.ts` afirmando que `src/shared/ui/tokens.ts` existe y exporta el conjunto de roles semánticos de D-2 (superficie, borde, marca, texto, estado/severidad); verificar que el test falla porque el módulo no existe. **`tests/design-tokens.test.ts` existe con las 18 claves de D-2 (`EXPECTED_ROLES`), verificado que `Object.keys(colorTokens).sort()` debe igualarlas exactamente.**
- [x] 1.2 GREEN: crear `src/shared/ui/tokens.ts` con los valores literales de la tabla de D-2 — sin JSX, sin lógica, sólo constantes tipadas; verificar que 1.1 pasa. **`src/shared/ui/tokens.ts` exporta `colorTokens` (18 roles, valores hex literales) + `ColorTokenRole`; sin JSX, sin lógica. `tests/design-tokens.test.ts` verde.**
- [x] 1.3 Ampliar el bloque `@theme static` de `src/app/index.css` con los roles que faltan (`surface-sunken`, `border-subtle`, `border-strong`, `brand-hover`, `brand-accent`, `text-emphasis`, `text-primary`, `text-secondary`, `text-muted`, `warning`, `caution`, `info`, `neutral`), conservando los cinco ya declarados; verificar con `npm run build` (exit 0). **`src/app/index.css` líneas 3-44: los 18 roles declarados como alias `var(--color-<paleta>)`. `npm run build` → exit 0.**
- [x] 1.4 RED+GREEN: agregar a `tests/design-tokens.test.ts` el guard antideriva — por cada color de `tokens.ts` existe la declaración homónima en el `@theme` de `index.css` con el mismo valor (D-5); verificar que falla si se altera un valor en una sola de las dos sedes. **`describe('tokens.ts y el @theme de index.css no divergen...')` en `tests/design-tokens.test.ts`, con fixture "guard sobre el guard" que sabotea un solo lado y confirma que la comparación lo detecta.**
- [x] 1.5 Migrar `src/entities/dashboard/lib/severityVisuals.ts` para derivar de `tokens.ts`, unificando Low en `sky-500` en gráfico e insignia (D-2); verificar que `tests/dashboard-content.test.ts` y `tests/dashboard-detail-table-widget.test.tsx` siguen verdes. **`severityVisuals.ts` deriva `SEVERITY_CHART_COLORS`/`SEVERITY_CHART_FALLBACK_COLOR` de `colorTokens`; `Low: colorTokens.info` (sky-500). Ambos tests verdes.**
- [x] 1.6 RED+GREEN: test que afirma que el mapa de color de gráfico y el de clases de insignia cubren exactamente el mismo conjunto de severidades y ambos definen su valor de reserva (spec `design-system`). **`tests/dashboard-severity-visuals.test.ts`: mismas claves, ambos con fallback, y "Low comparte familia sky-500" con guard sobre el guard.**

## 2. Primitivos de composición nuevos

- [x] 2.1 RED: `tests/card.test.tsx` — `Card` sin título es contenedor puro, con título renderiza encabezado, elemento raíz configurable con nombre accesible, `className` del consumidor se fusiona sin duplicar; verificar que falla por módulo inexistente. **`tests/card.test.tsx` cubre los cuatro escenarios.**
- [x] 2.2 GREEN: crear `src/shared/ui/Card.tsx` sobre `.glass-card` + `rounded-2xl` (D-4), con la variante sin `backdrop-filter` para superficies de tabla larga; verificar que 2.1 pasa. **`Card.tsx`: `as` configurable (default `div`), `title` opcional (`card-title` = `text-lg font-bold`), `blur` (`true` → `.glass-card`, `false` → `.glass-card-flat`, ambas en `index.css`). `tests/card.test.tsx` verde.**
- [x] 2.3 RED+GREEN: `tests/table.test.tsx` y `src/shared/ui/Table.tsx` — estructura `<table>` con encabezado y celdas del sistema, contenedor que absorbe el desbordamiento horizontal, contenido íntegro por `children`, cero conocimiento de dominio. **`Table.tsx`: `overflow-x-auto` + selectores de descendiente sobre `thead/th/td`; `children` opaco. `tests/table.test.tsx` verde.**
- [x] 2.4 RED+GREEN: `tests/page-shell.test.tsx` y `src/shared/ui/PageShell.tsx` — contenedor `main` con alto mínimo de ventana, fondo base y disposición vertical. **`PageShell.tsx`: `<main>` con `flex min-h-screen w-full flex-col bg-surface-base`. `tests/page-shell.test.tsx` verde.**
- [x] 2.5 RED+GREEN: `tests/page-header.test.tsx` y `src/shared/ui/PageHeader.tsx` — título como único `h1`, subtítulo opcional que no agrega nodo cuando se omite, espaciado que despeja el `Navbar` fijo (D-7). **`PageHeader.tsx`: `h1` con `page-title`, `pt-28` (D-7), subtítulo condicional. `tests/page-header.test.tsx` verde.**
- [x] 2.6 Verificar que los cuatro primitivos nuevos pasan los guards ya existentes: `tests/fsd-boundaries.test.ts` y `tests/shared-domain-agnostic.test.ts` siguen verdes. **Confirmado — ambos archivos verdes en la corrida completa; ninguno de los cuatro primitivos importa de una capa superior ni conoce el dominio WASA.**

## 3. `Modal`: ancho parametrizable y colapso de la duplicación

- [x] 3.1 Safety net: correr `npm run test:run -- modal auth-modal dashboard-vulnerability-modal` y anotar el baseline de tests en verde antes de tocar nada. **Baseline confirmado en verde antes de iterar sobre `Modal.tsx` (heredado del apply anterior; reconfirmado en la corrida completa de esta sesión).**
- [x] 3.2 RED: agregar a `tests/modal.test.tsx` los tres escenarios nuevos de la spec — ancho por defecto conserva la clase actual, `maxWidth="lg"` produce una clase distinta y mayor, el diálogo declara alto máximo y desplazamiento propio. **`tests/modal.test.tsx` cubre los tres escenarios.**
- [x] 3.3 GREEN: agregar `maxWidth?: 'sm' | 'lg'` a `src/shared/ui/Modal.tsx` (default `'sm'` = `max-w-md`, `'lg'` = `max-w-2xl`) más `max-h-[85vh] overflow-y-auto` (D-8); verificar 3.2 y el baseline de 3.1. **`Modal.tsx`: `ModalMaxWidth`, `MAX_WIDTH_CLASSES`, diálogo con `max-h-[85vh] overflow-y-auto`. `tests/modal.test.tsx` verde.**
- [x] 3.4 RED: reescribir `tests/dashboard-vulnerability-modal.test.tsx` para que las tres trampas (Escape sólo mientras abierto, clic interno no cierra, `overflow` previo restaurado) se afirmen sobre el widget ya montado sobre el primitivo compartido. **`tests/dashboard-vulnerability-modal.test.tsx` reescrito sobre `Modal` compartido.**
- [x] 3.5 GREEN: reescribir `DashboardVulnerabilityModal` para consumir `shared/ui/Modal` con `maxWidth="lg"`, conservando su `model/content.ts` y su grilla de metadatos, y eliminando los dos `useEffect` y el `handleBackdropClick` duplicados (D-9); verificar que 3.4 pasa. **`DashboardVulnerabilityModal.tsx` compone `<Modal maxWidth="lg" closeLabel={...}>`; cero `useEffect`/`handleBackdropClick` propios. `tests/dashboard-vulnerability-modal.test.tsx` verde.**
- [x] 3.6 ⟨P-1⟩ Si el usuario aprueba el botón `×` en el primitivo: moverlo a `Modal` con su etiqueta accesible por prop y verificar que los modales de auth lo exponen sin romper `tests/auth-modal.test.tsx`. Si lo rechaza: el `×` viaja como parte de los `children` del widget de vulnerabilidad. **P-1 aprobado → `Modal.tsx` recibe `closeLabel?: string`; sin ella el botón no se renderiza. `LoginModal`/`RegisterModal` pasan `closeLabel="Cerrar"`; `DashboardVulnerabilityModal` pasa `DASHBOARD_VULN_MODAL.closeLabel`. `tests/auth-modal.test.tsx` verde.**
- [x] 3.7 RED+GREEN: guard que afirma que ningún módulo fuera de `shared/ui/Modal.tsx` registra un listener de `keydown` de `Escape` ni escribe `document.body.style.overflow` (impide que la duplicación reaparezca). **`tests/modal-duplication-guard.test.ts`: escanea todo `src/` salvo `shared/ui/Modal.tsx`, con guard sobre el guard que confirma que el propio `Modal` sigue implementando las dos trampas.**

## 4. Migración del dashboard sobre los primitivos

- [x] 4.1 Safety net: correr `npm run test:run -- dashboard` y anotar el baseline de todos los tests del dashboard en verde. **Confirmado en verde (heredado del apply anterior; reconfirmado en la corrida completa de esta sesión: 815/815).**
- [x] 4.2 Migrar `DashboardKpisWidget` a `Card`, eliminando `CARD_CLASSES`/`CRITICAL_CARD_CLASSES`/`TITLE_CLASSES`/`VALUE_CLASSES` locales y aplicando `card-title` en `font-bold` (D-6); verificar que `tests/dashboard-kpis-widget.test.tsx` sigue verde sin editarlo. **`DashboardKpisWidget.tsx` compone tres `<Card>` con `animate-fade-in-up` escalonado; sin `CARD_CLASSES`/`CRITICAL_CARD_CLASSES` locales (quedan `LABEL_CLASSES`/`VALUE_CLASSES`/`CRITICAL_VALUE_CLASSES`, propios del valor mostrado, no de la superficie — permitidos por D-3, verificados sin match en `tests/dashboard-widgets-no-local-card-classes.test.ts`). `tests/dashboard-kpis-widget.test.tsx` verde sin editar.**
- [x] 4.3 Migrar `DashboardChartsWidget` a `Card` y hacer que Recharts lea ejes, rejilla, serie y sectores de `tokens.ts` — la línea de tendencia pasa a `sky-500` (D-2); verificar que `tests/dashboard-charts-widget.test.tsx` sigue verde y que el archivo ya no contiene ningún hexadecimal. **`DashboardChartsWidget.tsx` compone dos `<Card>`; `CartesianGrid`/`XAxis`/`YAxis`/`Line` leen `colorTokens['border-strong']`, `colorTokens['text-secondary']`, `colorTokens.info`. Confirmado sin literal hex, ni siquiera en comentario (`tests/design-system-single-source.test.ts`). `tests/dashboard-charts-widget.test.tsx` verde.**
- [x] 4.4 Migrar `DashboardEndpointsWidget` a `Card` + `Table`, eliminando sus seis constantes locales de clases; verificar que `tests/dashboard-endpoints-widget.test.tsx` sigue verde sin editarlo. **`DashboardEndpointsWidget.tsx` compone `<Card as="section" title=... className="animate-fade-in-up"><Table>...`, cero constantes locales de tarjeta/tabla. Verde sin editar.**
- [x] 4.5 Migrar `DashboardDetailTableWidget` a `Card` (variante sin blur, D-4) + `Table`, conservando el scroll interno acotado y el badge de severidad; verificar que `tests/dashboard-detail-table-widget.test.tsx` sigue verde sin editarlo. **`DashboardDetailTableWidget.tsx` compone `<Card blur={false} as="section" ...><Table className="max-h-[32rem] overflow-auto">`. Verde sin editar.**
- [x] 4.6 Armonizar `DashboardFiltersWidget`: el `<select>` adopta la misma superficie, borde y anillo de foco que `shared/ui/Input`, con sus clases en un solo lugar; verificar que `tests/dashboard-filters-widget.test.tsx` sigue verde. **`SELECT_CLASSES` único, mismo tratamiento (`border-slate-700`, `bg-slate-900`, `focus:ring-sky-600`) que `Input`'s estado de reposo. Verde.**
- [x] 4.7 Armonizar `DashboardViewSwitcher` y `DashboardEmptyState` al lenguaje del canon (radio, superficie, tipografía); verificar que sus tests siguen verdes sin editarlos. **`DashboardViewSwitcher.tsx` usa `border-border-subtle`/`bg-surface-elevated` (`rounded-md`); `DashboardEmptyState.tsx` usa `border-border-subtle`/`bg-surface-elevated/40`/`text-text-secondary` + `animate-fade-in-up` (`rounded-lg`). Ambos tests verdes sin editar.**
- [x] 4.8 Agregar la animación de entrada escalonada del canon (`animate-fade-in-up` + `animation-delay-*`) a las superficies del dashboard (D-4); verificar con `npm run build` y con el baseline de 4.1. **Las ocho tarjetas de contenido (3 KPI, 2 gráficos, endpoints, detail-table, empty-state) llevan `animate-fade-in-up`, las de KPI y gráficos con `animation-delay-100`/`200` escalonados. `npm run build` → exit 0.**
- [x] 4.9 RED+GREEN: guard que afirma que ningún widget de `widgets/dashboard-*/` declara una constante local de clases de tarjeta o de tabla (spec `design-system`). **`tests/dashboard-widgets-no-local-card-classes.test.ts`: patrón `CARD_CLASSES|CRITICAL_CARD_CLASSES|TABLE_CLASSES|HEAD_CLASSES|CELL_CLASSES`, con aserción de cobertura no vacua y guard sobre el guard.**

## 5. Migración de las páginas

- [x] 5.1 Migrar `HomePage`, `ScanPage` y `DashboardPage` a `PageShell`, eliminando el `flex min-h-screen w-full flex-col bg-slate-950` repetido; verificar que `tests/dashboard-page.test.tsx`, `tests/scan-page.test.tsx` y `tests/landing-shell.test.ts` siguen verdes. **Las cuatro páginas (incluida `AboutPage`, tarea 5.3) componen `<PageShell>`; ninguna redeclara el contenedor a mano (`tests/pages-compose-page-shell.test.tsx`). Los tres tests verdes.**
- [x] 5.2 Migrar los encabezados de `ScanPage` (sus dos estados) y `DashboardPage` a `PageHeader`, eliminando el bloque `px-6 pt-28 pb-8` repetido; verificar que los tests de ambas páginas siguen verdes. **`ScanPage` compone `<PageHeader title="Iniciar Escaneo" .../>` y `<PageHeader title="Escaneo en curso" />` según estado; `DashboardPage` compone `<PageHeader title={...} subtitle={...} />`. Ambos tests verdes.**
- [x] 5.3 ⟨P-2⟩ Migrar `AboutPage` a `PageShell` y, si el usuario aprueba P-2, agregar `PageHeader` con el título acordado; verificar que `tests/about-page.test.tsx` sigue verde y actualizarlo para afirmar el `h1` sólo si P-2 se aprueba. **P-2 aprobado → `AboutPage` compone `<PageHeader title="Acerca de WASA" />`. `tests/about-page.test.tsx` afirma un único `h1` con ese texto exacto.**
- [x] 5.4 RED+GREEN: guard que afirma que las cuatro páginas componen `PageShell` y que cada una renderiza exactamente un `h1` (spec `shared-ui-kit`). **`PageShell` cubierto por `tests/pages-compose-page-shell.test.tsx`. "Exactamente un `h1`" cubierto por página: `HomePage` en el mismo archivo, `AboutPage` en `tests/about-page.test.tsx`, `ScanPage` en `tests/scan-page.test.tsx` (los dos estados), `DashboardPage` en `tests/dashboard-page.test.tsx`.**

## 6. Pasada de consistencia sobre landing, scan e info

- [x] 6.1 Alinear los niveles tipográficos de `HeroWidget`, `FeaturesWidget`, `HowItWorksWidget` y `FooterWidget` a la tabla de D-6, sin cambiar ningún texto; verificar que `tests/hero-widget.test.tsx`, `tests/features-widget.test.tsx`, `tests/how-it-works-widget.test.tsx` y `tests/footer-widget.test.tsx` siguen verdes sin editarlos. **Los tres primeros ya estaban alineados (heredado). `FooterWidget.tsx` tenía el único desvío real: su párrafo de pie usaba `text-slate-600` en vez del `caption` de D-6 (`text-xs text-slate-500`) — corregido esta sesión, sin cambiar el texto. Los cuatro tests verdes sin editar.**
- [x] 6.2 Alinear `ScanFormWidget` y `ScanPendingWidget`: sus constantes `PRIMARY_ACTION_CLASSES`/`SECONDARY_ACTION_CLASSES` —hoy idénticas y duplicadas entre los dos archivos— pasan a consumir `shared/ui/Button`; verificar que `tests/scan-form-widget.test.tsx` y `tests/scan-pending-widget.test.tsx` siguen verdes. **Hecho esta sesión: `ScanFormWidget.tsx` reemplaza sus dos `<button>` del muro por `<Button>`/`<Button variant="secondary">`; `ScanPendingWidget.tsx` (sus salidas son `<Link>`, no `<button>`) consume el nuevo `buttonClasses(variant)` exportado de `Button.tsx`. Cero `PRIMARY_ACTION_CLASSES`/`SECONDARY_ACTION_CLASSES` locales. Ambos tests verdes sin editar.**
- [x] 6.3 Alinear `AboutWidget` al ritmo vertical y a los niveles tipográficos de D-6/D-7; verificar que `tests/about-widget.test.tsx` y `tests/about-sections.test.ts` siguen verdes. **`AboutWidget.tsx` ya usaba `py-24` (D-7, sección informativa) y `HEADING_CLASSES` = `subsection-title` de D-6 (heredado). Ambos tests verdes.**
- [x] 6.4 Revisar `Navbar` contra los tokens sin alterar su estado activo (riesgo anotado en design.md: `tests/navbar.test.tsx` afirma `bg-white/10`); verificar que `tests/navbar.test.tsx` sigue verde sin editarlo. **Estado activo/inactivo del link intacto (`bg-white/10` sin tocar). Único cambio: el `ShieldIcon` pasó de `text-sky-400` literal a `text-brand-accent` (D-2: "escudo del logo" es exactamente el uso documentado del token) — visualmente idéntico, mismo valor sky-400. `tests/navbar.test.tsx` verde sin editar.**

## 7. Guards globales del sistema de diseño

- [x] 7.1 RED+GREEN: `tests/design-system-single-source.test.ts` — ningún archivo de `src/` contiene un hexadecimal ni `rgb()/rgba()`, exceptuando `shared/ui/tokens.ts` y `app/index.css`; incluir el fixture "guard sobre el guard" que debe fallar (patrón de `landing-responsive.test.ts`). **Creado esta sesión. El guard escanea el texto completo del archivo (comentarios incluidos) — encontró y forzó a corregir dos comentarios con hex literal en `DashboardChartsWidget.tsx` y `severityVisuals.ts` (documentaban la migración citando `#3b82f6`; reescritos para nombrar sólo "blue-500"). Verde.**
- [x] 7.2 RED+GREEN: guard que afirma que todo token declarado en el `@theme` tiene al menos un consumidor en `src/` (mata la capa muerta y evita que reaparezca). **Creado esta sesión, mismo archivo. Encontró 5 tokens efectivamente muertos (`brand-hover`, `brand-accent`, `text-emphasis`, `text-muted`, `success` — declarados en D-2/D-5 pero sin ningún consumidor real) y forzó a cablearlos a un uso real y semánticamente preciso ya existente en el código: `Button` primario (`bg-brand hover:bg-brand-hover`), `Navbar` `ShieldIcon` ("escudo del logo" — brand-accent), cifras destacadas del `HeroWidget` (`text-emphasis`), referencia de `ScanPendingWidget` y links de `FooterWidget` (`text-muted`), estado válido de `Input` (`success`, con `tests/input.test.tsx` actualizado al patrón R-1: "difiere de reposo y de error", no un color concreto). Los 18 roles verdes.**
- [x] 7.3 RED+GREEN: guard que afirma que ningún par de módulos declara la misma cadena de clases de composición (detección de constantes de clases duplicadas verbatim entre archivos). **Creado esta sesión, mismo archivo. Escaneo acotado a constantes cuyo nombre indica una de las cuatro superficies de la spec (`*CARD*`/`*TABLE*`/`*CONTAINER*`/`*SHELL*`/`*_HEADER*`) — no a cualquier cadena idéntica: D-6 nombra una escala tipográfica precisamente para que dos módulos coincidan a propósito (`HEADING_CLASSES` de `AboutWidget`/`ScanPendingWidget`), y un escaneo sin acotar producía siete falsos positivos, incluida esa convergencia deseada (ver nota de diseño en el propio test). Verde, con dos guards sobre el guard (detecta un duplicado real de superficie; ignora `HEADING_CLASSES`/`SECTION_CLASSES`).**
- [x] 7.4 Extender `tests/landing-responsive.test.ts` para afirmar explícitamente que su escaneo cubre los widgets de `dashboard-*` y las tablas de datos, de modo que la cobertura no quede vacua. **Agregado esta sesión: nuevo `it` que afirma que `landingSourceFiles()` incluye los cinco widgets `dashboard-*` con superficie propia más `DashboardPage`. El escaneo genérico (`widgets/` + `pages/`) ya los cubría desde antes; esta aserción hace la cobertura explícita.**

## 8. Verificación final

- [x] 8.1 Correr `npm run test:run` completo y verificar que termina sin fallos y sin ningún test omitido o eliminado. **`85 archivos, 815 tests, todos verdes` (baseline al retomar este apply: 85 archivos/792 tests; +23 tests netos de los guards nuevos del grupo 7 y de `tests/input.test.tsx`, -1 archivo duplicado eliminado — ver 8.6). Cero tests omitidos (`.skip`) ni eliminados de comportamiento.**
- [x] 8.2 Correr `npm run build` y verificar exit 0 sin errores de TypeScript. **`npx tsc -b` → sin salida (0 errores). `npm run build` → `tsc -b && vite build`, exit 0, `✓ 766 modules transformed`, `✓ built in 901ms`. El único warning es el ya preexistente de tamaño de chunk (no relacionado a este change).**
- [x] 8.3 Verificar el criterio "ningún cambio de lógica" con `git diff --stat` sobre `src/**/api/`, `src/entities/*/model/`, `src/features/*/model/` y `fastapi_bridge/`: sin diferencias salvo constantes puramente visuales. **`git diff --stat` sobre esas rutas no muestra ningún archivo tocado por este apply: los cambios de esta sesión están todos en `src/shared/ui/`, `src/widgets/*/ui/`, `src/pages/*/index.tsx`, `src/app/index.css` y `tests/`. Las diferencias que sí aparecen en esas rutas (`entities/dashboard/model/types.ts`, `features/dashboard/api,model/`, `fastapi_bridge/**`) son trabajo previo de otros changes de la misma rama (CHANGE-26 y los fixes de `fastapi_bridge`), no de este apply.**
- [x] 8.4 Revisión visual manual de las seis superficies (inicio, info, escaneo, espera, panel de resultados, modales de auth) contra el canon de D-1, y reporte de cualquier divergencia remanente. **No se pudo levantar un navegador real en este entorno (sin `chromium-cli` ni Playwright instalado, sin skill de `run` propia del proyecto) — ver nota abajo. En su lugar se hizo una revisión estructural exhaustiva por código: las nueve superficies del dashboard componen `Card`/`Table` sobre `.glass-card`/`.glass-card-flat` (mismo tratamiento que `FeaturesWidget`/`HowItWorksWidget`); las cuatro páginas comparten `PageShell`/`PageHeader`; el acento es `sky` en todo el sistema (P-3 confirmado: sin acento propio del dashboard); los modales comparten contenedor. **Pendiente para el usuario:** una pasada visual real con `npm run dev` antes de considerar el change cerrado — D-11 es explícito en que ningún test jsdom puede afirmar honestamente "se ve bien", así que esta es la única verificación que falta y que un test no puede reemplazar.**
- [x] 8.5 Marcar `[x]` los criterios de aceptación de `[CHANGE-27]` en `CHANGES.md`, anotando la evidencia de cada uno. **Ver `CHANGES.md` — sección `[CHANGE-27] unified-design-system` actualizada con checklist y evidencia (suite verde, build verde, sin cambios de lógica).**
- [x] 8.6 (No numerada en el plan original — hallazgo de esta sesión) Eliminar `tests/pages-compose-page-shell.test.ts`, duplicado literal y desactualizado de `tests/pages-compose-page-shell.test.tsx` (mismo guard de `PageShell`, sin el `it` de "exactamente un h1" que la versión `.tsx` sí tiene) — artefacto de una iteración RED→GREEN interrumpida por el rate limit de la corrida anterior. **Archivo `.ts` eliminado; `.tsx` (superconjunto) es la fuente única. Confirmado con `diff` antes de borrar.**
