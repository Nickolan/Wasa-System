> **Governance: MEDIUM** (override del proyecto para el dominio Auth, CHANGE-01..07 — ver `design.md` §Context).
> **Checkpoint de governance ya cerrado.** El usuario respondió las cinco decisiones que estaban marcadas para revisión en `design.md`, y los artefactos ya reflejan sus respuestas: **D-2 aceptada** (techo de 72 bytes UTF-8, medido en bytes y no en caracteres), **D-3 aceptada tal cual** (`min_length=1` en `UserLogin`), **D-4 aceptada tal cual** (sin reglas de complejidad), **D-6 aceptada tal cual** (`str` + `repr=False`, no `SecretStr`) y **D-10 aceptada con cambio** (`ErrorDetail` va en el módulo nuevo `schemas/error_schemas.py`, desviación del roadmap aprobada). No queda nada pendiente de confirmación: se puede implementar de corrido.
>
> **Strict TDD**: cada tarea de implementación arranca por el test (RED), sigue con el mínimo código (GREEN), triangula con casos adicionales y refactoriza. Los grupos 3 a 6 están ordenados para que el test de cada uno se pueda escribir antes que su implementación.

## 1. Red de seguridad y decisiones ya cerradas

- [x] 1.1 Correr la suite completa de `fastapi_bridge/tests/` y registrar el baseline (`N passed`). Si algo ya falla, reportarlo como falla preexistente y no arreglarlo dentro de este change. **Baseline: 89 passed, 0 failed.**
- [x] 1.2 Verificar que `email-validator` está instalado y que `from pydantic import EmailStr` funciona en el venv del proyecto — entra por `pydantic[email]>=2.0`, ya declarado en `requirements.txt` desde CHANGE-00a. Este change es el primero que lo ejercita de verdad; no se agrega ninguna dependencia nueva. **Verificado: pydantic 2.13.4, `EmailStr` funciona.**
- [x] 1.3 **Checkpoint con el usuario (MEDIUM governance) — CERRADO**: las cinco decisiones (D-2, D-3, D-4, D-6, D-10) fueron presentadas y respondidas; `design.md`, los dos specs, `proposal.md` y este `tasks.md` ya están actualizados con las respuestas. Antes de escribir código, releer la tabla "Resueltas en el checkpoint de governance" al final de `design.md` y **no reabrir** ninguna de esas decisiones.
- [x] 1.4 Releer `fastapi_bridge/schemas/auth_schemas.py` y `schemas/scan_schemas.py` y anotar que sus docstrings placeholder citan números de change desactualizados (CHANGE-04/06/12) y que el de scan aún promete `ErrorDetail`, que por D-10 ya no vive ahí; se corrigen en el refactor de los grupos 3 y 6. Confirmar además que `fastapi_bridge/schemas/error_schemas.py` **no existe todavía** y que `schemas/__init__.py` sí, de modo que el módulo nuevo del grupo 6 sea importable sin tocar el paquete. **Confirmado: ambos placeholders citan CHANGE-04/06/12; `error_schemas.py` no existe; `schemas/__init__.py` existe.**

## 2. Frontera de capa de `schemas/`

- [x] 2.1 RED — agregar a `LAYER_IMPORT_RULES` de `fastapi_bridge/tests/test_layer_boundaries.py` las tres filas `("schemas", "fastapi")`, `("schemas", "sqlalchemy")` y `("schemas", "httpx")` (D-12). Confirmar que los tests parametrizados nuevos aparecen y pasan sobre los placeholders actuales. **9 passed (5 previos + 3 nuevos + el negativo).**
- [x] 2.2 Verificar que el test negativo `test_helper_detects_a_forbidden_import` sigue en verde: la regla nueva no vale nada si el helper dejara de detectar imports.

## 3. `UserRegister` — contrato y política de contraseña

- [x] 3.1 RED — crear `fastapi_bridge/tests/test_auth_schemas.py` y escribir los tests de forma de `UserRegister`: payload válido se construye; campos exactamente `{email, password}`; falta de `email` o de `password` produce `ValidationError`; email sin arroba / sin dominio / vacío produce `ValidationError` señalando `email`. **RED confirmado: ImportError (UserRegister no existe).**
- [x] 3.2 GREEN — implementar `UserRegister(BaseModel)` en `schemas/auth_schemas.py` con `email: EmailStr` y `password: str = Field(..., min_length=8, repr=False)` (D-1, D-6). Type hints obligatorios; sin imports de FastAPI, SQLAlchemy, httpx ni `Settings`. **8/8 passed.**
- [x] 3.3 TRIANGULATE — tests de la política de longitud: 7 caracteres falla, exactamente 8 pasa, contraseña solo de minúsculas de longitud válida pasa (D-4, sin reglas de complejidad). **11/11 passed.**
- [x] 3.4 RED — tests del techo de bcrypt (D-2): contraseña de 73 bytes ASCII falla; contraseña de menos de 72 **caracteres** pero más de 72 **bytes** UTF-8 (multibyte) falla; contraseña multibyte de 72 bytes o menos pasa. **RED confirmado: 2 failed (sin techo implementado).**
- [x] 3.5 GREEN — implementar el validador del techo: una función que mide `len(value.encode("utf-8"))` y lanza si supera 72, envuelta en un alias `Annotated[str, AfterValidator(...)]` a nivel de módulo para reutilizarla en `UserLogin` (D-2, decisión **aceptada** por el usuario). Usar `mode="after"` según la convención ya establecida en `core/settings.py`. Repasar que `UserRegister.password` pase a declararse sobre ese alias (conservando `min_length=8` y `repr=False` de 3.2) y **no** con un `max_length=72`: `max_length` cuenta caracteres y dejaría pasar una contraseña multibyte que después revienta en bcrypt. **14/14 passed.**
- [x] 3.6 RED+GREEN — `extra="forbid"` (D-5): test de que un campo adicional no declarado produce `ValidationError` identificando ese campo; implementar con `model_config = ConfigDict(extra="forbid")`. **RED: 1 failed. GREEN: 15/15 passed.**
- [x] 3.7 TRIANGULATE — test de no filtración (RN-WS-12): `repr(UserRegister(...))` no contiene el valor de la contraseña. Usar una contraseña con un marcador único e improbable para que el assert no pueda pasar por casualidad. **16/16 passed.**
- [x] 3.8 REFACTOR — extraer a constantes de módulo el mínimo (8) y el techo (72) con nombres explícitos, para que el spec y el código citen el mismo número una sola vez. **16/16 passed tras el refactor (`REGISTER_PASSWORD_MIN_LENGTH`, `_BCRYPT_MAX_PASSWORD_BYTES`).**

## 4. `UserLogin` — contrato asimétrico respecto del registro

- [x] 4.1 RED — tests de `UserLogin`: payload válido pasa; email inválido falla señalando `email`; contraseña vacía falla; campo extra falla (`extra="forbid"`). **RED confirmado: ImportError (UserLogin no existe).**
- [x] 4.2 RED — el test que codifica la asimetría deliberada (D-3): una contraseña de menos de 8 caracteres **pasa** en `UserLogin` aunque falle en `UserRegister`. Comentar en el test *por qué* (evita bloquear usuarios de una política anterior y evita que un 422 distinga el fallo del 401 genérico de HU-03-02), para que nadie "unifique" las constraints en un refactor futuro. **Incluido en el mismo RED de 4.1.**
- [x] 4.3 GREEN — implementar `UserLogin(BaseModel)` con `email: EmailStr`, `password` sobre el alias `Annotated` del techo de 72 bytes con `min_length=1` y `repr=False`, y `model_config = ConfigDict(extra="forbid")`. **21/21 passed.**
- [x] 4.4 TRIANGULATE — verificar que `UserLogin` también rechaza la contraseña de más de 72 bytes (comparte el alias de 3.5) y que `repr()` no filtra la contraseña. **23/23 passed.**

## 5. `TokenResponse` y `TokenData`

- [x] 5.1 RED — tests de `TokenResponse`: se construye sin pasar `token_type` y este vale `"bearer"`; un `token_type` distinto de `"bearer"` falla; `expires_in` de 0 o negativo falla; `expires_in = 86400` (24 h en segundos, D-8) es válido. **RED confirmado: ImportError (TokenResponse/TokenData no existen).**
- [x] 5.2 GREEN — implementar `TokenResponse(BaseModel)` con `access_token: str`, `token_type: Literal["bearer"] = "bearer"` (D-7) y `expires_in: int = Field(..., gt=0)` (D-8). Documentar en el docstring que la unidad es **segundos** y que la conversión desde `settings.TOKEN_EXPIRE_HOURS` la hace el `AuthService` (CHANGE-04), no el schema. **31/31 passed (implementado junto con 5.4).**
- [x] 5.3 RED — tests de `TokenData`: se construye sin `email` y este queda en `None`; un `email` que no es un email válido **no** falla (D-1); un payload con claims extra (`exp`, `iat`, `sub`) se construye sin error (D-5, asimetría con los schemas de entrada). **Incluido en el mismo RED de 5.1.**
- [x] 5.4 GREEN — implementar `TokenData(BaseModel)` con `email: str | None = None` y **sin** `extra="forbid"`. Documentar en el docstring por qué la asimetría existe, para que nadie la "corrija" y rompa CHANGE-06. **31/31 passed.**
- [x] 5.5 TRIANGULATE — test estructural de RN-WS-12: recorrer los modelos definidos en `auth_schemas.py` y afirmar que ningún modelo de respuesta declara `password` ni `hashed_password`, y que no existe ningún `UserResponse`. **33/33 passed.**
- [x] 5.6 TRIANGULATE — test de independencia (spec `auth-contracts`): `auth_schemas.py` no importa `fastapi_bridge.db.models` ni `fastapi_bridge.db.session` ni ningún módulo de configuración. Reutilizar el helper AST `get_imported_top_level_modules` de `test_layer_boundaries.py` en lugar de duplicar el parseo. **34/34 passed.**
- [x] 5.7 REFACTOR — reemplazar el docstring placeholder de `auth_schemas.py` por documentación real: qué contratos define, a qué reglas de negocio responden (RN-WS-12, RN-WS-15) y qué changes los consumen. **126/126 passed (suite completa) tras el refactor.**

## 6. `ErrorDetail` — contrato de error RFC 7807 en su módulo propio

> D-10 quedó **decidido**: `ErrorDetail` vive en `fastapi_bridge/schemas/error_schemas.py`, un módulo nuevo, y **no** en `scan_schemas.py`. Es una desviación del texto de `CHANGES.md` aprobada explícitamente por el usuario; se documenta en la nota de implementación de 8.1.

- [x] 6.1 RED — crear `fastapi_bridge/tests/test_error_schemas.py` y escribir los tests de forma de `ErrorDetail`, importándolo desde `fastapi_bridge.schemas.error_schemas` (módulo que todavía no existe: el import fallando **es** el RED): construcción válida; campos exactamente `{type, title, status, detail, instance}`; `type` por defecto `"about:blank"`. **RED confirmado: ModuleNotFoundError.**
- [x] 6.2 GREEN — crear el módulo `fastapi_bridge/schemas/error_schemas.py` e implementar en él `ErrorDetail(BaseModel)` con `type: str = "about:blank"`, `title: str`, `status: int = Field(..., ge=100, le=599)`, `detail: str`, `instance: str` (D-9, D-10). Type hints obligatorios; sin imports de FastAPI, SQLAlchemy ni httpx. **3/3 passed.**
- [x] 6.3 TRIANGULATE — tests del rango de `status`: 400, 401, 409, 422, 429, 502 y 500 pasan (los siete estados que CHANGE-07 debe producir); 99 falla; 600 falla. **15/15 passed.**
- [x] 6.4 TRIANGULATE — tests de las referencias URI (D-9): `instance` con un path relativo (`/api/v1/auth/login`) pasa; `type` con una URI absoluta pasa. Ambos casos confirman que no se usó `AnyUrl`. **Incluido en el mismo run de 15/15.**
- [x] 6.5 TRIANGULATE — test de ubicación (spec `error-contract`): `ErrorDetail` está definido en `fastapi_bridge.schemas.error_schemas` y `fastapi_bridge.schemas.scan_schemas` **no** lo define ni lo reexporta, de modo que nadie lo reubique de vuelta en un módulo de dominio sin un test rojo. **Incluido en el mismo run de 15/15.**
- [x] 6.6 REFACTOR — escribir el docstring de `error_schemas.py`: que declara la forma canónica RFC 7807 de todo error de la API (RN-WS-09), que es un contrato **transversal** compartido por Auth y Scan (por eso no vive en el módulo de un dominio) y que la emisión de estos errores es responsabilidad de `exceptions/handlers.py` (CHANGE-07), no de este módulo. **Escrito directamente en el GREEN de 6.2 (docstring completo desde el inicio); verificado que sigue vigente.**
- [x] 6.7 REFACTOR — actualizar el docstring placeholder de `scan_schemas.py`: **quitar** la promesa de `ErrorDetail` (que por D-10 se mudó a `error_schemas.py`), dejar únicamente `ScanRequest`/`ScanResponse`/`N8nPayload` y corregir la referencia obsoleta a CHANGE-12 por CHANGE-08. El módulo sigue siendo un placeholder sin símbolos exportados: este change no lo llena. **141/141 passed (suite completa) tras el refactor.**

## 7. Verificación e integración con la suite existente

- [x] 7.1 Correr la suite completa y comparar contra el baseline de 1.1: ningún test previamente verde puede quedar en rojo. En particular `tests/test_structure.py` (los 18 módulos siguen existiendo y siendo importables — sus asserts son de existencia, no de conjunto exacto, así que `error_schemas.py` como módulo 19 no lo rompe) y `tests/test_layer_boundaries.py` con las filas nuevas, que al declararse sobre el paquete `schemas/` ya cubren `error_schemas.py` sin filas adicionales. **142 passed, 0 failed (baseline 89 + 53 nuevos: 3 de layer_boundaries, 36 de test_auth_schemas, 15 de test_error_schemas; 0 regresiones).**
- [x] 7.2 Verificar que `tests/test_settings.py` y `tests/test_env_contract.py` siguen en verde: este change no agrega ni consume variables de entorno. **17/17 passed (con test_no_shared_db_impact.py).**
- [x] 7.3 Verificar que `tests/test_no_shared_db_impact.py` sigue en verde: los schemas no abren conexiones ni referencian tablas. **Verificado en el mismo run que 7.2.**
- [x] 7.4 Confirmar de punta a punta la ausencia de la contraseña en salidas: para cada modelo de respuesta del dominio auth, `model_dump()` no contiene ninguna clave de contraseña. **142/142 passed (test_token_response_model_dump_has_no_password_key agregado).**
- [x] 7.5 Correr `openspec validate auth-pydantic-schemas --strict` y confirmar que pasa. **"Change 'auth-pydantic-schemas' is valid".**

## 8. Cierre y traspaso

- [x] 8.1 Marcar `[CHANGE-02] auth-pydantic-schemas` como completado en `CHANGES.md`, tildando sus criterios de aceptación y agregando una **Nota de implementación** con las dos desviaciones respecto del scope original, ambas aprobadas por el usuario en el checkpoint de 1.3: (a) el techo de 72 bytes UTF-8 en la contraseña, que no estaba en el roadmap ni en la KB; (b) `ErrorDetail` ubicado en `schemas/error_schemas.py` y no en `schemas/scan_schemas.py` como decía el texto del roadmap. Corregir además toda referencia de `CHANGES.md` que ubique `ErrorDetail` en `scan_schemas.py`, incluidas las de CHANGE-07 y CHANGE-08 si las hubiera, para que ningún change posterior importe del módulo equivocado. **Hecho: única referencia obsoleta era la línea de scope de CHANGE-02 (CHANGE-07 y CHANGE-08 no mencionan `ErrorDetail`).**
- [x] 8.2 Registrar en `CHANGES.md` o en la nota de CHANGE-04 el hallazgo bloqueante R-1 (`passlib 1.7.4` roto contra `bcrypt 5.0.0`), con las dos salidas posibles: fijar `bcrypt<4.1` o reemplazar `passlib` por la librería `bcrypt` directa. No se resuelve en este change. **Registrado como nota bloqueante en la sección de CHANGE-04.**
- [x] 8.3 Guardar en Engram las decisiones de contrato (D-2, D-3, D-5, D-8, D-10) y el hallazgo R-1, con `topic_key: "opsx/auth-pydantic-schemas/apply"`. **Guardado (obs id 501, sin conflictos de judgment).**
- [x] 8.4 Anotar para CHANGE-14 (Zod) la paridad obligatoria: mismo mínimo de 8, mismo techo de 72 bytes, mismos campos y sin campos extra — de lo contrario el formulario deja escribir contraseñas que el backend rechaza con un 422 opaco. **Nota agregada a la sección de CHANGE-14 en `CHANGES.md`.**
