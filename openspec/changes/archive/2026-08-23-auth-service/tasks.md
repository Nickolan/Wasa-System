> **Governance: MEDIUM** (override del proyecto para el dominio Auth, CHANGE-01..07 — `CHANGES.md` marca este change como CRÍTICO y el `CLAUDE.md` del proyecto lo baja a MEDIO por decisión explícita del usuario; ver `design.md` §Context, restricción 7).
> **Checkpoint de governance CERRADO** (tarea 1.4, 2026-08-22): las seis decisiones **⚠ REVISIÓN** de `design.md` (D-1, D-2, D-3, D-4, D-5, D-8) fueron confirmadas por el usuario tal como recomendadas. El grupo 2 puede abrirse.
>
> **Strict TDD**: cada tarea de implementación arranca por el test (RED), sigue con el mínimo código (GREEN), triangula con casos adicionales y refactoriza. Los grupos están ordenados de abajo hacia arriba: primitivas puras (3, 4), error de dominio (5), límite transaccional (6), y recién después las operaciones de negocio que los componen (7, 8).

## 1. Red de seguridad y checkpoint de governance

- [x] 1.1 Correr la suite completa (`fastapi_bridge/.venv/Scripts/python.exe -m pytest fastapi_bridge/tests/ -q`) y registrar el baseline (`N passed`). Si algo ya falla, reportarlo como falla preexistente y **no** arreglarlo dentro de este change. Baseline esperado según el cierre de CHANGE-03: ~230 passed (CHANGE-03 cerró con 182 + los tests que agregó). **Baseline real: 225 passed, 9 warnings (0 fallas preexistentes).**
- [x] 1.2 Verificar el estado del venv y dejar registradas las versiones exactas de `bcrypt`, `passlib`, `python-jose` y `anyio`. Reproducir R-1 explícitamente: `CryptContext(schemes=["bcrypt"]).hash("secret")` debe fallar. Anotar el error observado — es la evidencia que justifica D-1 ante el usuario. **Versiones: bcrypt 5.0.0, passlib 1.7.4, python-jose 3.5.0, anyio 4.14.2. R-1 reproducido: `AttributeError: module 'bcrypt' has no attribute '__about__'` (trapped por passlib), seguido de `ValueError: password cannot be longer than 72 bytes` al hashear `"secret"` (6 bytes) — evidencia de que passlib degrada a una ruta de código incorrecta al no poder leer la versión del backend.**
- [x] 1.3 Releer los tres placeholders que este change rellena (`core/security.py`, `uow/auth_unit_of_work.py`, `services/auth_service.py`) y confirmar que sus docstrings citan números de change obsoletos ("CHANGE-01", "CHANGE-03", "CHANGE-06") respecto del roadmap vigente. Anotarlo para corregirlos en el grupo 10, no ahora. **Confirmado: los tres docstrings citan CHANGE-01/03/06 en vez de CHANGE-04. Se reescriben en 10.1/10.2/6.11.**
- [x] 1.4 **Checkpoint con el usuario (MEDIUM governance) — CERRADO (2026-08-22)**: las seis decisiones se presentaron con recomendación y alternativa; el usuario aceptó las seis recomendaciones sin cambios: **D-1** sacar `passlib`, usar `bcrypt` directo. **D-2** coste 12 fijo. **D-3** primitivas síncronas + `anyio.to_thread.run_sync`. **D-4** único access token de 24h, sin refresh. **D-5** `Settings` como parámetro explícito. **D-8** verificación señuelo en `login`. Ninguna reabre `design.md` ni los specs — quedan tal como se propusieron.
- [x] 1.5 Confirmar que la fixture `user_session` de `fastapi_bridge/tests/conftest.py` (CHANGE-03) sigue disponible y que crea únicamente `User.__table__` sobre `sqlite+aiosqlite:///:memory:`. Los grupos 6, 7 y 8 se apoyan enteramente en ella; si no estuviera, el orden del plan cambia. **Confirmado en `conftest.py` líneas 83-105.**

## 2. Manifiesto de dependencias y fronteras de capa (D-1, R-1)

> Este grupo se ejecuta **antes** que cualquier código de producción: sin resolver R-1 no hay una línea de `hash_password` que pueda correr.

- [x] 2.1 Aplicar la resolución de D-1 en `fastapi_bridge/requirements.txt`. Si se aceptó la recomendación: reemplazar `passlib[bcrypt]>=1.7` por `bcrypt>=4.1`. Si se aceptó la alternativa (a): dejar `passlib[bcrypt]>=1.7` y agregar `bcrypt<4.1`, y revertir el delta de `bridge-bootstrap` en `specs/` antes de seguir.
- [x] 2.2 Reinstalar dependencias en el venv y verificar que la librería de hashing elegida hashea de verdad: derivar un hash y verificarlo, en un one-liner, antes de escribir tests. Si esto falla, R-1 no está resuelto y el resto del change no puede avanzar.
- [x] 2.3 RED — agregar a `LAYER_IMPORT_RULES` (`fastapi_bridge/tests/test_layer_boundaries.py`) las filas `("services", "bcrypt")`, `("services", "passlib")`, `("services", "jose")` y `("repositories", "bcrypt")`. Confirmar que los tests parametrizados nuevos aparecen y pasan sobre los placeholders actuales (nada importa todavía esas librerías). La fila `("repositories", "passlib")` que dejó CHANGE-03 se **conserva**: aunque `passlib` salga del manifiesto, la regla sigue siendo cierta y su costo es cero.
- [x] 2.4 Verificar que `test_helper_detects_a_forbidden_import` sigue en verde: las filas nuevas no valen nada si el helper AST dejara de detectar imports.
- [x] 2.5 RED/GREEN — agregar a `fastapi_bridge/tests/test_structure.py` (o a un test propio del manifiesto) la verificación de que `requirements.txt` declara la librería de hashing resuelta en 2.1 y —si se aceptó la recomendación— que **no** declara `passlib`. Es el ancla del escenario "Sin dependencias sin mantenimiento para la criptografía" del delta de `bridge-bootstrap`.

## 3. `hash_password` / `verify_password` — capability `password-hashing` (D-1, D-2)

- [x] 3.1 RED — crear `fastapi_bridge/tests/test_security.py` importando `hash_password` y `verify_password` desde `fastapi_bridge.core.security` (todavía inexistentes: el `ImportError` es el RED). Tests: `hash_password("secret")` devuelve un `str` que empieza por `$2b$`; `verify_password("secret", hash)` es `True`; `verify_password("wrong", hash)` es `False`. Son tres de los criterios de aceptación de `CHANGES.md`.
- [x] 3.2 GREEN — implementar ambas funciones en `core/security.py` con la librería resuelta en D-1, con type hints completos y **síncronas** (`def`, no `async def` — D-3). Declarar `_BCRYPT_ROUNDS = 12` como constante de módulo (D-2).
- [x] 3.3 TRIANGULATE — sal aleatoria: dos `hash_password` de la misma contraseña devuelven strings distintos, y ambos verifican `True` contra la contraseña original. Sin este test, una implementación con sal fija pasaría 3.1 sin problema.
- [x] 3.4 TRIANGULATE — coste embebido: el hash producido declara coste `12` en su prefijo. Este test es el ancla de D-2: bajar el coste en el código lo pone en rojo.
- [x] 3.5 TRIANGULATE — casos de verificación adicionales: contraseña con otra capitalización devuelve `False`; contraseña con un espacio final devuelve `False` (la contraseña **no** se normaliza, a diferencia del email); una contraseña Unicode multibyte hashea y verifica correctamente.
- [x] 3.6 RED — hash corrupto: `verify_password("secret", "no-soy-un-hash")` y `verify_password("secret", "")` deben devolver `False`, no propagar la excepción de la librería. El RED es la excepción escapándose.
- [x] 3.7 GREEN — envolver la verificación en el manejo de la excepción de la librería y devolver `False`. **No** capturar `Exception` a secas: capturar el tipo concreto que la librería lanza ante un hash inválido, para no tapar errores distintos.
- [x] 3.8 TRIANGULATE — el texto plano no aparece en el hash: afirmar que la contraseña no está contenida como substring en el string devuelto. Es un test barato que documenta RN-WS-12 en el sitio donde se cumple.
- [x] 3.9 TRIANGULATE — la firma de `hash_password` recibe **solo** la contraseña: el coste no es un parámetro del llamador (escenario "El coste no es un parámetro del llamador"). Verificar por `inspect.signature`.
- [x] 3.10 REFACTOR — docstring del bloque de hashing en `core/security.py`: por qué las funciones son síncronas (D-3), por qué el coste es constante y no configuración (D-2), y por qué un hash corrupto devuelve `False` en vez de romper.

## 4. `create_access_token` / `decode_access_token` — capability `access-token` (D-5, D-6, D-7, D-13)

- [x] 4.1 RED — agregar a `tests/test_security.py` los tests de emisión: `create_access_token({"sub": "a@b.com"}, timedelta(hours=24), settings)` devuelve un string de tres segmentos; decodificarlo con la misma clave recupera `sub == "a@b.com"`. Construir `Settings(JWT_SECRET=SecretStr("test-secret"))` a mano, sin tocar el cache de `get_settings` (D-5).
- [x] 4.2 GREEN — implementar `create_access_token(data, expires_delta, settings)` con `python-jose`, `_JWT_ALGORITHM = "HS256"` como constante de módulo (D-6), agregando `exp` e `iat` al payload (D-13). Desenvolver el `SecretStr` **dentro** de esta función, nunca fuera.
- [x] 4.3 TRIANGULATE — el token lleva `exp` e `iat`, ambos coherentes con el instante de emisión y el `expires_delta` recibido; emitir con un `expires_delta` distinto produce un `exp` distinto (el vencimiento sigue al parámetro, no a un valor fijo).
- [x] 4.4 RED — tests de decodificación del camino feliz: `decode_access_token(valid_jwt, settings)` devuelve un `TokenData` con el email correcto (criterio de aceptación de `CHANGES.md`).
- [x] 4.5 GREEN — implementar `decode_access_token(token, settings)` con `jwt.decode(..., algorithms=[_JWT_ALGORITHM])` — **lista explícita** (D-6) — devolviendo `TokenData(email=payload.get("sub"))`.
- [x] 4.6 RED — el test más importante del change (R-5): construir un token con `alg: none` (sin firma) y otro firmado con una clave **distinta**, y exigir `TokenData(email=None)` en ambos casos. Comentar en el test por qué existe: un `decode` sin lista de algoritmos permite suplantar cualquier identidad.
- [x] 4.7 GREEN — asegurar que ambos casos se rechazan. Si `python-jose` ya los rechaza por defecto con la lista explícita, el test queda como ancla de regresión, no como corrección.
- [x] 4.8 RED — token expirado: emitir con un `expires_delta` negativo (o congelar el reloj) y exigir `TokenData(email=None)` en vez de excepción (criterio de aceptación de `CHANGES.md`, D-7).
- [x] 4.9 GREEN — capturar la excepción de la librería de JWT y devolver `TokenData(email=None)`. Capturar el tipo base de errores de JWT, no `Exception` a secas.
- [x] 4.10 TRIANGULATE — resto de los rechazos, todos con el mismo resultado: token basura que no tiene forma de JWT; token válido y vigente **sin** claim `sub`; token válido con un carácter alterado en el segmento de payload; token con `alg: RS256` en la cabecera. Afirmar además que los cuatro resultados son **iguales entre sí** (escenario "Todos los rechazos son indistinguibles").
- [x] 4.11 TRIANGULATE — el tipo devuelto es siempre `TokenData`, válido o no (nunca `None`, nunca un `dict`), de modo que CHANGE-06 no tenga que ramificar por tipo de retorno.
- [x] 4.12 TRIANGULATE — test AST sobre `core/security.py` (reutilizando el helper de `test_layer_boundaries.py` o `ast` directo): ninguna sentencia de `logging` recibe el token, el secreto ni la contraseña; no hay literales usados como clave de firma. Ancla los escenarios de "El secreto de firma nunca se registra ni se expone".
- [x] 4.13 TRIANGULATE — test AST sobre el código de producción fuera de `core/security.py`: ningún módulo invoca `.get_secret_value()` sobre `JWT_SECRET`. El único que desenvuelve el secreto es el módulo que firma y verifica.
- [x] 4.14 REFACTOR — docstring del bloque de JWT: por qué `algorithms` es una lista explícita y nunca sale de la cabecera del token (D-6), por qué la función no lanza (D-7), y por qué `Settings` llega por parámetro en vez de por `get_settings()` (D-5).

## 5. `InvalidCredentialsError` — error de dominio (D-11)

- [x] 5.1 RED — agregar a `fastapi_bridge/tests/test_domain_exceptions.py` los tests de la excepción nueva: se importa desde `fastapi_bridge.exceptions.domain`; hereda de `DomainError`; se construye **sin argumentos**; su mensaje es fijo.
- [x] 5.2 GREEN — agregar `class InvalidCredentialsError(DomainError)` a `exceptions/domain.py`, con mensaje constante ("email o contraseña incorrectos") y sin atributos. Es lo que anticipa el docstring que dejó CHANGE-03 — extender ese módulo, no crear uno nuevo.
- [x] 5.3 TRIANGULATE — la excepción **no** expone el email: afirmar que no tiene atributo `email` y que su `str()` no contiene ningún email (D-11). Es el ancla que impide que CHANGE-07 lo interpole en el `detail` del RFC 7807.
- [x] 5.4 TRIANGULATE — `except DomainError` la atrapa, igual que a `EmailAlreadyExistsError`: es lo que habilita el handler único de CHANGE-07. Verificar además que el test AST de independencia del framework web que dejó CHANGE-03 (`forbidden = {"fastapi", "starlette", "slowapi", "sqlalchemy", "passlib"}`) sigue en verde, y agregarle `bcrypt` y `jose` al conjunto prohibido.
- [x] 5.5 REFACTOR — docstring de `InvalidCredentialsError` explicando la asimetría con `EmailAlreadyExistsError` (una lleva el email, la otra no) y citando RN-WS-12 §Excepciones globales.

## 6. `AuthUoW` — límite transaccional (D-9, traspaso D-5/R-4 de CHANGE-03)

- [x] 6.1 RED — crear `fastapi_bridge/tests/test_auth_uow.py` importando `AuthUoW` desde `fastapi_bridge.uow.auth_unit_of_work` (inexistente: el `ImportError` es el RED). Construir la factory de sesiones sobre el motor SQLite en memoria de la fixture de CHANGE-03 (no vía `get_session_factory`, que apuntaría a `db_fuzzing`). Test: entrar al bloque expone `uow.users` como un `UserRepository`.
- [x] 6.2 GREEN — implementar `AuthUoW` con `__init__(session_factory)`, `__aenter__` (abre sesión, construye `UserRepository`, devuelve `self`) y `__aexit__` (cierra la sesión). Type hints completos. Sin imports de FastAPI.
- [x] 6.3 RED — camino feliz: dar de alta un usuario dentro del bloque, salir sin excepción, y verificar **desde una sesión nueva** que el usuario quedó persistido. El RED es que sin `commit` en `__aexit__` no está.
- [x] 6.4 GREEN — `commit()` en `__aexit__` cuando no hubo excepción.
- [x] 6.5 RED — camino de fallo: dar de alta un usuario dentro del bloque y lanzar una excepción arbitraria antes de salir; verificar que la excepción se propaga al llamador y que el usuario **no** quedó persistido.
- [x] 6.6 GREEN — `rollback()` en `__aexit__` ante excepción, sin suprimirla (`__aexit__` devuelve `False`/`None`).
- [x] 6.7 TRIANGULATE — deshacer también ante excepción **de dominio**: intentar dar de alta un email ya registrado dentro del bloque, dejar escapar `EmailAlreadyExistsError`, y verificar que la transacción se deshizo y que la sesión no quedó con sentencias pendientes. Es el caso que más importa: es el que va a ocurrir en producción.
- [x] 6.8 TRIANGULATE — la sesión se cierra siempre: verificar tras salir del bloque (con y sin excepción) que la sesión quedó cerrada.
- [x] 6.9 TRIANGULATE — reentrancia (D-9): usar la **misma instancia** en dos bloques consecutivos; el segundo abre una sesión nueva y su resultado no depende del estado del primero. Es lo que permite que CHANGE-05 inyecte un `AuthService` de vida larga.
- [x] 6.10 TRIANGULATE — acceder a `uow.users` **fuera** del bloque lanza un error explícito que menciona que la UoW no está activa, no un `AttributeError` opaco ni un `None`.
- [x] 6.11 REFACTOR — reescribir el docstring del módulo (hoy es el placeholder de CHANGE-00a con referencias de change desactualizadas): qué garantiza la UoW, por qué el constructor recibe la factory y no la sesión, y por qué el `commit` vive acá y no en el Service (D-9, traspaso de CHANGE-03).

## 7. `AuthService.register` — capability `auth-session` (D-3, D-10, D-12, D-13)

- [x] 7.1 RED — crear `fastapi_bridge/tests/test_auth_service.py` importando `AuthService` desde `fastapi_bridge.services.auth_service` (inexistente: el `ImportError` es el RED). Montar un `AuthService(AuthUoW(factory))` sobre el motor en memoria. Test: `await service.register(UserRegister(email=..., password=...))` devuelve un `TokenResponse`.
- [x] 7.2 GREEN — implementar `AuthService.__init__(self, uow: AuthUoW)` y `async def register(self, data: UserRegister) -> TokenResponse`: hashea, `async with self._uow as uow: user = await uow.users.create(...)`, emite token, devuelve `TokenResponse`. Hashear con la descarga a thread pool desde el principio (D-3) — no "lo agregamos después". Type hints completos. Sin imports de SQLAlchemy ni de FastAPI.
- [x] 7.3 TRIANGULATE — lo persistido es el hash: leer la fila desde una sesión nueva y verificar que `hashed_password` es un hash bcrypt, distinto del texto plano, y que `verify_password(plain, stored)` es `True` (RN-WS-12).
- [x] 7.4 TRIANGULATE — la respuesta no filtra nada: el `TokenResponse` devuelto no contiene la contraseña ni el hash en ninguno de sus campos, y su `token_type` es `"bearer"`.
- [x] 7.5 TRIANGULATE — el `sub` del token es el email **normalizado** (D-13): registrar `USER@TEST.COM` y verificar que `decode_access_token(resp.access_token, settings).email == "user@test.com"`, el mismo valor que quedó en la base. Es el test que impide la clase entera de bug de CHANGE-06.
- [x] 7.6 TRIANGULATE — `expires_in` (D-12): está en **segundos**, es `TOKEN_EXPIRE_HOURS * 3600`, y coincide con la distancia entre el `iat` y el `exp` del token emitido. Repetir con un `Settings` de expiración distinta para confirmar que ambos siguen a la misma fuente.
- [x] 7.7 RED — email duplicado: registrar dos veces el mismo email y esperar `EmailAlreadyExistsError` (criterio de aceptación de `CHANGES.md`). Triangular con el mismo email en otra capitalización.
- [x] 7.8 GREEN — confirmar que la excepción se propaga tal cual desde el repositorio, sin capturarla ni reenvolverla, y que la UoW la convierte en `rollback`. Verificar que no quedó un segundo usuario.
- [x] 7.9 TRIANGULATE — sin consulta previa de existencia (D-10, R-7 de CHANGE-03): test AST sobre `services/auth_service.py` que afirme que `register` no invoca `get_by_email`. Comentar en el test que un pre-chequeo es optimización de UX, no la garantía de RN-WS-13, y que agregarlo invita a borrar la captura que sí la garantiza.

## 8. `AuthService.login` — 401 indistinguible en mensaje y en tiempo (D-8, D-11)

- [x] 8.1 RED — camino feliz: registrar un usuario y luego `await service.login(UserLogin(email=..., password=...))` devuelve un `TokenResponse` cuyo token decodifica a ese email.
- [x] 8.2 GREEN — implementar `async def login(self, data: UserLogin) -> TokenResponse`: `async with self._uow as uow: user = await uow.users.get_by_email(data.email)`, verificar contraseña (con descarga a thread pool, D-3), emitir token.
- [x] 8.3 TRIANGULATE — el email se acepta con cualquier capitalización (lo garantiza la normalización simétrica de CHANGE-03, D-4), y la contraseña **no** se normaliza: la misma contraseña con otra capitalización falla.
- [x] 8.4 RED — contraseña incorrecta lanza `InvalidCredentialsError` (criterio de aceptación de `CHANGES.md`).
- [x] 8.5 RED — email inexistente lanza **la misma** `InvalidCredentialsError`.
- [x] 8.6 GREEN — implementar ambos rechazos con la misma excepción y el mismo mensaje. **No** lanzar excepciones distintas ni incluir el email.
- [x] 8.7 TRIANGULATE — indistinguibilidad en el mensaje: capturar las dos excepciones (email inexistente y contraseña incorrecta) y afirmar que son del mismo tipo y que `str(exc)` es **idéntico** entre ambas. Es el ancla de RN-WS-12 §Excepciones globales.
- [x] 8.8 RED — indistinguibilidad en el tiempo (D-8): test que mide los dos caminos de rechazo y exige que sean del mismo orden de magnitud. El RED es el camino "email inexistente" retornando en microsegundos. Usar un umbral relativo (ratio), no absoluto, para que el test no sea frágil en CI.
- [x] 8.9 GREEN — agregar `_DUMMY_PASSWORD_HASH`, constante de módulo derivada **una sola vez**, y ejecutar `verify_password(data.password, _DUMMY_PASSWORD_HASH)` descartando el resultado cuando `get_by_email` devuelve `None`, antes de lanzar. Comentar en el código por qué el resultado se descarta a propósito (R-3).
- [x] 8.10 TRIANGULATE — el señuelo es constante, no recalculado: test AST que afirme que el hash señuelo se deriva a nivel de módulo y no dentro de `login`.
- [x] 8.11 TRIANGULATE — `login` no escribe: verificar que tras un inicio de sesión exitoso la fila del usuario queda idéntica (mismo `hashed_password`, mismo `created_at`).
- [x] 8.12 TRIANGULATE — el rechazo no registra el email: test AST sobre el camino de rechazo confirmando que ninguna sentencia de `logging` recibe `data.email`.

## 9. Anclajes estructurales del Service (D-3, R-4)

- [x] 9.1 RED/GREEN — test AST sobre `services/auth_service.py` que exija que las invocaciones a `hash_password` y `verify_password` pasen por la descarga a thread pool (`anyio.to_thread.run_sync` o equivalente) y **no** se llamen directamente dentro de la corrutina (R-4). Es el test que sobrevive a un refactor de "simplificación" que volvería a bloquear el event loop.
- [x] 9.2 TRIANGULATE — test AST que afirme que `services/auth_service.py` no invoca `commit` ni `rollback` sobre nada: ese límite pertenece a la UoW (mismo patrón que el ancla que CHANGE-03 puso sobre el repositorio).
- [x] 9.3 TRIANGULATE — test AST que afirme que ninguna sentencia de `logging` de `services/auth_service.py` recibe la contraseña, el hash ni el token.
- [x] 9.4 Confirmar que las filas agregadas en 2.3 siguen en verde con el código real ya escrito: `services/` no importa `bcrypt`, `passlib` ni `jose`; `repositories/` tampoco. Si alguna falla, el import se movió a la capa equivocada.
- [x] 9.5 Correr la suite completa y medir su duración frente al baseline de 1.1 (R-7). Si el coste 12 la degradó de forma notable, aplicar la mitigación de D-14: monkeypatch del coste en los tests de `AuthService` que no verifican la fortaleza del hash, dejando el coste real solo en los tests dedicados de 3.4. **Baseline 1.90s → 11.17s con 295 tests (vs 225). Mitigación de D-14 ya aplicada: `test_auth_service.py` usa `_BCRYPT_ROUNDS=4` vía monkeypatch (autouse), salvo el test de timing 8.8 que necesita coste real simétrico en ambos caminos. El grueso del incremento (~7s) es `test_security.py`, que hashea a propósito con coste 12 real (ancla D-2) — aceptable, no degrada la suite de forma prohibitiva.**

## 10. Cierre

- [x] 10.1 REFACTOR — reescribir el docstring de `core/security.py` (hoy placeholder de CHANGE-00a, que además menciona una firma `decode_access_token(token) -> dict` desactualizada respecto de `TokenData`): las cuatro primitivas, sus garantías de seguridad y las decisiones D-1, D-2, D-3, D-5, D-6 y D-7 que las explican.
- [x] 10.2 REFACTOR — reescribir el docstring de `services/auth_service.py` (placeholder de CHANGE-00a): las dos operaciones de negocio, por qué el 401 es indistinguible en mensaje **y** en tiempo, y por qué el acceso a datos pasa por la UoW.
- [x] 10.3 Correr la suite completa y registrar el resultado final (`N passed`), comparándolo con el baseline de 1.1. Cero fallas nuevas. **Final: 295 passed, 9 warnings (vs baseline 225 passed) — +70 tests nuevos, 0 regresiones.**
- [x] 10.4 Verificar que `uvicorn fastapi_bridge.main:app` sigue arrancando: este change no monta rutas, pero sí cambió el manifiesto de dependencias (escenario "Instalación limpia" de `bridge-bootstrap`).
- [x] 10.5 Actualizar `CHANGES.md`: marcar CHANGE-04 como `[x]`, tildar sus criterios de aceptación, agregar la sección de notas de implementación con las decisiones del checkpoint, y **cerrar R-1** dejando constancia de cuál de las dos opciones se eligió. Anotar además la nota documental de la Open Question 4 (`knowledge-base/08_arquitectura_propuesta.md` §Seguridad dice "bcrypt vía passlib") y el traspaso a CHANGE-05/06/07: `EmailAlreadyExistsError` → 409, `InvalidCredentialsError` → 401, y `decode_access_token` devolviendo `TokenData(email=None)` como base del 401 de `get_current_user`.
- [x] 10.6 Guardar en engram (`topic_key: "opsx/auth-service/apply"`) las decisiones cerradas del checkpoint y el estado final de R-1, para que CHANGE-05 y CHANGE-06 arranquen sin reabrirlas.
