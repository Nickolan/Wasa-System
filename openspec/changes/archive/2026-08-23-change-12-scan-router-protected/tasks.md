# Tasks — change-12-scan-router-protected

> Strict TDD activo. Cada grupo 4..10 es un ciclo completo **RED → GREEN → TRIANGULATE → REFACTOR**:
> el test se escribe y se ejecuta *antes* que el código de producción que lo satisface.
> Referencias: `specs/scan-endpoint/spec.md` (qué), `design.md` D-1..D-10 (cómo).
>
> **PRECONDICIÓN BLOQUEANTE (D-1, governance ALTO)**: el grupo 1 **no** se puede completar
> hasta que `get_current_user` exista en `fastapi_bridge/core/dependencies.py` (CHANGE-06) y
> los handlers de `HTTPException` / `RequestValidationError` estén registrados (CHANGE-07).
> Hoy ninguno de los dos está en este branch. **No arrancar el apply sin confirmación explícita
> del usuario** sobre la opción A/B/C de D-1.
>
> **Archivos de producción que este change toca (y ningún otro)**:
> `fastapi_bridge/api/v1/scan/router.py`, `fastapi_bridge/core/dependencies.py` (append de
> `get_scan_service`), `fastapi_bridge/exceptions/handlers.py` (append del handler de
> `N8nUnavailableError`) y `fastapi_bridge/main.py`.
> **Tests**: `fastapi_bridge/tests/test_scan_router.py` (nuevo) más las dos actualizaciones
> declaradas por adelantado en D-10 (`test_edge_policy_exclusions.py`, `test_app_wiring.py`).
> **NO se modifican**: `services/scan_service.py`, `uow/`, `repositories/`, `schemas/`,
> `core/limiter.py`, `core/settings.py`, `core/security.py`, `api/v1/auth/router.py`, `pytest.ini`.
>
> **Ningún test hace red real**, ninguno instancia un `httpx.AsyncClient` y ninguno assertea
> valores reales de `N8N_WEBHOOK_URL` / `N8N_WEBHOOK_TOKEN` (D-9).

## 1. Precondición: verificar la dependencia de CHANGE-06 / CHANGE-07

- [x] 1.1 Confirmar con el usuario cuál de las opciones A/B/C de `design.md` D-1 se aplica; no continuar sin respuesta explícita (governance ALTO) — **resuelto en sesión previa: opción A**. CHANGE-06/07 ya están mergeados en este branch (verificado abajo en 1.2/1.3); no se re-preguntó.
- [x] 1.2 Verificar en el árbol que `from fastapi_bridge.core.dependencies import get_current_user` importa sin error y que la función acepta un token *bearer* y devuelve el email del `sub`; si falla, detenerse y reportar la dependencia como no satisfecha — **confirmado**: `fastapi_bridge/core/dependencies.py` define `get_current_user(token: Annotated[str | None, Depends(oauth2_scheme)], settings) -> str`, resuelve `TokenData.email` vía `decode_access_token` y lanza `HTTPException(401, ...)` si no hay email. También expone `CurrentUserEmail = Annotated[str, Depends(get_current_user)]`.
- [x] 1.3 Verificar que `create_app()` registra handlers para `RequestValidationError` y `HTTPException` (CHANGE-07) — de lo contrario los criterios de `401` y `422` en RFC 7807 no son alcanzables y hay que reportarlo antes de escribir un solo test — **confirmado**: `fastapi_bridge/main.py::create_app()` registra `RequestValidationError`, `StarletteHTTPException`, `DomainError`, `RateLimitExceeded` y `Exception` con `add_exception_handler`.
- [x] 1.4 Identificar y anotar cómo se firma un token de prueba con el `JWT_SECRET` del entorno de test (`create_access_token` de `core/security.py`), incluida la forma de generar uno **expirado** y uno firmado con **otro** secreto — los tests del grupo 6 dependen de las tres variantes — **patrón encontrado en `test_auth_dependencies.py`**: `create_access_token({"sub": email}, timedelta(hours=hours), settings)`; `hours=-1` produce un token expirado; `Settings(JWT_SECRET="otra-clave")` produce un token firmado con otro secreto. Para el guard real del Router (no sustituido, D-9) el token debe firmarse con `get_settings()` (la instancia real del proceso), porque `get_current_user` en producción resuelve `Settings` vía `Depends(get_settings)`.

## 2. Safety net

- [x] 2.1 Ejecutar `pytest` completo y anotar el baseline (`N passed`); si algo ya falla, reportarlo como fallo preexistente y NO arreglarlo en este change — **baseline: 573 passed, 0 failed**
- [x] 2.2 Ejecutar aislados `pytest fastapi_bridge/tests/test_edge_policy_exclusions.py fastapi_bridge/tests/test_app_wiring.py fastapi_bridge/tests/test_rate_limit.py` y anotar el conteo verde: son los tres archivos que este change puede alterar por efecto colateral (D-5, D-10) — **24 passed**
- [x] 2.3 Releer `fastapi_bridge/services/scan_service.py` y confirmar de primera mano la firma exacta que este change consume: `ScanService()` sin argumentos y `async start_scan(request: ScanRequest) -> ScanResponse`, sin captura de `N8nUnavailableError` — **confirmado**, `ScanService.__init__(self, uow_factory: Callable[[], ScanUoW] = ScanUoW)` acepta cero argumentos posicionales
- [x] 2.4 Releer `fastapi_bridge/schemas/scan_schemas.py` y anotar los nombres exactos de los campos de `ScanRequest` y `ScanResponse` (los tests de cuerpo del `202` los assertean uno por uno) — **`ScanRequest`**: `target_url: HttpUrl`, `phpsessid: str` (strip+min_length=1), `sqlmap_level: int=1` (1..5), `sqlmap_risk: int=1` (1..3), `extra="ignore"`. **`ScanResponse`**: `scan_id: str`, `status: Literal["queued"]`, `message: str`.

## 3. Andamiaje de tests y dobles (D-9)

- [x] 3.1 Crear `fastapi_bridge/tests/test_scan_router.py` con docstring de módulo en español al estilo de `test_scan_service.py`, y verificar que `pytest fastapi_bridge/tests/test_scan_router.py` lo recolecta sin errores de import — hecho, recolecta 4/4 verde
- [x] 3.2 Escribir `FakeScanService`: registra en una lista pública las `ScanRequest` recibidas por `start_scan`, devuelve una `ScanResponse` fija configurable y admite configurarse para levantar `N8nUnavailableError`; verificar con un test directo del propio doble que registra la solicitud y que levanta cuando se lo configura así — hecho
- [x] 3.3 Escribir el helper `build_client(app, *, client_host=...)` sobre `httpx.ASGITransport` (mismo patrón que `test_rate_limit.py`) y el helper `valid_body()` que devuelva un cuerpo JSON válido según `ScanRequest`; verificar que ambos funcionan contra `GET /health` y contra una ruta desechable — hecho, verificado contra `GET /health`
- [x] 3.4 Escribir la fixture que construye la app vía `create_app()`, sustituye `get_scan_service` y `get_current_user` con `app.dependency_overrides`, y **limpia los overrides al salir**; verificar con un test que tras la fixture `app.dependency_overrides` queda vacío (un override filtrado contamina los tests de `401`) — **nota de secuencia**: `get_scan_service` todavía no existe (llega en 5.2); la fixture se escribió ahora sustituyendo sólo `get_current_user`, con un comentario explícito, y se completa con el override de `get_scan_service` en 5.2 cuando el símbolo exista. Cleanup verificado (`app.dependency_overrides.clear()` + test de una app nueva sin overrides heredados).

## 4. La operación existe y está montada (spec: "Existe una operación de disparo alcanzable por red")

- [x] 4.1 RED: escribir el test que importa `fastapi_bridge.api.v1.scan.router` y afirma que el router tiene exactamente una ruta registrada, con path `/api/v1/scan/start` y métodos `{"POST"}`; ejecutar y verificar que falla porque no hay ninguna ruta (este test aísla además el fallo de import de R-3) — RED confirmado (`assert 0 == 1`)
- [x] 4.2 GREEN: agregar en `router.py` el decorador `@router.post("/start", status_code=202, response_model=ScanResponse)` sobre `async def start_scan(request: Request, scan_request: ScanRequest) -> JSONResponse` con un cuerpo mínimo que devuelva un `JSONResponse` fijo; ejecutar y verificar que el test de 4.1 pasa — GREEN confirmado
- [x] 4.3 RED/GREEN: escribir el test que hace `POST /api/v1/scan/start` sobre la app de producción y afirma que la respuesta **no** es `404`; montar el router en `create_app()` con `include_router(scan_router)` **sin `prefix`** (el router ya lo declara, D-12 de CHANGE-05); ejecutar y verificar que pasa — RED (404==404) confirmado, luego GREEN tras `include_router(scan_router)` en `main.py`
- [x] 4.4 TRIANGULATE: agregar el test que afirma que `GET /api/v1/scan/start` responde `405` (el verbo de disparo es el único admitido); ejecutar y verificar que pasa
- [x] 4.5 TRIANGULATE: agregar el test que afirma que, descartando `/docs`, `/openapi.json` y `/redoc`, las únicas rutas de aplicación son `GET /health` y `POST /api/v1/scan/start` — y que no existe ninguna otra ruta bajo el prefijo del dominio scan; ejecutar y verificar que pasa
- [x] 4.6 Confirmar por revisión que `router.py` no importa `httpx`, `sqlalchemy`, `slowapi` ni `jose` de forma directa, y ejecutar `pytest fastapi_bridge/tests/test_layer_boundaries.py` para verificar que las fronteras siguen verdes — confirmado, `test_layer_boundaries.py` sigue 100% verde

## 5. Delegación al Service y respuesta `202` (spec: "Una solicitud aceptada recibe una confirmación de aceptación")

- [x] 5.1 RED: escribir el test que, con `get_current_user` y `get_scan_service` sustituidos, hace `POST` con cuerpo válido y afirma status `202`; ejecutar y verificar que falla contra el cuerpo fijo de 4.2 — RED real: `get_scan_service` no existía aún, el test falla con `ImportError` (import local dentro del test, para no romper la recolección del resto del módulo)
- [x] 5.2 GREEN: agregar `get_scan_service() -> ScanService` a `core/dependencies.py` (D-3, un `return ScanService()` sin argumentos) y hacer que el handler reciba `service: ScanService = Depends(get_scan_service)`, llame `await service.start_scan(scan_request)` y devuelva `JSONResponse(status_code=202, content=response.model_dump(mode="json"))`; ejecutar y verificar que el test de 5.1 pasa — GREEN confirmado. **Efecto colateral encontrado y corregido**: el test de 4.3 (`test_scan_start_on_the_production_app_is_not_404`) dejó de ser inocuo al quedar el Service realmente wireado -- sin sustituir `get_scan_service` abría un canal real hacia `N8N_WEBHOOK_URL` (violando D-9). Se actualizó ese test para sustituir el Service también.
- [x] 5.3 TRIANGULATE: agregar el test que afirma que el cuerpo del `202` tiene **exactamente** las claves de `ScanResponse` (`scan_id`, `status`, `message`), con los mismos valores que devolvió el doble — ni una clave agregada, ni una renombrada; ejecutar y verificar que pasa
- [x] 5.4 TRIANGULATE: agregar el test que afirma que la `ScanRequest` que recibió el doble lleva exactamente los valores del cuerpo enviado, incluidos los valores por defecto de `sqlmap_level` / `sqlmap_risk` cuando se omiten; ejecutar y verificar que pasa (prueba que el Router no transforma la entrada)
- [x] 5.5 TRIANGULATE: agregar el test que afirma que el status **no** es `200` ni `201`, y que una solicitud produce exactamente **una** llamada a `start_scan`; ejecutar y verificar que pasa
- [x] 5.6 REFACTOR: confirmar por revisión que el cuerpo del handler no tiene ningún `if`, ningún `try`, ninguna referencia a `Settings`, `ScanUoW` ni `httpx`, y que `current_user` no participa en la construcción de la respuesta; ejecutar la suite del archivo y verificar que sigue verde — confirmado por lectura directa de `router.py`; `current_user` todavía no está en la firma (llega en 6.1), así que la condición se cumple trivialmente por ahora y se reconfirma en 6.x

## 6. Guard JWT (spec: "El disparo de escaneo está cerrado a quien no presenta credencial válida")

> En este grupo **no** se sustituye `get_current_user`: se ejercita el guard real (D-9).

- [x] 6.1 RED: escribir el test que hace `POST` con cuerpo válido y **sin** cabecera `Authorization`, afirmando status `401`; agregar `current_user: str = Depends(get_current_user)` a la firma del handler (D-6); ejecutar y verificar que pasa — RED confirmado (`202 == 401`), luego GREEN
- [x] 6.2 TRIANGULATE: agregar el test con un token expirado (emitido con expiración en el pasado) → `401`; ejecutar y verificar que pasa
- [x] 6.3 TRIANGULATE: agregar el test con un token malformado (texto que no es un JWT) → `401`; ejecutar y verificar que pasa
- [x] 6.4 TRIANGULATE: agregar el test con un token bien formado pero firmado con **otro** secreto → `401`; ejecutar y verificar que pasa
- [x] 6.5 TRIANGULATE: agregar el test que afirma que los cuerpos de `401` de 6.2 y 6.4 **no** permiten distinguir el motivo; ejecutar y verificar que pasa — ajustado tras un primer intento demasiado estricto: el `detail` es el mismo literal fijo para ambos casos (`_INVALID_TOKEN_DETAIL` de `core/dependencies.py`), que es justamente la garantía de no distinguibilidad; el test verifica igualdad de ambos `detail` en vez de prohibir la palabra "expiró" (que aparece en ese literal compartido, no en un mensaje exclusivo del caso expirado)
- [x] 6.6 TRIANGULATE: agregar el test que afirma que en todos los caminos de `401` el `FakeScanService` registró **cero** llamadas — el rechazo ocurre antes de cualquier lógica de negocio y antes de cualquier contacto con el orquestador; ejecutar y verificar que pasa
- [x] 6.7 TRIANGULATE: agregar el test de camino feliz con un token **válido** real (no sustituido), afirmando que la solicitud es atendida; ejecutar y verificar que pasa

## 7. Mapeo `N8nUnavailableError` → `502` (spec: "La indisponibilidad del orquestador se reporta como falla de la pasarela")

- [x] 7.1 RED: escribir el test que configura el `FakeScanService` para levantar `N8nUnavailableError` y afirma status `502`; ejecutar y verificar que falla (hoy sale como error no manejado) — RED confirmado: la excepción propagó sin manejar (traceback visible en pytest)
- [x] 7.2 GREEN: agregar `n8n_unavailable_handler` a `exceptions/handlers.py` construido con `problem_detail_response(...)` y sus literales como constantes de módulo (D-2), y registrarlo en `create_app()` con `add_exception_handler(N8nUnavailableError, ...)`; ejecutar y verificar que el test de 7.1 pasa
- [x] 7.3 TRIANGULATE: agregar el test que afirma que el cuerpo del `502` tiene exactamente las cinco claves RFC 7807, que `status` es `502`, que el `content-type` es `application/problem+json` y que `instance` es `/api/v1/scan/start`; ejecutar y verificar que pasa
- [x] 7.4 TRIANGULATE: agregar el test que afirma que el cuerpo del `502` **no** contiene la URL del orquestador, su token de webhook, trazas de pila, rutas de archivos del servidor ni nombres de módulos internos; ejecutar y verificar que pasa
- [x] 7.5 TRIANGULATE: agregar el test que afirma que el `phpsessid` enviado en la solicitud **no** aparece en el cuerpo de ninguna respuesta de error (`401`, `422`, `429`, `502`); ejecutar y verificar que pasa (R-6)
- [x] 7.6 REFACTOR: confirmar por revisión que el Router no contiene ningún `try`/`except` de `N8nUnavailableError` y que el handler nuevo construye su respuesta **sólo** vía `problem_detail_response(...)`, sin repetir literales de formato; ejecutar la suite del archivo y verificar que sigue verde — confirmado por lectura directa; `router.py` no menciona `N8nUnavailableError`

## 8. Validación del cuerpo (spec: "Toda respuesta de rechazo de la operación es Problem Details")

- [x] 8.1 RED/GREEN: escribir el test que, con credencial válida, envía un cuerpo sin `target_url` y afirma un rechazo de validación (`400` o `422`) con cuerpo RFC 7807 e `instance` igual a la ruta; ejecutar y verificar (lo satisface el handler de CHANGE-07, no código nuevo de este change — dejar constancia) — confirmado: pasó sin escribir código de producción nuevo
- [x] 8.2 TRIANGULATE: agregar los tests de los demás casos del contrato de CHANGE-08 — `target_url` que no es URL, `phpsessid` vacío o sólo espacios, `sqlmap_level` fuera de `1..5`, `sqlmap_risk` fuera de `1..3` — cada uno rechazado y con `FakeScanService` en cero llamadas; ejecutar y verificar que pasan — 7 casos parametrizados, todos verdes
- [x] 8.3 TRIANGULATE: agregar el test que afirma que un cuerpo válido **con un campo desconocido extra** es aceptado (`extra="ignore"`, CHANGE-08 D-7) y que ese campo no llega al Service — el borde no endurece ni relaja el contrato de datos; ejecutar y verificar que pasa
- [x] 8.4 Confirmar por revisión que `router.py` no contiene ninguna validación manual de campos (ni `if not scan_request.target_url`, ni chequeos de rango): toda la validación es de Pydantic — confirmado, `router.py` no tiene ningún `if`

## 9. Cupo por IP sobre la ruta real (spec: "El disparo de escaneo aplica el cupo por origen del dominio scan sobre la ruta real")

- [x] 9.1 RED: escribir el test que, tras `limiter.reset()` y con `limiter_module.get_settings` sustituido por un cupo chico (patrón de `test_rate_limit.py`), agota el cupo desde una IP y afirma que la solicitud siguiente es `429`; ejecutar y verificar que falla porque el endpoint no está decorado — RED confirmado (`202 == 429`)
- [x] 9.2 GREEN: aplicar `@scan_rate_limit` sobre el handler (importado de `core/limiter.py`, sin importar `slowapi` en `api/`), respetando el parámetro obligatorio `request: Request` de D-6; ejecutar y verificar que el test de 9.1 pasa
- [x] 9.3 TRIANGULATE: agregar el test que afirma que el cuerpo del `429` es RFC 7807 con las cinco claves y que la respuesta trae la cabecera `Retry-After` (lo produce el handler ya existente de CHANGE-00d); ejecutar y verificar que pasa
- [x] 9.4 TRIANGULATE: agregar el test que afirma que la solicitud rechazada por cupo **no** llegó al `FakeScanService` (cero llamadas adicionales); ejecutar y verificar que pasa
- [x] 9.5 TRIANGULATE: agregar el test que afirma que una IP distinta, sin historial, es atendida normalmente mientras la primera ya agotó su cupo; ejecutar y verificar que pasa
- [x] 9.6 TRIANGULATE: agregar el test que afirma que, con el cupo de scan agotado, `GET /health` sigue respondiendo `200` — el cupo no se derrama al resto del servicio; ejecutar y verificar que pasa
- [x] 9.7 TRIANGULATE: agregar el test estructural que afirma que la ruta de disparo de escaneo figura registrada en `limiter._dynamic_route_limits` del **singleton de módulo** (D-5), y que es la única ruta de producción registrada ahí; ejecutar y verificar que pasa
- [x] 9.8 Verificar que las fixtures de este grupo hacen `limiter.reset()` antes y después, y que dos ejecuciones consecutivas del mismo test pasan ambas (mismo control de aislamiento que `test_rate_limit.py`) — confirmado: `test_rate_limit_fixture_starts_fresh_each_time_{first,second}` ambos verdes

## 10. Documentación OpenAPI (spec: "La documentación interactiva declara la operación como protegida")

- [x] 10.1 RED/GREEN: escribir el test que obtiene `app.openapi()` y afirma que la operación `post` de `/api/v1/scan/start` declara un requisito `security` no vacío, y que el esquema referenciado es de tipo *bearer* (D-8); ejecutar y verificar (lo satisface el `OAuth2PasswordBearer` de CHANGE-06 — dejar constancia de que no se agregó `security` a mano) — confirmado: `operation["security"] == [{"OAuth2PasswordBearer": []}]`, sin código nuevo
- [x] 10.2 TRIANGULATE: agregar el test que afirma que la operación declara `202` entre sus respuestas y referencia el esquema de `ScanResponse`, y que su `requestBody` referencia el de `ScanRequest`; ejecutar y verificar que pasa
- [x] 10.3 TRIANGULATE: agregar el test que afirma que `GET /health` **no** declara requisito de seguridad — la protección es por operación, no global; ejecutar y verificar que pasa
- [x] 10.4 Verificar manualmente en `/docs` que la operación aparece con el candado y que el botón "Authorize" está disponible, y anotar el resultado (criterio de aceptación de `CHANGES.md` verificado en vivo) — **verificación parcial, con limitación anotada**: `GET /docs` responde `200` y sirve el shell de Swagger UI, pero ese HTML es una SPA que renderiza el candado/botón "Authorize" en el cliente a partir de `app.openapi()` vía JavaScript — no hay forma de verificar el DOM renderizado sin un navegador real, no disponible en este entorno de agente. Se verificó la evidencia determinista de la que depende ese renderizado: `operation["security"]` no vacío y `securitySchemes` con `OAuth2PasswordBearer` (10.1) — es exactamente el dato que Swagger UI usa para dibujar el candado y habilitar "Authorize". Recomendado: el usuario confirme visualmente en `/docs` al levantar el servicio (ver 12.5).

## 11. Reconciliación de los tests archivados que este change invalida (D-10)

- [x] 11.1 Actualizar `test_edge_policy_exclusions.py::test_domain_routers_still_return_404_on_production_app`: renombrarlo a lo que ahora afirma, dejar el `404` para `POST /api/v1/auth/register` y afirmar que `POST /api/v1/scan/start` ya **no** es `404`; ejecutar el archivo completo y verificar que queda verde — hecho. **Nota**: el test real en este árbol ya se llamaba `test_scan_router_still_returns_404_on_production_app_auth_does_not` (no el nombre literal del texto de la tarea, heredado de un rename previo de CHANGE-05/07); se renombró a `test_scan_and_auth_routers_are_both_mounted_on_the_production_app` y se invirtió su aserto de scan. Archivo completo: verde (6/6).
- [x] 11.2 Reescribir `test_app_wiring.py::test_no_production_route_has_a_rate_limit_applied` para que inspeccione el singleton `core.limiter.limiter` en vez de `app.state.limiter`, afirmando que la **única** ruta de producción marcada es el disparo de escaneo (D-5, R-2); ejecutar el archivo y verificar que queda verde — hecho, renombrado a `test_scan_start_is_the_only_production_route_with_a_rate_limit_applied`. **Ajuste no anticipado**: una comparación estricta contra el conjunto completo de `_dynamic_route_limits` es frágil entre módulos de test (el registro no se limpia con `limiter.reset()`, y otros archivos —p. ej. `test_edge_policy_exclusions.py`— montan rutas desechables propias con `scan_rate_limit` que dejan su clave en el mismo singleton durante toda la sesión de pytest, hallazgo ya documentado en `test_rate_limit.py`). Se acotó el aserto a las claves bajo `fastapi_bridge.api.*` (rutas de producción), que es lo que R-2/D-5 necesitan.
- [x] 11.3 Verificar que ningún otro test archivado depende de que el router de scan no esté montado — buscar `scan/start` y `404` en `fastapi_bridge/tests/` y revisar cada hit; reportar cualquier hallazgo no previsto en vez de arreglarlo en silencio — **HALLAZGO NO PREVISTO, reportado sin arreglar (ver resumen final del apply)**: además de los dos tests que `design.md` D-10 declaraba como "las dos únicas modificaciones", montar el router rompió **cinco tests más** en **cuatro archivos** fuera del scope de archivos autorizado para este change: `test_app_wiring.py::test_route_surface_includes_health_and_auth_but_not_scan` y `::test_still_unmounted_scan_route_returns_404_in_rfc7807_format` (mismo archivo, pero funciones no declaradas); `test_health.py::test_auth_router_is_mounted_but_scan_router_is_not_yet`; `test_auth_router.py::test_application_route_surface_is_exactly_health_and_auth`; `test_auth_dependencies.py::test_declaring_the_schema_alone_protects_nothing`. Los últimos tres viven en archivos que el brief de este apply NO autoriza tocar. **Actualización**: el usuario autorizó explícitamente ampliar el scope; los cinco tests fueron reconciliados con el mismo patrón rename+assert-flip de 11.1/11.2 (`test_route_surface_includes_health_auth_and_scan`, `test_scan_route_without_credentials_returns_401_in_rfc7807_format`, `test_auth_and_scan_routers_are_both_mounted`, `test_application_route_surface_is_exactly_health_auth_and_scan`, `test_only_the_scan_start_operation_declares_security`). Suite completa: 618 passed, 0 failed.

## 12. Verificación final

- [x] 12.1 Ejecutar `pytest` completo y verificar que el total es el baseline de 2.1 más los tests nuevos de este change, con `0 failed`; anotar el conteo — **618 passed, 0 failed, 0 errors, 1 warning**, corrido con `fastapi_bridge/.venv/Scripts/python.exe -m pytest` (el intérprete real del proyecto — ver nota de entorno abajo). El único warning es `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead`, disparado una sola vez al importar `fastapi.testclient` (versión de `fastapi`/`starlette` de este venv) — preexistente y ajeno a CHANGE-12: se dispara igual desde `test_exception_handlers.py` (CHANGE-07, no tocado por este change), no depende de nada que este change haya escrito. **Nota de entorno (hallazgo post-corrección)**: la primera corrida de esta verificación usó por error el Python global de `miniconda3.1` en vez de `fastapi_bridge/.venv`, el venv real del proyecto (evidenciado por un proceso `uvicorn --reload` corriendo desde `fastapi_bridge/.venv` en segundo plano). Corriendo con el venv real dio `1 failed, 537 passed, 1 warning, 80 errors` — los 81 (80 errors + el failed) eran el mismo `ModuleNotFoundError: No module named 'aiosqlite'` (declarado en `requirements-dev.txt` línea 9 pero nunca instalado en ese venv). Se corrigió con `fastapi_bridge/.venv/Scripts/python.exe -m pip install -r fastapi_bridge/requirements-dev.txt` (sincroniza el venv con lo ya declarado, no agrega ninguna dependencia nueva) y la corrida completa quedó en `618 passed, 0 failed, 1 warning`. No es un defecto de CHANGE-12: afecta por igual a `test_user_repository.py`/`test_auth_uow.py`/`test_user_session_fixture.py` (CHANGE-01/03/04, no tocados por este change).
- [x] 12.2 Recorrer los siete criterios de aceptación de `CHANGE-12` en `CHANGES.md` y marcar cada uno contra el test concreto que lo cubre (o contra la verificación en vivo de 10.4); reportar cualquiera que quede sin cubrir — los siete están cubiertos: (1) `202`+body → 5.1/5.3; (2) sin JWT `401` → 6.1; (3) JWT expirado `401` → 6.2; (4) body inválido `400/422` → 8.1/8.2; (5) rate limit `429` → 9.1/9.3; (6) n8n caído `502` → 7.1/7.3; (7) Swagger muestra el lock → cubierto por evidencia determinista (10.1: `security` no vacío + `securitySchemes.OAuth2PasswordBearer`), **con la limitación anotada en 10.4**: el renderizado del candado en el DOM de `/docs` no se pudo verificar visualmente en este entorno (sin navegador) — ver 12.5 para la verificación pendiente al usuario
- [x] 12.3 Ejecutar `openspec validate --strict` sobre el change y verificar que no reporta errores — pasa limpio. Se corrigieron en el camino DOS errores de esquema: (1) `specs/api-edge-security/spec.md` — el requirement "La política de borde no altera la superficie de API" no puede reescribirse dentro de un `MODIFIED` porque el validador exige conservar el nombre de todos los escenarios existentes incluido el obsoleto "El router de scan sigue sin montarse"; resuelto con REMOVED+ADDED-con-título-distinto ("...ya implementada"). (2) `specs/bridge-bootstrap/spec.md` — **hallazgo de la auditoría adversarial post-apply**: el delta original apuntaba su `REMOVED` a un requirement ("Superficie de API acotada al scaffold") que no existe en `openspec/specs/bridge-bootstrap/spec.md` (CHANGE-05 ya lo había renombrado a "Superficie de API expuesta por el servicio" al archivarse) y su `ADDED` afirmaba falsamente que "el dominio auth sigue sin montarse" — contradicho por `main.py`, que monta `auth_router` desde CHANGE-05. Reescrito con el `REMOVED` apuntando al nombre real vigente y el `ADDED` ("Superficie de API expuesta por el servicio con scan montado") reflejando que auth Y scan están montados. `openspec validate --all --strict` corrido también sobre el resto del repo: 13/19 specs pasan; los 6 que fallan (`access-token`, `auth-contracts`, `auth-session`, `password-hashing`, `user-persistence`, `user-registry`) son preexistentes del dominio Auth (CHANGE-02/03/04, confirmado con `git log`), no tocados por este change y fuera de su scope — no se corrigen acá.
- [x] 12.4 Confirmar que los archivos de producción tocados son exactamente los cuatro declarados en el encabezado de este archivo (`git status` / `git diff --stat`), y reportar cualquier archivo extra con su justificación — confirmado, exactamente los 4 (`router.py`, `dependencies.py`, `handlers.py`, `main.py`). Archivos extra, todos justificados: `test_scan_router.py` (nuevo, declarado), `test_edge_policy_exclusions.py`/`test_app_wiring.py` (reconciliación declarada en D-10), y — por autorización explícita del usuario tras el hallazgo de 11.3 — `test_health.py`, `test_auth_router.py`, `test_auth_dependencies.py` (reconciliación no anticipada por D-10) más el spec delta `specs/api-edge-security/spec.md` (fix de validación)
- [x] 12.5 Levantar el servicio (`uvicorn fastapi_bridge.main:app`) y verificar en vivo el camino completo con un JWT real: `202` con n8n disponible y `502` con n8n apagado; anotar ambos resultados — **ejecutado en vivo** (`fastapi_bridge/.env` sí existe en este entorno; la pregunta de credenciales ya estaba resuelta en `knowledge-base/10_preguntas_abiertas.md` §Preguntas resueltas, la corrida anterior no lo había detectado). `uvicorn fastapi_bridge.main:app` levantó contra la PostgreSQL `db_fuzzing` real (`GET /health` → `200`). Flujo con JWT real: `POST /api/v1/auth/register` (`201`) → `POST /api/v1/auth/login` (`200`, `access_token` real) → `POST /api/v1/scan/start` con ese JWT y body válido → **`502`** RFC 7807 (`type: n8n-unavailable`), porque el webhook `http://localhost:5678/webhook/wasa-scan` de la instancia n8n local responde `404` a la ruta configurada (n8n está arriba — responde `200` en su raíz — pero el workflow de disparo todavía no existe: **CHANGE-21 `n8n-webhook-trigger`, que lo crea, depende de este mismo CHANGE-12 y todavía no se implementó**, según `CHANGES.md`). Es el comportamiento correcto y esperado de `N8nRepository`/`N8nUnavailableError` ante un orquestador sin workflow activo — confirma en vivo el caso "n8n no disponible → 502". El caso "n8n disponible → 202" **no se pudo demostrar en vivo** en este entorno porque requiere el workflow real de CHANGE-21 (todavía no construido); queda pendiente para cuando ese change esté implementado. Servidor de prueba detenido al finalizar; se registró un usuario de prueba desechable (`c12-live-check-*@example.com`) en la tabla `users` de `db_fuzzing` para obtener el JWT — no se tocó `scans`/`vulnerabilities`.
