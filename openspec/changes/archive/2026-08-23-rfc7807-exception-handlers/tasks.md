> **Governance: MEDIO** (`CHANGES.md` y `CLAUDE.md` coinciden: este change no es del dominio Auth crítico, es infraestructura de errores). Se implementa en pasos, surfaceando las decisiones no obvias. El **checkpoint** (1.5) presenta al usuario las tres decisiones **⚠ REVISIÓN** de `design.md` (D-1, D-2, D-6) antes de escribir código de producción.
>
> **Strict TDD**: cada tarea de implementación arranca por el test (RED), sigue con el mínimo código (GREEN), triangula con casos adicionales y refactoriza. Los grupos van de la infraestructura compartida hacia arriba: constructor del cuerpo (2), validación (3), excepciones HTTP (4), errores de dominio (5), último recurso (6), cableado (7), y recién al final las anclas estructurales y de regresión (8).
>
> **Comando de la suite**: `fastapi_bridge/.venv/Scripts/python.exe -m pytest fastapi_bridge/tests/ -q`

## 1. Red de seguridad y checkpoint de governance

- [x] 1.1 Correr la suite completa y registrar el baseline (`N passed`). Si algo ya falla, reportarlo como **falla preexistente** y NO arreglarlo dentro de este change. Anotar el número acá — es la evidencia de que este change no rompió nada.
      **Baseline: 295 passed, 0 failed** (`fastapi_bridge/.venv/Scripts/python.exe -m pytest fastapi_bridge/tests/ -q`). Ninguna falla preexistente.
- [x] 1.2 Releer `fastapi_bridge/exceptions/handlers.py` completo y confirmar los tres puntos de los que depende todo el plan: que `problem_detail_response(...)` existe con la firma documentada, que las constantes `PROBLEM_DETAIL_MEDIA_TYPE` / `DEFAULT_PROBLEM_TYPE` / `DEFAULT_PROBLEM_TITLE` están declaradas, y que `rate_limit_exceeded_handler` setea `Retry-After` sobre la respuesta ya construida (el patrón que D-3 generaliza).
      Confirmado, los tres puntos existían tal como el diseño los describe.
- [x] 1.3 Releer `fastapi_bridge/exceptions/domain.py` y confirmar que `DomainError` es la base común, que `EmailAlreadyExistsError` expone `.email` y que `InvalidCredentialsError` **no** expone el email. Confirmar además que el módulo no importa nada (D-4 se apoya en esa pureza).
      Confirmado: `DomainError(Exception)`, `EmailAlreadyExistsError.email`, `InvalidCredentialsError` sin atributo `email`, módulo sin imports.
- [x] 1.4 Reproducir los seis hallazgos de `design.md` §Context (H-1 a H-6) en un script descartable contra el venv, y anotar el resultado observado de cada uno. **No** son supuestos: H-1 (la contraseña en `input`) justifica D-7, H-2 (404/405 escapan) justifica D-3, H-6 (`raise_server_exceptions`) determina cómo se escriben los tests del grupo 6. Si alguno **no** se reproduce en la versión instalada, el diseño correspondiente hay que revisarlo antes de seguir.
      **Los seis hallazgos se reprodujeron exactamente como en `design.md`** (fastapi 0.141.1, starlette 1.6.0, pydantic 2.13.4): H-1 (`input: "short"` en `exc.errors()`), H-2 (404/405 con `{"detail":"Not Found"}` fuera de RFC 7807), H-3 (`WWW-Authenticate` presente en `exc.headers` del handler por defecto de FastAPI), H-4 (422 con CORS, 500 sin CORS), H-5 (`json_invalid` con `loc: ["body", 1]`), H-6 (`TestClient` por defecto re-lanza; `raise_server_exceptions=False` devuelve el 500). Ningún diseño requirió revisión.
- [x] 1.5 **Checkpoint con el usuario (governance MEDIO)**: presentar las tres decisiones ⚠ REVISIÓN con recomendación y alternativa. **D-1**: enrutar el cuerpo por `ErrorDetail` y relajar `detail` a `str | None` (alternativa: dejar el `dict` literal y no tocar `error_schemas.py` — implica revertir el delta de `error-contract`). **D-2**: 422 para violación de schema / 400 para cuerpo no parseable, **y corregir a 422 el criterio de aceptación de CHANGE-05 que hoy dice 400** (alternativas: todo a 400, o todo a 422). **D-6**: aceptar y documentar que el 500 no lleva headers CORS (alternativa: middleware propio por dentro de CORS). Registrar la resolución acá antes de abrir el grupo 2.
      **Resuelto: las tres recomendaciones de `design.md` aceptadas tal cual, sin alternativas.** D-1: `problem_detail_response` enruta por `ErrorDetail`, `detail` relajado a `str | None`. D-2: 422 para violación de schema / 400 para `json_invalid`; `CHANGES.md` línea 523 corregida (tarea 9.4). D-6: se acepta y documenta que el 500 no lleva CORS (ancla en `test_cors.py`, tarea 8.3); no se construye middleware propio.

## 2. `problem_detail_response` pasa a construir por `ErrorDetail` (D-1)

> Si el usuario eligió la alternativa (a) de D-1 en 1.5, **saltear el grupo entero**, revertir `specs/error-contract/spec.md` y seguir en el grupo 3: los cuatro handlers funcionan igual con el `dict` literal.

- [x] 2.1 RED — en `fastapi_bridge/tests/test_error_schemas.py`, agregar el test de que `ErrorDetail` se construye **sin** pasar `detail` y el campo queda en `None`. Hoy falla: `detail: str` es obligatorio. Es el escenario "Detalle ausente" del delta.
- [x] 2.2 GREEN — cambiar `ErrorDetail.detail` a `str | None = None` en `fastapi_bridge/schemas/error_schemas.py`, y actualizar el docstring del campo explicando que RFC 7807 declara los cinco miembros opcionales.
- [x] 2.3 TRIANGULATE — verificar que los tests existentes de `test_error_schemas.py` que pasan un `detail` explícito siguen en verde, y que los cinco campos siguen siendo exactamente los mismos (`ErrorDetail.model_fields`). Relajar un tipo no debe agregar ni quitar miembros.
- [x] 2.4 RED — agregar a `tests/test_problem_details.py` el test del escenario "El cuerpo emitido se construye a través del modelo": afirmar que `problem_detail_response(status_code=700, instance="/x")` **falla** con un error de validación de Pydantic. Hoy pasa sin chistar y emite un 700 al cliente — ese es el RED.
- [x] 2.5 GREEN — reescribir el cuerpo de `problem_detail_response` para construir `ErrorDetail(type=..., title=..., status=..., detail=..., instance=...)` y volcarlo con `model_dump()`. **No** cambiar la firma ni el valor de retorno: sigue devolviendo un `JSONResponse` con `media_type=PROBLEM_DETAIL_MEDIA_TYPE`.
- [x] 2.6 TRIANGULATE — confirmar que **los siete tests preexistentes** de `test_problem_details.py` siguen verdes sin tocarlos, incluido `test_problem_detail_response_allows_missing_detail` (que exige `"detail": null` en el cuerpo) y los tres de `rate_limit_exceeded_handler`. Si alguno se pone rojo, el enrutado cambió comportamiento observable y hay que corregir el enrutado, no el test.
- [x] 2.7 REFACTOR — actualizar el docstring de `problem_detail_response` para decir que el cuerpo se construye a través de `ErrorDetail` y por qué (una sola declaración de la forma; las restricciones del modelo alcanzan a lo que se emite).

## 3. Handler de validación — 422 / 400 y la fuga de la contraseña (D-2, D-7)

- [x] 3.1 RED — crear `fastapi_bridge/tests/test_exception_handlers.py` con la app de prueba de D-9: una `FastAPI` propia con `TestClient(app, raise_server_exceptions=False)` y una ruta `POST /validate` que recibe un modelo Pydantic con `email: str` y `password: str = Field(min_length=8)`. Registrar sobre ella `request_validation_exception_handler` (todavía inexistente: el `ImportError` es el RED). **Comentar en el archivo por qué `raise_server_exceptions=False`** (H-6): con el default, el `TestClient` re-lanza y el test verificaría el `TestClient`, no el handler.
- [x] 3.2 GREEN — implementar `request_validation_exception_handler(request, exc) -> JSONResponse` en `handlers.py`, devolviendo 422 vía `problem_detail_response` con las constantes de tipo/título de validación (D-8). Mínimo: todavía sin componer el `detail` a partir de los errores.
- [x] 3.3 RED — **el test más importante del change** (D-7): postear `{"email":"a@b.com","password":"corta"}` con un valor de contraseña reconocible y exigir que ese string exacto **no** aparezca en `response.text`. Comentar en el test por qué existe: H-1 muestra que `exc.errors()` trae la contraseña en `input`, y serializar `exc.errors()` entero es el patrón que aparece en la mayoría de los ejemplos de FastAPI.
- [x] 3.4 RED — el `detail` debe **nombrar el campo** que falló: postear con `password` corta y exigir que `body["detail"]` contenga `"password"`. El RED es el `detail` genérico que dejó 3.2.
- [x] 3.5 GREEN — implementar el compositor del `detail` como **función pura de módulo** (`_format_validation_detail(errors: list[dict]) -> str`), separada del handler: recibe la lista de errores y devuelve el string. Usar **solo** `loc` y `msg` (allowlist explícita, D-7); descartar el primer elemento de `loc` cuando es `"body"` / `"query"` / `"path"` / `"header"`; unir el resto con `"."`; formato `"campo: msg"`, varios unidos por `"; "`.
- [x] 3.6 TRIANGULATE — probar `_format_validation_detail` directamente como función pura, sin HTTP: un solo error, dos errores (los dos aparecen), un error cuyo `loc` es `["body"]` a secas, y un error con `loc` anidado (`["body","user","email"]` → `"user.email"`).
- [x] 3.7 RED — caso `json_invalid` (H-5): postear `content=b"{not json"` con `Content-Type: application/json`. Exigir status **400**, no 422 (D-2). El RED es el 422 que devuelve la implementación actual.
- [x] 3.8 GREEN — agregar el discriminante: si algún error tiene `type == "json_invalid"`, el estado es 400 con las constantes de "malformed request"; si no, 422. Declarar el literal `"json_invalid"` como constante de módulo, no en línea.
- [x] 3.9 TRIANGULATE — el `loc` de `json_invalid` es `["body", 1]`, un **offset de caracter**, no un nombre de campo (H-5). Exigir que el `detail` del 400 no contenga `"body.1"` ni `".1"`: cuando tras descartar la ubicación no queda ningún elemento de tipo `str`, el error se describe solo con su `msg`. Ajustar `_format_validation_detail` si hace falta.
- [x] 3.10 TRIANGULATE — resto de los casos de validación, todos 422: campo obligatorio faltante; tipo incorrecto (`password` numérico); **dos** campos inválidos a la vez, exigiendo que ambos nombres aparezcan en el `detail`. Verificar en todos: `Content-Type: application/problem+json`, las cinco claves exactas, `status` del cuerpo igual al de la respuesta, e `instance == "/validate"`.
- [x] 3.11 TRIANGULATE — la ausencia de `ctx`: postear con `password` corta y exigir que el cuerpo no contenga `"min_length"` ni el valor `8` como contexto de la restricción (escenario "El contexto interno de la restricción no se expone").
- [x] 3.12 REFACTOR — docstring del bloque de validación: por qué el estado no es fijo (D-2), por qué la composición es allowlist y no denylist (D-7, con la referencia explícita a H-1), y por qué el compositor es una función pura separada (testeable sin HTTP).

## 4. Handler de excepciones HTTP — la clase base y los headers (D-3)

- [x] 4.1 RED — agregar a la app de prueba una ruta `GET /ok` (que devuelve 200) y una `GET /challenge` que lanza `HTTPException(401, detail="No autenticado", headers={"WWW-Authenticate": "Bearer"})`. Registrar `http_exception_handler` (inexistente: RED). Primer test: `GET /challenge` devuelve 401 con cuerpo RFC 7807 y `detail == "No autenticado"`.
- [x] 4.2 GREEN — implementar `http_exception_handler(request, exc) -> JSONResponse` usando `problem_detail_response` con `status_code=exc.status_code`, `detail=str(exc.detail)`, `instance=request.url.path`, sin pasar `type_` (hereda `about:blank`, D-8) y con `title` derivado de `http.HTTPStatus(exc.status_code).phrase`, cayendo a `DEFAULT_PROBLEM_TITLE` si el código no es estándar.
- [x] 4.3 RED — **404 y 405** (H-2): `GET /ruta-inexistente` y `POST /ok` deben devolver cuerpo RFC 7807. Registrar el handler sobre `fastapi.HTTPException` primero, ver el test **rojo** (`{"detail":"Not Found"}` escapa), y recién entonces cambiar a `starlette.exceptions.HTTPException`. Este RED intencional es lo que documenta la trampa; comentarlo en el test.
- [x] 4.4 GREEN — registrar sobre `starlette.exceptions.HTTPException` y confirmar que 404 y 405 pasan por el handler.
- [x] 4.5 RED — headers preservados (H-3): exigir que la respuesta de `GET /challenge` lleve `WWW-Authenticate: Bearer`. El RED es su ausencia — el handler de 4.2 no copia `exc.headers`.
- [x] 4.6 GREEN — volcar `exc.headers` (si no es `None`) sobre la respuesta ya construida, **excluyendo cualquier header que sobrescriba `Content-Type`** (D-3). Mismo patrón que `rate_limit_exceeded_handler` usa para `Retry-After`.
- [x] 4.7 TRIANGULATE — `Content-Type` protegido: agregar una ruta que lance `HTTPException(400, headers={"Content-Type": "text/plain"})` y exigir que la respuesta siga siendo `application/problem+json`.
- [x] 4.8 TRIANGULATE — excepción HTTP **sin** headers: una ruta que lanza `HTTPException(403)` sin `headers` no debe romper el handler (`exc.headers is None`), y su `title` debe ser la frase de estado `"Forbidden"`.
- [x] 4.9 TRIANGULATE — `title` derivado de la frase de estado para tres códigos distintos (404 → `"Not Found"`, 405 → `"Method Not Allowed"`, 403 → `"Forbidden"`), y `type == "about:blank"` en los tres (escenario "Excepción HTTP genérica usa el tipo por defecto").
- [x] 4.10 REFACTOR — docstring del handler HTTP: por qué se registra sobre la clase de Starlette y no sobre la de FastAPI (con el caso 404/405 nombrado, para que nadie lo "corrija"), y por qué los headers se vuelcan salvo `Content-Type`.

## 5. Handler de errores de dominio — un handler, una tabla (D-4)

- [x] 5.1 RED — agregar a la app de prueba `GET /conflict` (lanza `EmailAlreadyExistsError("dup@b.com")`) y `GET /invalid` (lanza `InvalidCredentialsError()`). Registrar `domain_error_handler` sobre `DomainError` (inexistente: RED). Tests: `/conflict` → 409, `/invalid` → 401, ambos con cuerpo RFC 7807.
- [x] 5.2 GREEN — declarar `_DOMAIN_ERROR_MAP` en `handlers.py` (tipo explícito: `dict[type[DomainError], tuple[int, str, str]]` o un `NamedTuple` `ProblemSpec` si mejora la legibilidad), con las dos filas y sus constantes de tipo/título (D-8). Implementar `domain_error_handler(request, exc)` resolviendo por `type(exc)` en la tabla.
- [x] 5.3 RED — el `detail` del 409 debe identificar el email en conflicto: exigir que `body["detail"]` contenga `"dup@b.com"`. Verificar además, por inspección del código, que el handler **no** consulta la base — el dato viene de `exc.email`.
- [x] 5.4 GREEN — componer el `detail` del 409 desde `exc.email`. La composición aplica **solo** a esa fila: la tabla debe permitir filas con `detail` fijo y filas con `detail` compuesto sin volverse un árbol de `if`. Una función por fila, o un callable opcional en la tupla — elegir lo más simple que no ramifique en el handler.
- [x] 5.5 RED — **el ancla anti-enumeración** (D-4): exigir que el cuerpo del 401 de `/invalid` **no** contenga ningún email, y que su `detail` sea un literal fijo. Comentar por qué: §Excepciones globales prohíbe distinguir email inexistente de contraseña incorrecta.
- [x] 5.6 TRIANGULATE — dos instancias de `InvalidCredentialsError` levantadas desde solicitudes con emails distintos producen cuerpos **idénticos** salvo `instance` (escenario "Email inexistente y contraseña incorrecta son indistinguibles").
- [x] 5.7 RED — `DomainError` sin mapear: definir en el test una subclase local `class _UnmappedDomainError(DomainError)`, lanzarla desde una ruta y exigir **500** con el `detail` genérico (no un 400 inventado). El RED es el `KeyError` que produce el lookup directo del 5.2.
- [x] 5.8 GREEN — resolver el lookup con caída al 500 genérico, reutilizando las mismas constantes que el handler del grupo 6 (una sola declaración del `detail` genérico en el módulo, no dos).
- [x] 5.9 TRIANGULATE — resolución por herencia: verificar que el handler resuelve correctamente cuando la excepción lanzada es de una subclase de una clase mapeada (que la búsqueda no sea un `==` de tipo exacto que falle ante la primera especialización futura). Si se elige lookup por MRO, testearlo explícitamente.
- [x] 5.10 TRIANGULATE — `type` distinto por error: exigir que el `type` del 409, el del 401, el del 422 y el del 500 sean los cuatro distintos entre sí (escenario "Tipos distintos para clases de error distintas").
- [x] 5.11 REFACTOR — docstring del bloque de dominio: por qué el handler va sobre la base y no sobre cada subclase (traspaso de CHANGE-03), por qué la tabla vive en la capa web y no como atributo de la excepción (D-4), y por qué un error sin mapear cae a 500 y no a 400.

## 6. Handler de último recurso — 500 opaco, log completo (D-5)

- [x] 6.1 RED — agregar a la app de prueba `GET /boom`, que lanza `RuntimeError("MARCADOR-INTERNO-XYZ users.email_key SELECT * FROM users")`. Registrar `unhandled_exception_handler` sobre `Exception` (inexistente: RED). Test: la respuesta es 500 con cuerpo RFC 7807.
- [x] 6.2 GREEN — implementar `unhandled_exception_handler(request, exc)` devolviendo 500 vía `problem_detail_response` con `INTERNAL_ERROR_PROBLEM_DETAIL` como **literal de módulo** (no f-string, D-5), sin inspeccionar `exc`.
- [x] 6.3 RED — no filtra internals: exigir que `"MARCADOR-INTERNO-XYZ"`, `"users.email_key"`, `"SELECT"` y `"RuntimeError"` **no** aparezcan en `response.text`. Los cuatro cubren los escenarios de mensaje original, SQL, constraint y nombre de clase.
- [x] 6.4 TRIANGULATE — dos excepciones de tipos y mensajes distintos (`RuntimeError` y `ValueError`) desde el **mismo** path producen cuerpos byte a byte idénticos (escenario "El detalle es idéntico para causas distintas"); desde paths distintos, difieren **solo** en `instance`.
- [x] 6.5 RED — el stack trace llega al log: capturar con `caplog` a nivel `ERROR` y exigir que el registro contenga la información de la excepción original (`record.exc_info is not None` y el marcador en el texto formateado). El RED es la ausencia de logging.
- [x] 6.6 GREEN — agregar `logger = logging.getLogger(__name__)` a nivel de módulo y llamar `logger.exception(...)` **antes** de construir la respuesta. No configurar `basicConfig`, handlers ni formato (D-5).
- [x] 6.7 TRIANGULATE — el log no lleva material sensible: verificar por inspección del código (AST o lectura) que la llamada al logger recibe la excepción y el path, y no `JWT_SECRET`, contraseñas ni tokens. Ancla del escenario "El registro no incluye material sensible del servicio".
- [x] 6.8 TRIANGULATE — el handler no ejecuta lógica que pueda fallar: test estructural sobre el cuerpo de `unhandled_exception_handler` (vía `ast`, reutilizando el helper de `test_layer_boundaries.py`) exigiendo que no haya `if` sobre la excepción, ni llamadas a `get_settings`, ni acceso a la base. Ancla del escenario homónimo.
- [x] 6.9 REFACTOR — docstring del handler genérico: por qué el `detail` es un literal (los tres ejemplos concretos de fuga: SQLAlchemy trae SQL y constraint, asyncpg trae host y base, el nombre de la clase revela el motor), y por qué es el último recurso y no puede permitirse fallar.

## 7. Cableado en `main.py` (D-10)

- [x] 7.1 RED — agregar a `fastapi_bridge/tests/test_app_wiring.py` los tests de registro: `RequestValidationError`, `starlette.exceptions.HTTPException`, `DomainError` y `Exception` están en `create_app().exception_handlers`. `RateLimitExceeded` ya tiene su test y debe seguir verde.
- [x] 7.2 GREEN — registrar los cuatro handlers en `create_app()` de `fastapi_bridge/main.py` con `app.add_exception_handler`, en el orden de D-10 (del más específico al más general), junto al de `RateLimitExceeded` que ya está. Importar `starlette.exceptions.HTTPException` con alias explícito (`as StarletteHTTPException`) para que la elección de clase sea visible en el import.
- [x] 7.3 TRIANGULATE — independencia del estado del módulo: construir **dos** apps con `create_app()` y verificar que ambas quedan con el conjunto completo (escenario "El registro no depende del estado del módulo").
- [x] 7.4 TRIANGULATE — la superficie de rutas no cambia: `GET /health` sigue devolviendo `200` con `{"status":"ok","service":"wasa-fastapi-bridge"}`, y `POST /api/v1/auth/register` sigue devolviendo **404** — pero ahora ese 404 sale en formato RFC 7807. Es el test que demuestra los dos requirements a la vez.
- [x] 7.5 REFACTOR — actualizar el docstring de módulo de `main.py`: `create_app()` ahora arma CORS + rate limiting **+ la política de errores**, y esta última es la que hace cumplir la regla dura de que ningún error sale fuera de RFC 7807.

## 8. Anclas de regresión, precedencia y CORS

- [x] 8.1 RED/GREEN — **cobertura completa de la tabla de dominio** (D-4): test estructural que recorre recursivamente `DomainError.__subclasses__()` sobre el código de producción y exige que cada subclase concreta tenga entrada en `_DOMAIN_ERROR_MAP`. Cuidado: debe recorrer solo las clases de `fastapi_bridge.exceptions.domain`, no las que definan los propios tests (5.7 define una a propósito) — filtrar por `__module__`. Ancla del escenario "Toda subclase concreta de error de dominio está mapeada".
- [x] 8.2 TRIANGULATE — **precedencia del 429** (D-10): con los cinco handlers registrados, verificar que `RateLimitExceeded` sigue resolviéndose a su handler propio y no al de `Exception`. Verificarlo sobre el mecanismo de resolución, no solo afirmando que la clave está en el dict — el riesgo real es que una versión futura de slowapi cambie la herencia y el 429 pierda su `Retry-After` en silencio.
- [x] 8.3 RED/GREEN — **CORS sobre los errores** (D-6, H-4): sobre una app de prueba **con** `CORSMiddleware` y un origen permitido, verificar que las respuestas 422, 401 y 404 llevan `Access-Control-Allow-Origin` y que la respuesta **500 no lo lleva**. El test fija el comportamiento real, no el deseado; comentarlo explícitamente para que nadie lo lea como un bug abierto. Si en 1.5 el usuario eligió la alternativa (middleware propio), este test se invierte y hay que agregar el grupo de implementación correspondiente.
- [x] 8.4 TRIANGULATE — **la cadena completa sobre la app de prueba**: una tabla parametrizada que recorre los siete estados de RN-WS-09 producibles hoy (400, 401, 404, 405, 409, 422, 500) y, para cada uno, verifica las cinco claves exactas, `Content-Type: application/problem+json`, `body["status"] == response.status_code` e `instance == path`. Es el ancla del requirement "Ninguna respuesta de error escapa al formato RFC 7807" en una sola tabla legible.
- [x] 8.5 TRIANGULATE — `instance` sin cadena de consulta: solicitar una ruta que falla con `?token=abc123` y exigir que `body["instance"]` sea solo el path, sin `token` ni `abc123` (escenario "La cadena de consulta no llega al cuerpo del error"). Doble función: contrato y anti-fuga.
- [x] 8.6 Verificar las fronteras de capa: `handlers.py` puede importar de `exceptions/domain.py` y de `schemas/`, y `error_schemas.py` sigue sin importar `fastapi` (regla ya en `LAYER_IMPORT_RULES`). Evaluar si agregar la fila inversa —que `exceptions/domain.py` no importe `starlette` ni `fastapi`— para anclar D-4 estructuralmente; si `LAYER_IMPORT_RULES` acepta la ruta `exceptions`, es una línea.

## 9. Cierre

- [x] 9.1 Correr la suite completa y comparar contra el baseline de 1.1: todos los tests preexistentes siguen verdes, más los nuevos. Ninguna falla preexistente arreglada ni introducida.
- [x] 9.2 Revisar los docstrings de `handlers.py` que quedaron desactualizados: el docstring de módulo dice "CHANGE-07 reutiliza este helper para los handlers restantes … debe **extender** este módulo" — reescribirlo en pasado, describiendo el módulo tal como quedó (constructor + cinco handlers + tabla de dominio) y dejando anotado el punto de extensión que hereda CHANGE-11 (una fila en `_DOMAIN_ERROR_MAP`).
- [x] 9.3 Revisar los docstrings que otros módulos escribieron **sobre** este change y que ahora son pasado: `schemas/error_schemas.py` ("la emisión … es responsabilidad de CHANGE-07"), `exceptions/domain.py` ("mapeado a 409 Conflict por CHANGE-07", "CHANGE-07 puede registrar un único `exception_handler`"). Actualizarlos a presente sin cambiar comportamiento.
- [x] 9.4 Si en 1.5 se aceptó la recomendación de D-2: **editar `CHANGES.md`** para que el criterio de aceptación de CHANGE-05 diga `422` en vez de `400` para `password < 8 chars`, y anotar por qué. Es una línea, y no hacerla deja una contradicción que CHANGE-05 va a encontrar de frente.
- [x] 9.5 Anotar en `design.md` §Open Questions la resolución de las tres preguntas abiertas (identificador de correlación, criterio de CHANGE-05, prefijo de URIs de tipo), aunque la resolución sea "se difiere".
- [x] 9.6 Guardar en engram (`topic_key: opsx/rfc7807-exception-handlers/apply`) las decisiones del checkpoint 1.5 tal como se resolvieron, los hallazgos H-1 a H-6 confirmados, y el contrato que heredan CHANGE-05, CHANGE-06 y CHANGE-11.
