> **Governance: MEDIUM** (override del proyecto para el dominio Auth, CHANGE-01..07 — ver `design.md` §Context).
> **Checkpoint de governance CERRADO** (tarea 1.3): el usuario aprobó las cinco recomendaciones de `design.md` sin cambios — **D-1** (módulo nuevo `exceptions/domain.py` con jerarquía `DomainError → EmailAlreadyExistsError`), **D-3** (captura amplia de `IntegrityError` anclada con test estructural), **D-4** (normalización de email **simétrica** en `create` y `get_by_email` — extensión respecto del texto de `CHANGES.md`), **D-5** (`flush` + `refresh` sin `commit` ni `rollback` en el repositorio; el límite transaccional queda para la `AuthUoW` de CHANGE-04) y **D-6** (`aiosqlite` como dependencia **de desarrollo** nueva). Ninguna requirió actualizar `design.md`/`specs/`/`tasks.md`.
>
> **Strict TDD**: cada tarea de implementación arranca por el test (RED), sigue con el mínimo código (GREEN), triangula con casos adicionales y refactoriza. El grupo 2 monta la infraestructura de test **antes** que cualquier código de producción: sin un motor ejecutable no hay RED posible para D-3 ni para D-5.

## 1. Red de seguridad y checkpoint de governance

- [x] 1.1 Correr la suite completa de `fastapi_bridge/tests/` y registrar el baseline (`N passed`). Si algo ya falla, reportarlo como falla preexistente y **no** arreglarlo dentro de este change. Baseline esperado según el cierre de CHANGE-02: 142 passed.
  - **Resultado**: `182 passed`, 0 fallas preexistentes. El baseline real es mayor al esperado (142) porque `CHANGE-00d` (`fastapi-cors-ratelimit`) se mergeó después del cierre de CHANGE-02 y agregó tests de CORS/rate-limit. Comando: `.venv/Scripts/python.exe -m pytest tests/ -q`.
- [x] 1.2 Verificar el estado del venv: `sqlalchemy` 2.0.x y `greenlet` instalados (ambos requeridos por `AsyncSession`), y confirmar que `aiosqlite` **no** está instalado todavía — la tarea 2.1 lo agrega. Anotar las versiones exactas.
  - **Resultado**: `SQLAlchemy 2.0.52`, `greenlet 3.5.5` instalados; `aiosqlite` no estaba instalado (`pip show` → "Package(s) not found").
- [x] 1.3 **Checkpoint con el usuario (MEDIUM governance)**: presentar las cinco decisiones **[REVISAR]** de `design.md` (D-1, D-3, D-4, D-5, D-6) con su recomendación y su alternativa. Registrar la respuesta de cada una acá mismo. Si alguna se rechaza, actualizar `design.md`, `specs/user-registry/spec.md` y este `tasks.md` **antes** de escribir código. No abrir el grupo 2 con decisiones pendientes.
  - **Respuestas del usuario** (aprobadas en sesión, sin cambios a `design.md`/`specs/`):
    - **D-1** — Aprobado: módulo nuevo `exceptions/domain.py` con jerarquía `DomainError → EmailAlreadyExistsError`.
    - **D-3** — Aprobado: captura amplia de `IntegrityError`, anclada con el test estructural de 5.10 (no inspección de SQLSTATE).
    - **D-4** — Aprobado: normalización simétrica (`create` y `get_by_email`), como extensión documentada del roadmap.
    - **D-5** — Aprobado: `flush()` + `refresh()` sin `commit()`/`rollback()`; el límite transaccional queda para la `AuthUoW` de CHANGE-04.
    - **D-6** — Aprobado: `aiosqlite` como dependencia de desarrollo nueva, solo en `requirements-dev.txt`.
- [x] 1.4 Releer `fastapi_bridge/repositories/user_repository.py` y `fastapi_bridge/uow/auth_unit_of_work.py`: ambos docstrings placeholder anuncian una firma `create(user)` desactualizada (el roadmap especifica `create(email, hashed_password)`, D-8) y citan números de change obsoletos ("CHANGE-02/CHANGE-04"). Confirmar además que `fastapi_bridge/exceptions/domain.py` **no** existe y que `exceptions/__init__.py` sí, de modo que el módulo nuevo del grupo 3 sea importable sin tocar el paquete.
  - **Resultado**: confirmado. Ambos docstrings corregidos en 6.7 (repositorio) y 1.4 (UoW, corregido de paso sin implementar nada de la UoW misma).
- [x] 1.5 Confirmar que `tests/test_structure.py` verifica una **lista de módulos esperados** (existencia + importabilidad) y no un conjunto exacto del árbol: `exceptions/domain.py` como módulo adicional no debe romperlo. Verificar lo mismo para `tests/test_no_shared_db_impact.py`, que excluye `tests/` de su barrido.
  - **Resultado**: confirmado por lectura de ambos módulos; `exceptions/domain.py` no rompió `test_structure.py` (sigue en verde tras el grupo 3).

## 2. Infraestructura de test: motor async real en memoria (D-6)

- [x] 2.1 Agregar `aiosqlite>=0.20` a `fastapi_bridge/requirements-dev.txt` (**no** a `requirements.txt`: producción habla únicamente `asyncpg`) e instalarlo en el venv. Dejar un comentario en el archivo explicando que existe solo para la suite.
- [x] 2.2 RED — agregar a `fastapi_bridge/tests/conftest.py` una fixture async de sesión (`user_session`) que: construya su **propio** `create_async_engine("sqlite+aiosqlite:///:memory:")` sin pasar por `get_engine`/`get_session_factory` (para que ningún test pueda apuntar accidentalmente a `db_fuzzing`), cree **únicamente** `User.__table__` vía `run_sync(Base.metadata.create_all, tables=[User.__table__])` —mismo alcance explícito que el `lifespan` de CHANGE-01—, entregue una `AsyncSession` con `expire_on_commit=False`, y al terminar cierre la sesión y haga `dispose()` del engine. Escribir un test de humo que inserte un `User` con la sesión de la fixture y lo recupere.
- [x] 2.3 GREEN — hacer pasar el test de humo. Confirmar que la tabla se crea, que el `id` autoincremental lo genera el motor y que `created_at` queda poblado por `func.now()` sin pasarlo.
- [x] 2.4 TRIANGULATE — test de que la fixture aísla: dos usos consecutivos de la fixture arrancan con la tabla vacía (base en memoria por test), de modo que el orden de los tests no pueda influir en el resultado.
- [x] 2.5 TRIANGULATE — test que confirma que la constraint `UNIQUE` de `email` **está activa en este motor**: insertar dos veces el mismo email y hacer `flush` produce un `sqlalchemy.exc.IntegrityError`. Sin este test, todo el grupo 5 estaría probando una traducción de un error que nunca ocurre. Este test valida la premisa de D-6, no el código del repositorio.
- [x] 2.6 REFACTOR — documentar en el docstring de la fixture los límites de fidelidad de SQLite frente a PostgreSQL (D-6): `String(320)` no valida longitud, `DateTime(timezone=True)` devuelve naive, no hay SQLSTATE. Anotar que la cobertura de esos aspectos vive en `tests/test_user_model.py` (DDL compilado contra el dialecto PostgreSQL, CHANGE-01) y que ningún test de este change debe apoyarse en ellos.

## 3. `EmailAlreadyExistsError` — error de dominio (D-1, D-2)

- [x] 3.1 RED — crear `fastapi_bridge/tests/test_domain_exceptions.py` importando `DomainError` y `EmailAlreadyExistsError` desde `fastapi_bridge.exceptions.domain` (módulo que todavía no existe: el `ModuleNotFoundError` **es** el RED). Tests: `EmailAlreadyExistsError("user@test.com")` se construye; expone `.email`; hereda de `DomainError`; `DomainError` hereda de `Exception`.
- [x] 3.2 GREEN — crear `fastapi_bridge/exceptions/domain.py` con `class DomainError(Exception)` y `class EmailAlreadyExistsError(DomainError)`, cuyo `__init__(self, email: str) -> None` guarda `self.email` y compone el mensaje a partir de él. Type hints obligatorios. Sin imports de FastAPI, Starlette, slowapi, SQLAlchemy ni passlib.
- [x] 3.3 TRIANGULATE — tests del mensaje y del atributo: `str(exc)` contiene el email; `exc.email` devuelve exactamente el valor pasado (no el mensaje completo); la excepción se puede capturar tanto por `EmailAlreadyExistsError` como por `DomainError`, y `except DomainError` la atrapa (esto es lo que habilita el handler único de CHANGE-07).
- [x] 3.4 TRIANGULATE — test de independencia del framework web: reutilizar el helper AST `get_imported_top_level_modules` de `tests/test_layer_boundaries.py` para afirmar que `exceptions/domain.py` no importa `fastapi`, `starlette`, `slowapi`, `sqlalchemy` ni `passlib`. Es la condición que permite que el repositorio lo importe sin violar su frontera de capa.
- [x] 3.5 REFACTOR — escribir el docstring de `exceptions/domain.py`: qué es un error de dominio, por qué vive en un módulo propio y no en `handlers.py` (que importa Starlette) ni en el módulo del repositorio (dependencia invertida, mismo razonamiento que D-10 de CHANGE-02), y que CHANGE-04 (`InvalidCredentialsError`) y CHANGE-07 (handler sobre `DomainError` → RFC 7807) lo extienden en vez de reescribirlo.

## 4. Frontera de capa de `repositories/` (D-9)

- [x] 4.1 RED — agregar la fila `("repositories", "passlib")` a `LAYER_IMPORT_RULES` en `fastapi_bridge/tests/test_layer_boundaries.py`, junto a la fila `("repositories", "fastapi")` ya existente. Confirmar que el test parametrizado nuevo aparece y pasa sobre el placeholder actual.
- [x] 4.2 Verificar que el test negativo `test_helper_detects_a_forbidden_import` sigue en verde: la fila nueva no vale nada si el helper dejara de detectar imports. **No** agregar `("repositories", "pydantic")` — ninguna regla del proyecto lo respalda y rompería a `n8n_repository.py` en CHANGE-09 (D-9).

## 5. `UserRepository.create` — alta, normalización y unicidad (D-3, D-4, D-5, D-8)

> Se implementa `create` antes que `get_by_email` a propósito: sin la capacidad de dar de alta un usuario, los tests de búsqueda tendrían que insertar por su cuenta con SQL crudo, duplicando lo que este grupo construye.

- [x] 5.1 RED — crear `fastapi_bridge/tests/test_user_repository.py` e importar `UserRepository` desde `fastapi_bridge.repositories.user_repository` (todavía inexistente: el `ImportError` es el RED). Tests: el constructor acepta la `AsyncSession` de la fixture; `create("user@test.com", "$2b$12$fakehash")` devuelve un `User`; el `id` del `User` devuelto no es `None`.
- [x] 5.2 GREEN — implementar `class UserRepository` con `__init__(self, session: AsyncSession) -> None` que solo guarda la sesión (sin crear engines, sin leer `Settings`) y `async def create(self, email: str, hashed_password: str) -> User` que construye el `User`, hace `session.add(...)` y `await session.flush()` (D-5), y devuelve la entidad. Type hints completos. Sin imports de `fastapi` ni de `passlib`.
- [x] 5.3 TRIANGULATE — tests de los valores generados por la base: `created_at` está poblado sin haberlo pasado (agregar el `await session.refresh(user)` posterior al `flush` que lo garantiza, D-5); el `hashed_password` almacenado es **exactamente** el string recibido, sin transformar, recortar ni re-hashear (RN-WS-12: el repositorio trata el hash como texto opaco); dos altas consecutivas con emails distintos reciben `id` distintos generados por el motor.
- [x] 5.4 RED — test de normalización en escritura (D-4): `create("USER@TEST.COM", ...)` almacena `user@test.com`. Verificar leyendo de la base con una consulta propia, no leyendo el atributo del objeto en memoria — así el test falla si se normaliza solo en el objeto Python y no en lo persistido.
- [x] 5.5 GREEN — implementar la normalización con una función de módulo `_normalize_email(email: str) -> str` (`.strip().lower()`) e invocarla desde `create`. Definirla **una sola vez**: el grupo 6 la reutiliza; el spec declara la simetría como normativa.
- [x] 5.6 TRIANGULATE — casos adicionales de normalización: email con capitalización mixta (`User@Test.Com`); email con espacios al principio o al final; email ya en minúsculas queda idéntico (la normalización es idempotente).
- [x] 5.7 RED — test del email duplicado (D-3): dar de alta `user@test.com`, luego intentar `create` del mismo email y esperar `EmailAlreadyExistsError`. Confirmar que el RED actual es un `IntegrityError` crudo escapándose del repositorio — ese es exactamente el error que este change existe para traducir.
- [x] 5.8 GREEN — envolver el `flush` en `try/except IntegrityError` y traducir con `raise EmailAlreadyExistsError(normalized_email) from exc` (D-1, D-2, D-3). **No** hacer `rollback` acá: la sesión queda en manos de la `AuthUoW` (D-5). **No** inspeccionar `exc.orig` ni el SQLSTATE (D-3).
- [x] 5.9 TRIANGULATE — casos adicionales del duplicado: (a) el alta duplicada con **otra capitalización** (`USER@TEST.COM` sobre un `user@test.com` existente) también lanza `EmailAlreadyExistsError` — es la intersección de D-3 con D-4 y el escenario que más fácil se rompe si la normalización se vuelve asimétrica; (b) `exc.email` contiene el email **normalizado**, no el original con mayúsculas (D-2); (c) `exc.__cause__` es el `IntegrityError` original (encadenamiento preservado); (d) tras el conflicto no queda un segundo usuario en la tabla.
- [x] 5.10 TRIANGULATE — **test-ancla de R-2/D-3**: afirmar sobre `User.__table__` que tiene exactamente **una** constraint de unicidad y **ninguna** clave foránea. Es lo que hace correcta la captura amplia de `IntegrityError`; si un change futuro agrega otra constraint, este test se pone rojo y obliga a revisar D-3. Comentar esa intención en el test.
- [x] 5.11 TRIANGULATE — test de que el repositorio **no** es dueño de la transacción (D-5): dar de alta un usuario, hacer `rollback` en el test y confirmar que el usuario no quedó persistido — prueba que `create` no había commiteado por su cuenta. Complementar con un test AST sobre `user_repository.py` que afirme que el código no invoca `commit` ni `rollback` sobre la sesión.

## 6. `UserRepository.get_by_email` — búsqueda (D-4, D-7)

- [x] 6.1 RED — tests de búsqueda: sobre un usuario dado de alta con `create`, `get_by_email` con su email devuelve ese usuario (mismo `id`); con un email inexistente devuelve `None` **sin lanzar excepción**.
- [x] 6.2 GREEN — implementar `async def get_by_email(self, email: str) -> User | None` con `select(User).where(User.email == _normalize_email(email))`, `await self._session.execute(...)` y `.scalar_one_or_none()` (D-7). Reutilizar la función de normalización del grupo 5, sin duplicarla.
- [x] 6.3 TRIANGULATE — tests de la simetría de normalización (D-4, el corazón de R-1): un usuario dado de alta como `user@test.com` se encuentra buscando `USER@TEST.COM`, `User@Test.Com` y `  user@test.com  `. Comentar en el test **por qué** existe: sin esto, quien se registra con mayúsculas no puede volver a entrar, y el 401 genérico de login (HU-03-02) vuelve el síntoma indistinguible de una contraseña mal escrita.
- [x] 6.4 TRIANGULATE — tests de coincidencia exacta: existiendo `alguien@test.com`, buscar `alguien@test.com.ar` devuelve `None` y buscar `guien@test.com` devuelve `None`. La coincidencia es por igualdad del email completo, no por prefijo, sufijo ni patrón.
- [x] 6.5 TRIANGULATE — test de parámetro ligado: buscar por un email que contiene comillas simples y un fragmento de SQL (`"a' OR '1'='1"`) devuelve `None` y **no** altera la tabla (contarla antes y después). Confirma que la consulta no se arma concatenando texto.
- [x] 6.6 TRIANGULATE — test de multiplicidad: con dos usuarios distintos en la tabla, `get_by_email` de cada uno devuelve el correcto y no el otro.
- [x] 6.7 REFACTOR — reemplazar el docstring placeholder de `repositories/user_repository.py` por documentación real: qué métodos expone y con qué firma (D-8, corrigiendo el `create(user)` obsoleto), que la sesión llega inyectada y que el repositorio **no** commitea (D-5), que el email se normaliza en ambos sentidos y por qué (D-4), que la captura de `IntegrityError` es amplia y qué test la ancla (D-3, R-2), y que el módulo no conoce FastAPI ni passlib (RN-WS-12, D-9). Correr la suite tras el refactor.

## 7. Verificación e integración con la suite existente

- [x] 7.1 Correr la suite completa y comparar contra el baseline de 1.1: ningún test previamente verde puede quedar en rojo. Prestar atención a `tests/test_structure.py` (el módulo adicional `exceptions/domain.py` no debe romperlo, 1.5) y a `tests/test_layer_boundaries.py` con la fila nueva.
- [x] 7.2 Verificar que `tests/test_no_shared_db_impact.py` sigue en verde: el repositorio no referencia `scans` ni `vulnerabilities`, no emite DDL y no llama `create_all` ni `create_async_engine` a nivel de módulo en código de producción (DD-02). La fixture de 2.2 vive en `tests/`, que ese test excluye de su barrido.
- [x] 7.3 Verificar que `tests/test_settings.py`, `tests/test_env_contract.py` y `tests/test_db_session.py` siguen en verde: este change no agrega variables de entorno, no toca `Settings` y no modifica la factory de sesiones de CHANGE-01.
- [x] 7.4 Confirmar de punta a punta la ausencia de contraseña en claro (RN-WS-12): el repositorio no tiene ningún parámetro ni atributo llamado `password` a secas, solo `hashed_password`, y nunca invoca nada de hashing.
- [x] 7.5 Verificar que `requirements.txt` **no** cambió (solo `requirements-dev.txt`), y que ningún módulo de producción importa `aiosqlite` (D-6, R-6).
- [x] 7.6 Correr `openspec validate user-repository --strict` y confirmar que pasa.

## 8. Cierre y traspaso

- [x] 8.1 Marcar `[CHANGE-03] user-repository` como completado en `CHANGES.md`, tildando sus criterios de aceptación y agregando una **Nota de implementación** con las desviaciones respecto del scope original que el usuario haya aprobado en 1.3: (a) la normalización **simétrica** del email, extendida a `get_by_email` y no solo a `create` como decía el roadmap (D-4); (b) `EmailAlreadyExistsError` alojada en el módulo nuevo `exceptions/domain.py` con base `DomainError`, ubicación que el roadmap no especificaba (D-1); (c) `flush` sin `commit`, con el límite transaccional traspasado a CHANGE-04 (D-5); (d) `aiosqlite` como dependencia de desarrollo nueva (D-6). Corregir además el nivel de governance registrado para CHANGE-03, que en `CHANGES.md` figura como **CRITICO** mientras que el `CLAUDE.md` del proyecto lo baja explícitamente a **MEDIUM** para todo el dominio Auth (CHANGE-01..07) — la misma corrección que se aplicó en CHANGE-02.
- [x] 8.2 Anotar en la sección de **CHANGE-04** de `CHANGES.md` los dos traspasos que este change deja abiertos: la `AuthUoW` debe hacer `commit` en el camino feliz y `rollback` ante cualquier excepción (D-5, R-4), y **no** debe reemplazar la captura de `IntegrityError` por un chequeo previo con `get_by_email` — ese chequeo es una optimización de UX, no la garantía de RN-WS-13, que la da el motor (R-7).
- [x] 8.3 Anotar en la sección de **CHANGE-07** de `CHANGES.md` que `EmailAlreadyExistsError` debe mapearse a `409 Conflict` en RFC 7807 vía `problem_detail_response(...)` de `exceptions/handlers.py`, usando `exc.email` para componer el `detail` (D-2), y que conviene registrar el handler sobre la base `DomainError` en vez de una excepción concreta (D-1).
- [x] 8.4 Verificar que la nota bloqueante R-1 de CHANGE-02 (`passlib 1.7.4` roto contra `bcrypt 5.0.0`) sigue registrada y sin resolver: no es asunto de este change —el repositorio nunca importa `passlib`, y la fila de 4.1 lo garantiza—, pero es lo primero que bloquea a CHANGE-04.
- [x] 8.5 Guardar en Engram las decisiones de este change (D-1, D-3, D-4, D-5, D-6) y las respuestas del checkpoint de 1.3, con `topic_key: "opsx/user-repository/apply"`.
