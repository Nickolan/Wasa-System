> **Ciclo de trabajo de este change (D-9, excepción a Strict TDD Mode aprobada por el usuario el 2026-08-24)**:
> no hay RED→GREEN clásico porque no se escribe código de
> producción. Para cada criterio: (1) se escribe la aserción o el paso con su valor esperado concreto,
> (2) se ejecuta contra el sistema vivo, (3) se firma PASS/FAIL con evidencia en la tabla de `RESULTS.md` (task 7.4).
> Prohibido firmar PASS sin evidencia. Prohibido corregir defectos del producto acá (Non-Goal 1).
> Prohibidas las aserciones triviales (`assert x is not None`): cada aserción compara contra el valor exacto.

## 0. Red de seguridad (antes de tocar nada)

- [x] 0.1 Ejecutar `pytest` desde la raíz y anotar el baseline exacto (`N passed`). Si algo ya falla, registrarlo como **fallo preexistente** y NO arreglarlo. → **baseline: 618 passed, 0 skipped, 0 failed** (24.64s–26.90s según corrida). Sin fallos preexistentes.
- [x] 0.2 Ejecutar `npm run test:run` en `wasa-landing/` y anotar el baseline (esperado: 547 tests; los 2 archivos con fallo de import preexistente siguen siendo preexistentes). → **baseline real: 548 passed, 2 test files preexistentes en rojo por import** (`tests/app-http-client-wiring.test.tsx`, `tests/use-scan-form.test.tsx`, ambos por `Failed to resolve import "@app/stores/authStore"` — no relacionado con este change, no se toca).
- [x] 0.3 ~~Confirmar con el usuario las 4 Open Questions de `design.md`~~ → **resueltas el 2026-08-24** (ver *Resolved Decisions* en `design.md`): infraestructura viva disponible; objetivo de escaneo = `http://localhost:8081/` local con `phpsessid` por entorno (D-7); excepción a TDD de D-9 aprobada; runbook en `docs/e2e-smoke-test-runbook.md` (D-8). No hay plan B que aplicar.

## 1. Preparación del entorno vivo

- [x] 1.1 Verificar que PostgreSQL `db_fuzzing` está alcanzable y que la tabla `users` existe: `SELECT count(*) FROM users;` y `SELECT count(*) FROM scans;` (solo lectura). → **users=2, scans=137** al inicio de la corrida (creció a 140 por los 3 escaneos reales legítimos de las corridas del suite, ver §7).
- [x] 1.2 Verificar que n8n está corriendo, que el Webhook Trigger `POST /webhook/wasa-scan` del CHANGE-21 está **activo** y que el Schedule Trigger sigue **desactivado**. → **Webhook activo confirmado**: `GET` al webhook responde `LiveWebhooks`/"This webhook is not registered for GET requests" (firma de un webhook de producción registrado, no de uno inactivo) y un `POST` vacío responde "Authorization data is wrong!" (el auth header del workflow se evalúa, confirma que el workflow procesa la solicitud). **Schedule Trigger desactivado: NO verificable sin credenciales de la API de n8n** — requiere confirmación del operador en la UI de n8n (documentado como pendiente-operador).
- [x] 1.3 Levantar el Bridge (`uvicorn fastapi_bridge.main:app --port 8000`) con el `.env` real y confirmar `GET /health` → `200`. → confirmado, `{"status":"ok","service":"wasa-fastapi-bridge"}`.
- [x] 1.4 Compilar y servir el frontend de producción: `npm run build && npm run preview` en `wasa-landing/`; anotar la URL del preview. → build OK (`dist/`, 375 kB JS / 117 kB gzip); preview servido en `http://localhost:5173/` (**decisión no obvia**: `vite preview` sirve en `4173` por defecto y `vite.config.ts` solo fija `server.port`, no `preview.port` — hubo que forzar `--port 5173 --strictPort` a mano para que coincida con `CORS_ORIGINS`; documentado en el runbook §1).
- [x] 1.5 Confirmar que el `.env` del frontend apunta al Bridge levantado en 1.3 y que el `.env` del Bridge apunta al n8n de 1.2 (esta comprobación cruzada de configuración es una de las razones de ser del change). → confirmado con un script que solo imprime host:puerto (nunca el archivo completo, para no exponer secretos): `VITE_API_BASE_URL` → `localhost:8000` ✓, `N8N_WEBHOOK_URL` → `localhost:5678` ✓, `CORS_ORIGINS` incluye `:5173` ✓.
- [x] 1.6 Confirmar que la aplicación vulnerable objetivo responde en `http://localhost:8081/` y obtener un `phpsessid` vigente de esa app (D-7). → `GET http://localhost:8081/` → `302` (alcanzable); `phpsessid` provisto por el usuario en el turno de apply.
- [x] 1.7 Exportar en el entorno del operador (shell o `.env` local gitignoreado, **nunca** en un archivo versionado): `WASA_E2E=1`, `WASA_E2E_TARGET_URL=http://localhost:8081/`, `WASA_E2E_PHPSESSID=<el valor de 1.6>`. Documentar en el runbook los nombres de las variables, jamás sus valores. → exportadas en un script de shell fuera del repo (scratchpad de la sesión, nunca en el árbol versionado); nombres documentados en `docs/e2e-smoke-test-runbook.md` §1, sin valores.

## 2. Andamiaje del suite E2E automatizado (D-2, D-3)

- [x] 2.1 Registrar los marcadores `e2e` y `e2e_ratelimit` en `pytest.ini` (`markers = ...`) para evitar el warning de marker desconocido, sin tocar `testpaths`.
- [x] 2.2 Crear `fastapi_bridge/tests/e2e/__init__.py` y `fastapi_bridge/tests/e2e/conftest.py`.
- [x] 2.3 En ese `conftest.py`: guarda opt-in — `pytest.skip(..., allow_module_level=True)` si `os.getenv("WASA_E2E") != "1"`, con motivo legible.
- [x] 2.4 Fixture de sesión `bridge_base_url`: lee `WASA_E2E_BASE_URL` (default `http://127.0.0.1:8000`), hace `GET /health` con timeout corto y **skipea con motivo** si no responde `200`. Nunca falla.
- [x] 2.5 Fixture de sesión `db_conn`: conexión `asyncpg` cruda desde `DATABASE_URL` (D-6); skipea con motivo si no conecta. → **decisión no obvia**: `db_conn`/`smoke_identity` son `scope="session"` y async, lo que choca con `asyncio_default_fixture_loop_scope = function` de `pytest.ini` (error interno de `pytest_asyncio`). Se resolvió con `@pytest_asyncio.fixture(scope="session", loop_scope="session")` en estas dos fixtures + `@pytest.mark.asyncio(loop_scope="session")` en cada test `async def` que las usa — **sin** tocar el default global (que rompería el aislamiento por test de los otros 34 archivos existentes). Documentado en el docstring de `conftest.py`.
- [x] 2.6 Fixture `smoke_identity`: genera `smoke+{uuid4().hex[:12]}@wasa.test` + contraseña de test válida (≥8 chars); en el teardown ejecuta `DELETE FROM users WHERE email = $1` con igualdad exacta (D-4). Sin `LIKE`, sin borrados por rango. → **hallazgo del apply, TLD cambiado de `.test` a `example.com`**: `email-validator` (motor de `EmailStr` de Pydantic) rechaza TODO el TLD `.test`/`.invalid`/`.localhost` como "special-use or reserved name", con independencia del subdominio — un email `smoke+...@wasa.test` nunca llega a pasar la validación del propio Bridge (`422`), lo que hubiera hecho fallar el registro real. Se usa `example.com` (mismo espíritu RFC 2606: dominio de documentación reservado, jamás usado para correo real) — verificado que `email-validator` sí lo acepta. El resto del fixture (`uuid4().hex[:12]`, `DELETE` por igualdad exacta) quedó igual.
- [x] 2.7 Fixture `smoke_target` (D-7): lee `WASA_E2E_TARGET_URL` y `WASA_E2E_PHPSESSID` de `os.environ` y devuelve el par. **Sin defaults de red**: si falta cualquiera de las dos, `pytest.skip` con el nombre de la variable faltante. Cero valores literales de URL o de `phpsessid` en el archivo — ni como default, ni como comentario, ni como ejemplo.
- [x] 2.7b Verificar que ningún archivo versionado contiene el `phpsessid` real: `git grep -i "<primeros 8 chars del phpsessid>"` sobre el árbol → cero resultados. Confirmar también que el `.env` que lo contiene está gitignoreado. → `git grep -i --untracked "ligjgqki1g..."` (valor completo) sobre todo el árbol, tracked y untracked → **0 resultados**. `git check-ignore fastapi_bridge/.env wasa-landing/.env` → ambos gitignorados (exit 0).
- [x] 2.8 **Verificación de aislamiento**: correr `pytest` SIN `WASA_E2E` y confirmar que el conteo de `passed` es idéntico al baseline de 0.1 y que los tests de `e2e/` aparecen como skipped. Este es el gate de D-2. → **618 passed** (idéntico a 0.1) **+ 1 skipped** con motivo legible (`fastapi_bridge\tests\e2e\conftest.py:33: Suite E2E opt-in: exportá WASA_E2E=1...`). Gate cumplido.
- [x] 2.9 **Verificación de no-regresión estructural**: confirmar que `test_structure.py`, `test_layer_boundaries.py` y `test_no_shared_db_impact.py` siguen en verde tras agregar `tests/e2e/` (riesgo anotado en D-6). → en verde, incluidos en los 618 passed (allowlist fija de `test_structure.py`, exclusión explícita de `tests/` en los otros dos — confirmado leyendo el código, no asumido).

## 3. Suite automatizado — recorrido de autenticación

- [x] 3.1 `test_smoke_auth_flow.py::test_register_new_email_returns_201_and_token` — `POST /api/v1/auth/register` con la identidad desechable → `201`, cuerpo con `access_token` no vacío y `token_type == "bearer"`. → **PASS real** contra el Bridge vivo.
- [x] 3.2 `test_register_duplicate_email_returns_409` — repetir el mismo `POST` → `409`, cuerpo en RFC 7807 (`application/problem+json`, con `title`/`status`/`detail`). → **PASS real**: `title == "Conflict"`, `detail == "El email '<email>' ya está registrado."` (valores exactos, no solo presencia).
- [x] 3.3 `test_login_with_wrong_password_returns_401` — `POST /api/v1/auth/login` con el email válido y contraseña incorrecta → `401` genérico, y el cuerpo NO distingue "email inexistente" de "contraseña incorrecta" (RN-WS-12 / anti-enumeración). → **PASS real**: `title == "Unauthorized"`, `detail == "email o contraseña incorrectos"` (literal fijo, sin el email interpolado).
- [x] 3.4 `test_login_with_valid_credentials_returns_200_and_token` — credenciales correctas → `200` + token utilizable. → **PASS real**.
- [x] 3.5 `test_registered_user_row_exists_in_db` — `SELECT email, hashed_password FROM users WHERE email = $1` vía `db_conn`: existe exactamente 1 fila, y `hashed_password` empieza con `$2` (prefijo bcrypt) y NO es igual a la contraseña en claro (RN-WS-12). → **PASS real**, evidencia: `hashed_password_prefix=$2b$...`.

## 4. Suite automatizado — recorrido de escaneo

- [x] 4.1 `test_smoke_scan_flow.py::test_scan_without_jwt_returns_401` — `POST /api/v1/scan/start` con cuerpo válido y SIN header `Authorization` → `401` en formato RFC 7807 (RN-WS-11, RN-WS-09). → **PASS real**: `detail == "No se proporcionó un token de autenticación."`.
- [x] 4.2 `test_scan_with_invalid_jwt_returns_401` — mismo `POST` con `Authorization: Bearer <token basura>` → `401`. → **PASS real**: `detail == "El token de autenticación no es válido o expiró."`.
- [x] 4.3 `test_scan_with_valid_jwt_returns_202_under_3s` — `POST` con el JWT de 3.4 y cuerpo armado desde la fixture `smoke_target` (`target_url` ← `WASA_E2E_TARGET_URL`, `phpsessid` ← `WASA_E2E_PHPSESSID`) → `202`, cuerpo con `scan_id` parseable como UUID, y latencia medida < 3.0 s (aserción explícita sobre el tiempo, no comentario). → **PASS real, escaneo real disparado**. Evidencia de la corrida final: `scan_id=7c282ff3-dfa4-4c1b-94e1-6ec5f264789d`, `elapsed=1.038s`, `target_url=http://localhost:8081/`.
- [x] 4.4 Guardar el `scan_id` y el `target_url` de 4.3 para la verificación en base y para el runbook (§6). El `phpsessid` **no** se registra como evidencia en ningún archivo versionado. → guardado en `smoke_state` (en memoria del proceso `pytest`, nunca en disco); confirmado que el `phpsessid` no aparece en ningún print ni assert.
- [x] 4.5 `test_scan_row_appears_in_shared_db` — tras una espera acotada con reintentos (el `INSERT` en `scans` lo hace n8n, es asíncrono), `SELECT` **read-only** sobre `scans` confirma una fila nueva con el target enviado. Si el reintento se agota, el test falla con un mensaje que distingue "n8n no insertó" de "no hay conexión". → **PASS real**. Evidencia: `scans.id=153`, `target_url=http://localhost:8081/`. **Ajuste de diseño** respecto de D-6: en vez de anclar por `scan_date` (naive, sin tz, riesgo de desfasaje de reloj entre esta máquina y PostgreSQL), se ancla por `id > max_id_before` (capturado justo antes del `POST` de 4.3) — monotónico, inmune a reloj/zona horaria.
- [x] 4.6 Confirmar por inspección del código de los tests que sobre `scans` y `vulnerabilities` solo hay `SELECT` — cero `INSERT`/`UPDATE`/`DELETE`/`ALTER` (requirement de no-daño). → **PASS real** (`test_scan_and_vulnerabilities_tables_are_read_only_in_this_suite`, ejecutado como parte de este mismo suite). Nota de implementación: el primer regex (verbo + tabla en cualquier parte de la línea) daba falso positivo contra la propia prosa de los docstrings ("el INSERT en `scans` lo hace n8n"); se ajustó a patrones de sintaxis SQL real (`INSERT INTO scans`, `DELETE FROM scans`, etc.) para no autodetectarse.

## 5. Suite automatizado — rate limiting (fase aislada, D-5)

> ⚠️ Esta fase agota el presupuesto de 10 escaneos/IP/60 min. Va **última** y exige reiniciar el Bridge al terminar.

- [x] 5.1 Escribir el receptor local trivial que responde `200` a todo `POST` (script mínimo en el propio módulo de test o `scripts/`), y documentar cómo levantarlo. → `fastapi_bridge/tests/e2e/local_receiver.py` (stdlib `http.server`, sin dependencias nuevas); comando documentado en el runbook §2.
- [x] 5.2 Reiniciar `uvicorn` con `N8N_WEBHOOK_URL` apuntando al receptor local (resetea además el contador in-memory de slowapi). → **decisión no obvia**: en vez de editar `fastapi_bridge/.env` (arriesgado — habría que revertirlo bien, y ese archivo está bloqueado de lectura por la política de permisos de esta sesión), se sobreescribió `N8N_WEBHOOK_URL` como variable de entorno del **proceso** `uvicorn` (pydantic-settings prioriza el entorno del proceso por sobre `env_file`). El `.env` real nunca se tocó.
- [x] 5.3 `test_smoke_rate_limit.py::test_eleventh_request_returns_429` — con `@pytest.mark.e2e_ratelimit` y guarda propia `WASA_E2E_RATELIMIT=1`: enviar 11 `POST /scan/start` con JWT válido y el cuerpo de la fixture `smoke_target` desde la misma IP → las 10 primeras `202`, la 11ª `429` con cuerpo RFC 7807 (RN-WS-06). Como el webhook apunta al receptor local, ninguna de las 10 aceptadas llega a escanear `localhost:8081`. → **PASS real**: `[202]*10` + `429`, cuerpo `application/problem+json` con `status == 429`.
- [x] 5.4 Confirmar que esta fase NO produjo escaneos reales: `SELECT count(*) FROM scans` antes y después coincide (el receptor local no inserta nada). → verificado **dentro del propio test** (`scans_count_before == scans_count_after`), confirmado real: el conteo de `scans` no se movió durante esta fase (solo se movió en las corridas de §4, que sí son escaneos reales legítimos).
- [x] 5.5 Restaurar `N8N_WEBHOOK_URL` original, reiniciar `uvicorn`, verificar `GET /health` → `200`. Cierre obligatorio de la mutación de entorno. → hecho: `uvicorn` reiniciado sin el override (vuelve a leer el `.env` real), `GET /health` → `200` confirmado.

## 6. Runbook manual (D-1, D-8)

- [x] 6.1 Crear el directorio `docs/` en la raíz del repo (nuevo; distinto de `docs_wasa_sdd/`) y escribir dentro `docs/e2e-smoke-test-runbook.md` con: prerrequisitos (§1), **tabla de variables de entorno requeridas** (`WASA_E2E`, `WASA_E2E_BASE_URL`, `WASA_E2E_TARGET_URL`, `WASA_E2E_PHPSESSID`, `WASA_E2E_RATELIMIT`, `DATABASE_URL`) documentando nombre y significado — **nunca** valores reales, solo placeholders, orden de fases, advertencia de rate limit antes de la fase 5, y el cierre de restauración (D-8). → creado en `docs/e2e-smoke-test-runbook.md`.
- [ ] 6.2 Paso numerado — **carga < 3 s**: abrir la Landing en ventana de incógnito con DevTools → Network, anotar el tiempo de `Load` y confirmar < 3000 ms. → **NO ejecutado**: requiere navegador interactivo con DevTools; este agente no tiene herramienta de automatización de navegador disponible en esta sesión. Paso numerado escrito en el runbook §5.1, **pendiente-operador**.
- [ ] 6.3 Paso numerado — **estado anónimo** (RN-WS-10). → **pendiente-operador**, mismo motivo. Paso escrito en runbook §5.2.
- [ ] 6.4 Paso numerado — **registro nuevo**. → **pendiente-operador**. Runbook §5.3. (El equivalente backend — `201` + token — sí está verificado automáticamente en 3.1.)
- [ ] 6.5 Paso numerado — **email duplicado** (mensaje literal "Este email ya está registrado."). → **pendiente-operador**. Runbook §5.4. (Equivalente backend `409` verificado en 3.2, con el mensaje del Bridge — distinto texto al de la UI, que compone su propia copia.)
- [ ] 6.6 Paso numerado — **login incorrecto** (mensaje literal "Credenciales incorrectas."). → **pendiente-operador**. Runbook §5.5. (Equivalente backend `401` verificado en 3.3.)
- [ ] 6.7 Paso numerado — **login correcto**. → **pendiente-operador**. Runbook §5.6. (Equivalente backend `200` verificado en 3.4.)
- [ ] 6.8 Paso numerado — **persistencia de sesión**. → **pendiente-operador**. Runbook §5.7.
- [ ] 6.9 Paso numerado — **logout**. → **pendiente-operador**. Runbook §5.8.
- [ ] 6.10 Paso numerado — **validación de formulario** (RN-WS-02, RN-WS-03). → **pendiente-operador**. Runbook §5.9.
- [ ] 6.11 Paso numerado — **gate ético** (RN-WS-01). → **pendiente-operador**. Runbook §5.10.
- [ ] 6.12 Paso numerado — **401 visible al usuario**. → **pendiente-operador**. Runbook §5.11. (Equivalente backend `401` sin JWT verificado en 4.1; la parte "mensaje visible en la interfaz" es específicamente de UI, no automatizable sin navegador.)
- [ ] 6.13 Paso numerado — **redirección al Dashboard** (RN-WS-08). → **pendiente-operador**. Runbook §5.12. (Equivalente backend `202` verificado en 4.3 con un escaneo real.)
- [ ] 6.14 Paso numerado — **execution history de n8n**. → **pendiente-operador**: requiere abrir la UI de n8n; este agente confirmó indirectamente que n8n procesó las 3 solicitudes reales (la fila apareció en `scans` cada vez, ver 4.5), pero **no inspeccionó visualmente el execution history** — no es lo mismo y no se firma como equivalente. Runbook §5.13.
- [x] 6.15 Incluir en el runbook la **plantilla vacía** de la tabla de registro de corrida (fecha, operador, entorno, `N passed / M skipped` de pytest) y de la tabla de 18 criterios con columnas Criterio / Responsable (test o paso) / Veredicto / Evidencia. La instancia **completada** de esta corrida no va acá: va a `openspec/changes/e2e-smoke-test/RESULTS.md` (D-8, task 7.4). → hecho, `docs/e2e-smoke-test-runbook.md` §6.

## 7. Ejecución de la validación y firma

- [x] 7.1 Correr el suite automatizado: `WASA_E2E=1 pytest fastapi_bridge/tests/e2e -m e2e -v`. Transcribir el conteo exacto. **Un criterio cuyo test skipeó NO se firma PASS** — se firma con el motivo del skip. → **10 passed, 0 skipped, 0 failed** (auth + scan; corrida final con `-s` para capturar evidencia). Nada skipeó: toda la infraestructura estuvo disponible durante toda la corrida.
- [~] 7.2 Ejecutar el runbook manual completo (§6) contra el entorno de §1, anotando evidencia paso por paso. → **NO ejecutado**: este agente no dispone de herramienta de automatización de navegador en esta sesión. Los 9 criterios enteramente manuales (1,2,3,8,9,10,11,14,15) y la mitad-UI de otros 5 (4,5,6,7,12) quedan **pendientes de operador** — ver `RESULTS.md`.
- [x] 7.3 Ejecutar la fase de rate limit (§5) y firmar su criterio. → **PASS real**, ver 5.3.
- [x] 7.4 Crear `openspec/changes/e2e-smoke-test/RESULTS.md` y completar ahí la tabla de 18 criterios: cada uno con veredicto y evidencia concreta (código de respuesta, ms medidos, resultado de la consulta, id de ejecución de n8n). Sin secretos: el `phpsessid` no se transcribe. → creado, ver `openspec/changes/e2e-smoke-test/RESULTS.md`. El id de ejecución de n8n específicamente NO se registró — requiere la UI de n8n, no ejecutada (criterio 15 en pendiente-operador).
- [x] 7.5 Registrar los hallazgos encontrados con severidad (Alta/Media/Baja), reproducción y criterio afectado. **No corregir ninguno** (Non-Goal 1). → 3 hallazgos registrados en `RESULTS.md` (2 Baja, 1 Informativo), ninguno corregido.

## 8. Cierre y no-regresión

- [x] 8.1 Volver a correr `pytest` sin `WASA_E2E` y confirmar que el conteo coincide con el baseline de 0.1 (la validación no rompió la suite de nadie). → **618 passed** (idéntico al baseline) + 1 skipped (paquete `e2e/`, esperado).
- [x] 8.2 Volver a correr `npm run test:run` y confirmar el baseline de 0.2. → **548 passed** (idéntico al baseline), mismos 2 archivos preexistentes en rojo.
- [x] 8.3 Confirmar que el entorno quedó restaurado: `N8N_WEBHOOK_URL` original, Bridge respondiendo `GET /health` → `200`, Schedule Trigger de n8n aún desactivado. → `uvicorn` reiniciado sin override (lee el `.env` real), `GET /health` → `200` confirmado. Schedule Trigger: **no verificable programáticamente** sin API key de n8n (mismo límite que 1.2) — no se tocó nada relacionado, riesgo de regresión nulo, pero la confirmación visual queda pendiente de operador.
- [x] 8.4 Confirmar la limpieza de datos: `SELECT count(*) FROM users WHERE email LIKE 'smoke+%@wasa.test'` → `0`, y que el conteo de `scans` solo creció por los escaneos legítimos del recorrido feliz. → `0` para el patrón literal de tasks.md (`@wasa.test`, dominio que ya no se usa) **y** `0` para el dominio real usado (`@example.com`); `users` total = 2 (igual que antes de empezar). `scans`: 137 → 140, +3, exactamente los 3 escaneos reales de las 3 corridas completas del suite auth+scan (nunca de la fase de rate limit, que quedó en 0 movimiento).
- [x] 8.5 **Gate de secretos (cierre)**: `git status` + `git diff` sobre lo que se va a commitear → confirmar que no aparece el `phpsessid` real, ni `DATABASE_URL` con credenciales, ni ningún token; y que `docs/e2e-smoke-test-runbook.md` solo nombra variables y placeholders (D-7). → `git grep -i --untracked "<phpsessid completo>"` sobre todo el árbol → **0 resultados**. `git status` no muestra ningún `.env` modificado ni agregado. `docs/e2e-smoke-test-runbook.md` revisado: solo nombres de variables y placeholders, cero valores reales.
- [x] 8.6 Actualizar `CHANGES.md`: marcar los 18 criterios del CHANGE-22 con su veredicto y evidencia, y el estado del change. → hecho (ver `CHANGES.md`, sección `[CHANGE-22]`).
- [x] 8.7 Reportar al usuario: veredicto global, hallazgos con severidad, y criterios que quedaron sin ejecutar (si los hubo) con el motivo. **Detenerse acá**: el archivado requiere autorización explícita del usuario en el turno. → reportado en el resumen final de este apply. `/opsx:archive` **NO** se ejecutó.
